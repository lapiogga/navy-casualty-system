---
phase: 08-verification-deploy
plan: 07
type: execute
wave: 4
depends_on: ["08-06"]
files_modified:
  - deploy/deploy.sh
  - deploy/generate-cert.sh
  - deploy/backup.sh
  - deploy/restore.sh
  - deploy/package.sh
  - .env.example
  - docker-compose.prod.yml
  - Dockerfile
autonomous: true
requirements:
  - INFRA-01
  - INFRA-04
must_haves:
  truths:
    - "deploy.sh가 docker load + docker compose up을 자동 실행한다"
    - "generate-cert.sh가 PKCS12 자체 서명 인증서를 생성한다"
    - "backup.sh가 pg_dump + 7일 롤링 백업을 수행한다"
    - "restore.sh가 pg_restore로 복원한다"
    - "package.sh가 tar 아카이브를 생성한다"
    - ".env.example이 모든 필수 환경변수 플레이스홀더를 포함한다"
  artifacts:
    - path: "deploy/deploy.sh"
      provides: "자동 배포 스크립트"
      contains: "docker load"
    - path: "deploy/generate-cert.sh"
      provides: "인증서 생성"
      contains: "keytool"
    - path: "deploy/backup.sh"
      provides: "DB 백업"
      contains: "pg_dump"
    - path: "deploy/restore.sh"
      provides: "DB 복원"
      contains: "pg_restore"
    - path: "deploy/package.sh"
      provides: "오프라인 패키징"
      contains: "docker save"
    - path: ".env.example"
      provides: "환경변수 템플릿"
      contains: "DB_PASSWORD"
  key_links:
    - from: "deploy.sh"
      to: "docker-compose.prod.yml"
      via: "docker compose -f"
      pattern: "docker compose"
    - from: "package.sh"
      to: "Docker images"
      via: "docker save"
      pattern: "docker save"
---

<objective>
군 내부망 에어갭 환경용 배포 패키징 — deploy.sh, generate-cert.sh, backup/restore.sh, package.sh, .env.example를 완성한다.

Purpose: Docker 이미지 tar 아카이브 + 자립형 배포 스크립트로 인터넷 없이 단일 명령 배포를 가능하게 한다.
Output: deploy/ 디렉토리 스크립트 6종, .env.example, Dockerfile/docker-compose.prod.yml 최종 정리
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Dockerfile
@docker-compose.yml
@docker-compose.prod.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 배포 스크립트 6종 + .env.example</name>
  <files>
    deploy/deploy.sh,
    deploy/generate-cert.sh,
    deploy/backup.sh,
    deploy/restore.sh,
    deploy/package.sh,
    .env.example
  </files>
  <read_first>
    Dockerfile,
    docker-compose.yml,
    docker-compose.prod.yml,
    docker-compose.override.yml
  </read_first>
  <action>
    `mkdir -p deploy`

    per D-05/D-07: **deploy/package.sh** — 오프라인 배포 패키지 생성:
    ```bash
    #!/bin/bash
    set -euo pipefail
    VERSION="${1:-1.0.0}"
    echo "=== 해군 사상자 관리 시스템 v${VERSION} 패키지 빌드 ==="

    # 1. Docker 이미지 빌드
    docker compose -f docker-compose.yml -f docker-compose.prod.yml build

    # 2. 이미지 태깅
    docker tag navy-casualty-app:latest navy-casualty-app:v${VERSION}

    # 3. Docker 이미지 tar 아카이브
    echo "Docker 이미지 저장 중..."
    docker save navy-casualty-app:v${VERSION} postgres:16-alpine | gzip > navy-casualty-images-v${VERSION}.tar.gz

    # 4. 배포 패키지 조립
    echo "배포 패키지 조립 중..."
    tar czf navy-casualty-deploy-v${VERSION}.tar.gz \
      navy-casualty-images-v${VERSION}.tar.gz \
      docker-compose.yml \
      docker-compose.prod.yml \
      deploy/deploy.sh \
      deploy/generate-cert.sh \
      deploy/backup.sh \
      deploy/restore.sh \
      .env.example

    rm navy-casualty-images-v${VERSION}.tar.gz
    echo "=== 패키지 완료: navy-casualty-deploy-v${VERSION}.tar.gz ==="
    ls -lh navy-casualty-deploy-v${VERSION}.tar.gz
    ```

    per D-05: **deploy/deploy.sh** — 에어갭 배포 자동 실행:
    ```bash
    #!/bin/bash
    set -euo pipefail
    echo "=== 해군 사상자 관리 시스템 배포 ==="

    # 1. Docker 이미지 로드
    IMAGES_TAR=$(ls navy-casualty-images-*.tar.gz 2>/dev/null | head -1)
    if [ -z "$IMAGES_TAR" ]; then
      echo "오류: 이미지 파일(navy-casualty-images-*.tar.gz)을 찾을 수 없습니다."
      exit 1
    fi
    echo "이미지 로드 중: $IMAGES_TAR"
    docker load < "$IMAGES_TAR"

    # 2. 환경변수 파일 확인
    if [ ! -f .env ]; then
      echo "오류: .env 파일이 없습니다. .env.example을 복사하여 설정하세요."
      echo "  cp .env.example .env && vi .env"
      exit 1
    fi

    # 3. TLS 인증서 확인 (선택)
    if [ ! -d certs ]; then
      echo "경고: certs/ 디렉토리가 없습니다. TLS 없이 HTTP로 실행됩니다."
      echo "  TLS 적용: ./generate-cert.sh 실행 후 .env에서 SERVER_SSL_ENABLED=true 설정"
    fi

    # 4. Docker Compose 실행
    echo "서비스 시작 중..."
    docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

    # 5. 헬스체크 대기
    echo "서비스 상태 확인 중 (최대 120초)..."
    for i in $(seq 1 24); do
      if curl -sf http://localhost:8080/actuator/health > /dev/null 2>&1; then
        echo "서비스가 정상 시작되었습니다."
        docker compose ps
        exit 0
      fi
      sleep 5
    done
    echo "경고: 서비스가 120초 내에 시작되지 않았습니다. 로그를 확인하세요:"
    echo "  docker compose logs app"
    exit 1
    ```

    per D-06: **deploy/generate-cert.sh** — 자체 서명 PKCS12 인증서:
    ```bash
    #!/bin/bash
    set -euo pipefail
    CERT_DIR="./certs"
    KEYSTORE="$CERT_DIR/keystore.p12"
    ALIAS="navy-casualty"
    VALIDITY=365

    mkdir -p "$CERT_DIR"

    read -sp "키스토어 비밀번호 입력: " KS_PASSWORD
    echo

    # 자체 서명 인증서 + PKCS12 키스토어 생성
    keytool -genkeypair \
      -alias "$ALIAS" \
      -keyalg RSA \
      -keysize 2048 \
      -storetype PKCS12 \
      -keystore "$KEYSTORE" \
      -storepass "$KS_PASSWORD" \
      -validity "$VALIDITY" \
      -dname "CN=navy-casualty,OU=Navy,O=ROK Navy,L=Gyeryong,ST=Chungnam,C=KR"

    echo "인증서 생성 완료: $KEYSTORE"
    echo ""
    echo "다음 단계:"
    echo "1. .env 파일에 다음 설정 추가:"
    echo "   SERVER_SSL_ENABLED=true"
    echo "   SSL_KEYSTORE_PASSWORD=$KS_PASSWORD"
    echo "2. 실 운영 시에는 CA 발급 인증서로 교체하세요."
    ```

    per D-11: **deploy/backup.sh** — pg_dump 7일 롤링:
    ```bash
    #!/bin/bash
    set -euo pipefail
    BACKUP_DIR="${BACKUP_DIR:-./backups}"
    DB_USER="${DB_USER:-casualty}"
    DB_NAME="${DB_NAME:-casualty}"
    DB_HOST="${DB_HOST:-localhost}"
    DB_PORT="${DB_PORT:-5432}"
    RETENTION_DAYS=7

    mkdir -p "$BACKUP_DIR"
    FILENAME="$BACKUP_DIR/${DB_NAME}_$(date +%Y%m%d_%H%M%S).sql.gz"

    echo "백업 시작: $DB_NAME -> $FILENAME"
    PGPASSWORD="${DB_PASSWORD}" pg_dump \
      -h "$DB_HOST" -p "$DB_PORT" -U "$DB_USER" "$DB_NAME" \
      | gzip > "$FILENAME"

    echo "백업 완료: $(ls -lh "$FILENAME" | awk '{print $5}')"

    # 7일 초과 백업 삭제
    DELETED=$(find "$BACKUP_DIR" -name "${DB_NAME}_*.sql.gz" -mtime +${RETENTION_DAYS} -print -delete | wc -l)
    echo "오래된 백업 삭제: ${DELETED}건 (${RETENTION_DAYS}일 초과)"

    # cron 등록 안내
    echo ""
    echo "자동 백업 설정 (cron):"
    echo "  0 2 * * * cd $(pwd) && DB_PASSWORD=\$DB_PASSWORD ./deploy/backup.sh"
    ```

    per D-12: **deploy/restore.sh** — pg_restore:
    ```bash
    #!/bin/bash
    set -euo pipefail
    DB_USER="${DB_USER:-casualty}"
    DB_NAME="${DB_NAME:-casualty}"
    DB_HOST="${DB_HOST:-localhost}"
    DB_PORT="${DB_PORT:-5432}"

    if [ -z "${1:-}" ]; then
      echo "사용법: $0 <백업파일>"
      echo "예시: $0 backups/casualty_20260404_020000.sql.gz"
      ls -lt ${BACKUP_DIR:-./backups}/*.sql.gz 2>/dev/null | head -5
      exit 1
    fi

    BACKUP_FILE="$1"
    if [ ! -f "$BACKUP_FILE" ]; then
      echo "오류: 파일을 찾을 수 없습니다: $BACKUP_FILE"
      exit 1
    fi

    echo "경고: 기존 데이터베이스 '$DB_NAME'의 모든 데이터가 교체됩니다."
    read -p "계속하시겠습니까? (yes/no): " CONFIRM
    if [ "$CONFIRM" != "yes" ]; then
      echo "복원 취소"
      exit 0
    fi

    echo "복원 시작: $BACKUP_FILE -> $DB_NAME"
    gunzip -c "$BACKUP_FILE" | PGPASSWORD="${DB_PASSWORD}" psql \
      -h "$DB_HOST" -p "$DB_PORT" -U "$DB_USER" "$DB_NAME"

    echo "복원 완료"
    ```

    **.env.example** 생성 (프로젝트 루트):
    ```
    # 해군 사상자 관리 시스템 환경변수
    # 이 파일을 .env로 복사 후 실제 값으로 변경하세요

    # 데이터베이스 (필수)
    DB_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD
    DB_USER=casualty
    DB_NAME=casualty

    # 주민번호 암호화 키 (필수, 32바이트 Base64)
    PII_ENCRYPTION_KEY=CHANGE_ME_TO_BASE64_ENCODED_32BYTE_KEY

    # TLS 설정 (선택 — generate-cert.sh 실행 후 설정)
    # SERVER_SSL_ENABLED=true
    # SSL_KEYSTORE_PASSWORD=CHANGE_ME

    # Spring 프로파일
    SPRING_PROFILES_ACTIVE=prod
    ```

    모든 스크립트에 `chmod +x` 실행 권한 부여.
  </action>
  <verify>
    <automated>ls deploy/deploy.sh deploy/generate-cert.sh deploy/backup.sh deploy/restore.sh deploy/package.sh .env.example && grep "docker load" deploy/deploy.sh && grep "docker save" deploy/package.sh && grep "pg_dump" deploy/backup.sh && grep "keytool" deploy/generate-cert.sh && grep "DB_PASSWORD" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - deploy/ 디렉토리에 5개 쉘 스크립트 존재 (deploy, generate-cert, backup, restore, package)
    - deploy.sh에 `docker load`, `docker compose`, `actuator/health` 헬스체크 루프 존재
    - package.sh에 `docker save`, `tar czf navy-casualty-deploy` 존재
    - generate-cert.sh에 `keytool -genkeypair`, `PKCS12` 존재
    - backup.sh에 `pg_dump`, `find.*-mtime.*-delete` (롤링 삭제) 존재
    - restore.sh에 `read -p.*계속하시겠습니까` 확인 프롬프트, `psql` 존재
    - .env.example에 `DB_PASSWORD`, `PII_ENCRYPTION_KEY`, `SERVER_SSL_ENABLED`, `SSL_KEYSTORE_PASSWORD` 플레이스홀더 존재
    - .env.example에 실제 비밀번호가 포함되지 않음 (CHANGE_ME 패턴)
  </acceptance_criteria>
  <done>배포 스크립트 6종 + .env.example 생성 완료, 에어갭 환경 단일 명령 배포 가능</done>
</task>

</tasks>

<verification>
- deploy/ 디렉토리에 5개 스크립트 + deploy/archive-audit-log.sh (Plan 04에서 생성)
- .env.example에 시크릿 플레이스홀더만 포함
- deploy.sh가 docker load + compose up + healthcheck 포함
- package.sh가 tar 아카이브 생성
</verification>

<success_criteria>
`./deploy/package.sh 1.0.0`으로 오프라인 패키지를 생성하고, 대상 서버에서 `tar xzf + ./deploy/deploy.sh`로 단일 명령 배포가 가능하다.
</success_criteria>

<output>
After completion, create `.planning/phases/08-verification-deploy/08-07-SUMMARY.md`
</output>
