---
phase: 08-verification-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/db/migration/V13__add_ssn_hash_column.sql
  - backend/src/main/resources/db/migration/V14__extend_code_tables.sql
  - backend/build.gradle.kts
  - frontend/package.json
  - frontend/.eslintrc.cjs
  - CHANGELOG.md
  - backend/src/main/resources/application.yml
autonomous: true
requirements:
  - INFRA-02
  - INFRA-04
must_haves:
  truths:
    - "Flyway validate가 V1~V17 전체 체인에서 오류 없이 통과한다"
    - "V13, V14 마이그레이션 파일이 git 추적 상태다"
    - "미사용 의존성이 제거되고 보안 취약점 버전이 업데이트되었다"
    - "SemVer v1.0.0이 application.yml에 선언되어 있다"
    - "CHANGELOG.md가 프로젝트 루트에 존재한다"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "버전별 변경사항 기록"
      contains: "v1.0.0"
    - path: "backend/src/main/resources/application.yml"
      provides: "버전 정보"
      contains: "app.version"
  key_links: []
---

<objective>
코드 품질 정리, Flyway 마이그레이션 체인 정합성 확인, 미사용 의존성 제거, 버전 관리 설정을 완료한다.

Purpose: 운영 배포 전 코드베이스를 깨끗한 상태로 정리하고, DB 마이그레이션 체인이 무결한지 확인한다.
Output: 정리된 코드베이스 + CHANGELOG.md + SemVer v1.0.0 선언
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/build.gradle.kts
@frontend/package.json
@backend/src/main/resources/application.yml
@backend/src/main/resources/db/migration/V13__add_ssn_hash_column.sql
@backend/src/main/resources/db/migration/V14__extend_code_tables.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway 정리 + 미사용 코드/의존성 제거 + 린트 강화</name>
  <files>
    backend/src/main/resources/db/migration/V13__add_ssn_hash_column.sql,
    backend/src/main/resources/db/migration/V14__extend_code_tables.sql,
    backend/build.gradle.kts,
    frontend/package.json,
    frontend/.eslintrc.cjs
  </files>
  <read_first>
    backend/src/main/resources/db/migration/V13__add_ssn_hash_column.sql,
    backend/src/main/resources/db/migration/V14__extend_code_tables.sql,
    backend/build.gradle.kts,
    frontend/package.json,
    frontend/.eslintrc.cjs,
    backend/src/main/resources/db/migration/V6__add_ssn_hash_column.sql
  </read_first>
  <action>
    per D-22: V13__add_ssn_hash_column.sql, V14__extend_code_tables.sql이 untracked 상태이므로 내용을 확인하고 git add하여 추적 상태로 만든다.
    또한 V6__add_ssn_hash_column.sql이 git에서 deleted 상태인데 (V13으로 교체된 것), 이 삭제를 git add로 스테이징한다.

    per D-21: `flyway validate` 실행하여 V1~V17 전체 마이그레이션 체인 정합성 확인.
    (테스트 프로파일에서 Flyway 비활성화 상태이므로, dev 프로파일로 validate 실행)

    per D-35: 백엔드/프론트엔드 소스 전체에서 미사용 import, TODO, FIXME를 grep으로 스캔.
    - TODO/FIXME: 각각 확인 후 해결 가능한 것은 정리, 나머지는 주석에 이유 기록
    - 미사용 import: IDE 수준 정리

    per D-37: build.gradle.kts에서 미사용 의존성 확인 및 제거. package.json에서도 동일.
    - `npm audit`으로 프론트엔드 보안 취약점 확인, 가능한 버전 업데이트

    per D-36: ESLint 규칙 강화 — frontend/.eslintrc.cjs에 `no-unused-vars: error`, `no-console: warn` 추가.
    백엔드는 Checkstyle 플러그인 추가 불필요 (기존 코드량 대비 ROI 낮음 — 린트 정리로 대체).
  </action>
  <verify>
    <automated>cd backend && ./gradlew flywayValidate --info 2>&1 | tail -5; cd ../frontend && npx eslint src/ --max-warnings=0 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `git status`에서 V13__add_ssn_hash_column.sql, V14__extend_code_tables.sql이 tracked 상태
    - `git status`에서 V6__add_ssn_hash_column.sql 삭제가 스테이징됨
    - backend 소스에 해결 가능한 TODO/FIXME가 0개
    - `npx eslint src/`가 error 0으로 통과
  </acceptance_criteria>
  <done>Flyway V1~V17 체인 정합성 확인 완료, 미사용 코드/의존성 정리 완료, 린트 통과</done>
</task>

<task type="auto">
  <name>Task 2: SemVer 버전 관리 + CHANGELOG.md 생성</name>
  <files>
    backend/src/main/resources/application.yml,
    CHANGELOG.md
  </files>
  <read_first>
    backend/src/main/resources/application.yml
  </read_first>
  <action>
    per D-40: application.yml에 `app.version: 1.0.0` 속성 추가.
    프론트엔드 로그인 페이지 하단 또는 AppLayout 하단에 버전 표시를 위해,
    backend `/api/version` 엔드포인트 추가 (GET, 인증 불필요, `@Value("${app.version}")` 반환).
    또는 더 간단하게: frontend의 LoginPage.tsx 하단에 `v1.0.0` 텍스트 직접 표시 (빌드 시점 환경변수).

    per D-41: 프로젝트 루트에 CHANGELOG.md 생성. 내용:
    ```markdown
    # Changelog

    ## [1.0.0] - 2026-04-04

    ### Added
    - 사망자 관리 (등록/수정/삭제/조회/Excel)
    - 상이자 관리 (등록/수정/삭제/조회/Excel)
    - 전공사상심사 관리 (등록/수정/삭제/조회/이력/자동반영/Excel)
    - 공식 문서 7종 출력 (JasperReports PDF)
    - 통계 6종 (신분별/월별/연도별/부대별 현황 + 명부)
    - 역할 기반 접근 제어 (ADMIN/MANAGER/OPERATOR/VIEWER)
    - AES-256-GCM 주민번호 암호화
    - AOP 감사 로그 자동 기록
    - Docker Compose 에어갭 배포
    ```
  </action>
  <verify>
    <automated>grep "app.version" backend/src/main/resources/application.yml && grep "1.0.0" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - application.yml에 `app.version: 1.0.0` 또는 `app:\n  version: 1.0.0` 존재
    - CHANGELOG.md 파일이 프로젝트 루트에 존재하고 `## [1.0.0]` 섹션 포함
    - CHANGELOG.md에 주요 기능 7개 이상 나열
  </acceptance_criteria>
  <done>SemVer v1.0.0 선언 완료, CHANGELOG.md에 v1.0.0 변경사항 기록 완료</done>
</task>

</tasks>

<verification>
- `flyway validate` 성공 (V1~V17 체인 무결)
- V13, V14 파일이 git tracked
- ESLint error 0
- CHANGELOG.md 존재
- application.yml에 app.version: 1.0.0
</verification>

<success_criteria>
코드베이스가 운영 배포 가능한 깨끗한 상태이고, 버전 관리가 설정되어 있다.
</success_criteria>

<output>
After completion, create `.planning/phases/08-verification-deploy/08-01-SUMMARY.md`
</output>
