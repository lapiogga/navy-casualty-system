---
phase: 08-verification-deploy
plan: 05
type: execute
wave: 2
depends_on: ["08-01", "08-03"]
files_modified:
  - frontend/vite.config.ts
  - frontend/src/routes/index.tsx
  - backend/src/main/java/com/navy/casualty/common/config/WebConfig.java
autonomous: true
requirements:
  - INFRA-05
  - STAT-07
must_haves:
  truths:
    - "프론트엔드 번들이 페이지별로 code splitting되어 있다"
    - "vite-plugin-visualizer로 번들 분석 HTML이 생성된다"
    - "정적 자산에 Cache-Control 헤더가 설정된다"
  artifacts:
    - path: "frontend/vite.config.ts"
      provides: "번들 분석 플러그인 + 해시 파일명"
      contains: "visualizer"
    - path: "frontend/src/routes/index.tsx"
      provides: "lazy route 코드 분할"
      contains: "lazy("
  key_links:
    - from: "vite.config.ts"
      to: "build output"
      via: "rollup-plugin-visualizer plugin"
      pattern: "visualizer"
    - from: "routes/index.tsx"
      to: "page components"
      via: "React.lazy dynamic import"
      pattern: "lazy\\("
---

<objective>
프론트엔드 빌드 최적화 — lazy route code splitting + 번들 분석 + 정적 자산 캐시 설정.

Purpose: 페이지별 코드 분할로 초기 로딩 속도를 개선하고, 번들 크기를 시각적으로 분석한다.
Output: lazy route 적용 routes/index.tsx, visualizer 플러그인, Cache-Control 설정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/vite.config.ts
@frontend/src/routes/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lazy route code splitting + visualizer 플러그인</name>
  <files>
    frontend/vite.config.ts,
    frontend/src/routes/index.tsx,
    frontend/package.json
  </files>
  <read_first>
    frontend/vite.config.ts,
    frontend/src/routes/index.tsx,
    frontend/package.json
  </read_first>
  <action>
    per D-23: `npm install -D rollup-plugin-visualizer` 실행.

    vite.config.ts에 visualizer 플러그인 추가:
    ```typescript
    import { visualizer } from 'rollup-plugin-visualizer';

    // plugins 배열에 추가 (build 시에만)
    plugins: [
      react(),
      ...(process.env.ANALYZE === 'true' ? [visualizer({
        open: true,
        filename: 'dist/stats.html',
        gzipSize: true,
      })] : []),
    ],
    ```
    `ANALYZE=true npm run build`로 번들 분석 실행 가능.

    per D-23: routes/index.tsx를 lazy route로 변환.
    기존 static import를 React.lazy + dynamic import로 교체:
    ```typescript
    import { lazy, Suspense } from 'react';
    import { createBrowserRouter, Navigate } from 'react-router-dom';
    import AppLayout from '../components/layout/AppLayout';
    import ErrorPage from '../pages/ErrorPage';
    import NotFoundPage from '../pages/NotFoundPage';
    import LoginPage from '../pages/LoginPage';
    import { AuthGuard, AdminGuard } from '../components/auth/AuthGuard';

    // lazy imports (페이지 컴포넌트만)
    const DeadListPage = lazy(() => import('../pages/dead/DeadListPage'));
    const WoundedListPage = lazy(() => import('../pages/wounded/WoundedListPage'));
    const ReviewListPage = lazy(() => import('../pages/review/ReviewListPage'));
    const DocumentIssueHistoryPage = lazy(() => import('../pages/document/DocumentIssueHistoryPage'));
    const BranchStatPage = lazy(() => import('../pages/statistics/BranchStatPage'));
    const MonthlyStatPage = lazy(() => import('../pages/statistics/MonthlyStatPage'));
    const YearlyStatPage = lazy(() => import('../pages/statistics/YearlyStatPage'));
    const UnitStatPage = lazy(() => import('../pages/statistics/UnitStatPage'));
    const UnitRosterPage = lazy(() => import('../pages/statistics/UnitRosterPage'));
    const AllRosterPage = lazy(() => import('../pages/statistics/AllRosterPage'));
    const AdminPage = lazy(() => import('../pages/admin/AdminPage'));
    const ChangePasswordPage = lazy(() => import('../pages/ChangePasswordPage'));
    ```

    AppLayout에 `<Suspense fallback={<Spin spinning />}>` 래퍼 추가.
    (또는 각 route의 element를 Suspense로 감싸기)
    AppLayout.tsx의 children outlet 영역에 Suspense 추가:
    ```tsx
    import { Suspense } from 'react';
    import { Spin } from 'antd';
    // Outlet 래핑
    <Suspense fallback={<div style={{display:'flex',justifyContent:'center',padding:'100px'}}><Spin size="large" /></div>}>
      <Outlet />
    </Suspense>
    ```

    per D-24: Vite는 기본적으로 `[name].[hash].js` 파일명을 생성하므로 별도 설정 불필요.
    Spring Boot 정적 자산 캐시 — WebConfig.java 또는 application.yml에:
    ```yaml
    spring:
      web:
        resources:
          cache:
            cachecontrol:
              max-age: 31536000
    ```
    또는 WebConfig.java에 WebMvcConfigurer 구현으로 `/assets/**` 패턴에 Cache-Control 설정.
    application.yml에 추가하는 것이 가장 간단.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5 && grep -n "lazy(" src/routes/index.tsx | head -5 && grep -n "visualizer" vite.config.ts && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json의 devDependencies에 `rollup-plugin-visualizer` 존재
    - vite.config.ts에 `visualizer` import + plugins 배열 내 visualizer 설정 존재
    - routes/index.tsx에 `lazy(() => import(` 패턴이 10개 이상 존재
    - routes/index.tsx에서 LoginPage, ErrorPage, NotFoundPage, AuthGuard는 static import 유지
    - AppLayout.tsx 또는 routes 내에 `Suspense` + `fallback` 존재
    - `npm run build` 성공
    - application.yml 또는 application-prod.yml에 `max-age: 31536000` 존재
  </acceptance_criteria>
  <done>페이지별 lazy route code splitting 적용, 번들 분석 플러그인 설정, 정적 자산 캐시 헤더 설정 완료</done>
</task>

</tasks>

<verification>
- `npm run build` 성공, dist/assets/ 내 청크 파일 다수 생성
- `npx tsc --noEmit` 성공
- routes/index.tsx에 lazy import 10개 이상
- visualizer 플러그인 설정 존재
</verification>

<success_criteria>
프론트엔드가 페이지별로 code splitting되어 초기 로딩 시 메인 번들만 다운로드하고, 번들 분석이 가능하며, 정적 자산에 장기 캐시가 설정된다.
</success_criteria>

<output>
After completion, create `.planning/phases/08-verification-deploy/08-05-SUMMARY.md`
</output>
