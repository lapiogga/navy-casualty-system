---
phase: 08-verification-deploy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/navy/casualty/common/config/SecurityConfig.java
  - backend/src/main/java/com/navy/casualty/common/config/TlsConfig.java
  - backend/src/main/resources/application-prod.yml
  - docker-compose.prod.yml
  - backend/src/main/java/com/navy/casualty/common/init/StartupValidator.java
autonomous: true
requirements:
  - INFRA-01
  - INFRA-03
  - INFRA-05
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - "보안 헤더(HSTS, X-Content-Type-Options, X-Frame-Options)가 응답에 포함된다"
    - "application-prod.yml에 HikariCP, 로그, 세션 설정이 명시적으로 선언되어 있다"
    - "Docker healthcheck가 app과 db 모두에 설정되어 있다"
    - "ApplicationRunner가 부팅 시 환경변수와 코드 테이블을 검증한다"
    - "Actuator /health만 외부에 노출된다"
  artifacts:
    - path: "backend/src/main/java/com/navy/casualty/common/config/TlsConfig.java"
      provides: "HTTP->HTTPS 리다이렉트 + TLS 설정"
      contains: "TomcatServletWebServerFactory"
    - path: "backend/src/main/java/com/navy/casualty/common/init/StartupValidator.java"
      provides: "부팅 검증"
      contains: "ApplicationRunner"
    - path: "backend/src/main/resources/application-prod.yml"
      provides: "운영 프로파일 설정"
      contains: "maximumPoolSize"
  key_links:
    - from: "SecurityConfig.java"
      to: "HTTP responses"
      via: "Spring Security headers configuration"
      pattern: "httpStrictTransportSecurity"
    - from: "docker-compose.prod.yml"
      to: "container runtime"
      via: "healthcheck + restart policy"
      pattern: "healthcheck"
---

<objective>
보안 강화(TLS, 보안 헤더), 운영 프로파일 설정(HikariCP, 로그, 세션), Docker healthcheck, 부팅 검증을 완성한다.

Purpose: 군 내부망 운영 환경에서 보안과 안정성을 보장하는 인프라 설정을 완료한다.
Output: SecurityConfig 보안 헤더, TlsConfig, application-prod.yml 완성, StartupValidator, Docker healthcheck
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/main/java/com/navy/casualty/common/config/SecurityConfig.java
@backend/src/main/resources/application-prod.yml
@backend/src/main/resources/application.yml
@docker-compose.prod.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 보안 헤더 + TLS 설정 + Actuator 제한 + 운영 프로파일</name>
  <files>
    backend/src/main/java/com/navy/casualty/common/config/SecurityConfig.java,
    backend/src/main/java/com/navy/casualty/common/config/TlsConfig.java,
    backend/src/main/resources/application-prod.yml,
    backend/src/main/resources/application.yml
  </files>
  <read_first>
    backend/src/main/java/com/navy/casualty/common/config/SecurityConfig.java,
    backend/src/main/resources/application-prod.yml,
    backend/src/main/resources/application.yml
  </read_first>
  <action>
    per D-10: SecurityConfig.java의 filterChain 메서드에 보안 헤더 설정 추가:
    ```java
    http.headers(headers -> headers
        .httpStrictTransportSecurity(hsts -> hsts
            .includeSubDomains(true)
            .maxAgeInSeconds(31536000))
        .contentTypeOptions(Customizer.withDefaults())
        .frameOptions(frame -> frame.deny())
    );
    ```
    주의: 기존 filterChain 설정(CSRF, 세션, 인증 등)을 유지하면서 headers 체인만 추가.

    per D-09: TlsConfig.java 생성 — `@Profile("prod")` 조건부.
    `@ConditionalOnProperty(name = "server.ssl.enabled", havingValue = "true")` 로
    TLS 활성화 시에만 HTTP->HTTPS 리다이렉트 Connector 추가:
    ```java
    @Configuration
    @Profile("prod")
    @ConditionalOnProperty(name = "server.ssl.enabled", havingValue = "true")
    public class TlsConfig {
        @Bean
        public ServletWebServerFactory servletContainer() {
            TomcatServletWebServerFactory tomcat = new TomcatServletWebServerFactory() {
                @Override
                protected void postProcessContext(Context context) {
                    var constraint = new SecurityConstraint();
                    constraint.setUserConstraint("CONFIDENTIAL");
                    var collection = new SecurityCollection();
                    collection.addPattern("/*");
                    constraint.addCollection(collection);
                    context.addConstraint(constraint);
                }
            };
            tomcat.addAdditionalTomcatConnectors(httpConnector());
            return tomcat;
        }
        private Connector httpConnector() {
            var connector = new Connector(TomcatServletWebServerFactory.DEFAULT_PROTOCOL);
            connector.setScheme("http");
            connector.setPort(8080);
            connector.setSecure(false);
            connector.setRedirectPort(8443);
            return connector;
        }
    }
    ```

    per D-13: application.yml에 Actuator 설정 추가:
    ```yaml
    management:
      endpoints:
        web:
          exposure:
            include: health
      endpoint:
        health:
          show-details: never
    ```

    per D-15/D-16/D-17/D-19: application-prod.yml 완전 재작성:
    ```yaml
    spring:
      jpa:
        show-sql: false
      datasource:
        hikari:
          maximum-pool-size: 10
          connection-timeout: 30000
          idle-timeout: 600000
          keepalive-time: 300000
      session:
        jdbc:
          initialize-schema: never
        timeout: 30m
    server:
      servlet:
        session:
          timeout: 30m
          cookie:
            secure: true
            http-only: true
      ssl:
        enabled: false
        key-store: /app/certs/keystore.p12
        key-store-password: ${SSL_KEYSTORE_PASSWORD:}
        key-store-type: PKCS12
      port: 8443
    logging:
      level:
        root: WARN
        com.navy.casualty: INFO
        org.springframework.security: WARN
        org.hibernate.SQL: WARN
        org.hibernate.type.descriptor.sql: WARN
      file:
        name: /app/logs/navy-casualty.log
      logback:
        rollingpolicy:
          max-file-size: 50MB
          max-history: 30
          total-size-cap: 1GB
    management:
      endpoints:
        web:
          exposure:
            include: health
      endpoint:
        health:
          show-details: never
    ```

    per D-08: TLS는 `server.ssl.enabled=true` + SSL_KEYSTORE_PASSWORD 환경변수로 활성화.
    기본값 `false`이므로 개발 환경에서는 HTTP만 사용.
  </action>
  <verify>
    <automated>cd backend && grep -n "httpStrictTransportSecurity" src/main/java/com/navy/casualty/common/config/SecurityConfig.java && grep -n "maximum-pool-size: 10" src/main/resources/application-prod.yml && grep -n "keepalive-time: 300000" src/main/resources/application-prod.yml</automated>
  </verify>
  <acceptance_criteria>
    - SecurityConfig.java에 `httpStrictTransportSecurity`, `contentTypeOptions`, `frameOptions` 설정 존재
    - TlsConfig.java에 `@Profile("prod")`, `TomcatServletWebServerFactory`, `httpConnector` 존재
    - application-prod.yml에 `maximum-pool-size: 10`, `connection-timeout: 30000`, `idle-timeout: 600000`, `keepalive-time: 300000` 존재
    - application-prod.yml에 `timeout: 30m` (세션), `secure: true` (쿠키) 존재
    - application-prod.yml에 `root: WARN`, `com.navy.casualty: INFO` 로그 레벨 존재
    - application.yml 또는 application-prod.yml에 `management.endpoints.web.exposure.include: health` 존재
  </acceptance_criteria>
  <done>보안 헤더, TLS 리다이렉트, Actuator 제한, HikariCP/로그/세션 운영 설정 완료</done>
</task>

<task type="auto">
  <name>Task 2: Docker healthcheck + restart 정책 + StartupValidator</name>
  <files>
    docker-compose.prod.yml,
    backend/src/main/java/com/navy/casualty/common/init/StartupValidator.java
  </files>
  <read_first>
    docker-compose.prod.yml,
    docker-compose.yml,
    backend/src/main/java/com/navy/casualty/common/init/StartupValidator.java
  </read_first>
  <action>
    per D-14/D-18: docker-compose.prod.yml에 healthcheck와 restart 정책 추가:
    ```yaml
    services:
      app:
        restart: unless-stopped
        environment:
          SPRING_PROFILES_ACTIVE: prod
        volumes:
          - ./certs:/app/certs:ro
          - ./logs:/app/logs
        healthcheck:
          test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
          interval: 30s
          timeout: 10s
          retries: 3
          start_period: 60s
        # TLS 활성화 시 아래 주석 해제
        # environment:
        #   SERVER_SSL_ENABLED: "true"
        #   SSL_KEYSTORE_PASSWORD: ${SSL_KEYSTORE_PASSWORD}

      db:
        restart: unless-stopped
        healthcheck:
          test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-casualty}"]
          interval: 10s
          timeout: 5s
          retries: 5
    ```
    주의: 기존 `restart: always`를 `restart: unless-stopped`로 변경 (D-18).
    app healthcheck에서 TLS 비활성화 시 http://localhost:8080, TLS 활성화 시에는 --insecure 옵션 필요 — 기본은 HTTP 포트로 체크.

    per D-28: StartupValidator.java 생성 (backend/src/main/java/com/navy/casualty/common/init/):
    ```java
    @Component
    @RequiredArgsConstructor
    @Slf4j
    public class StartupValidator implements ApplicationRunner {
        private final JdbcTemplate jdbcTemplate;

        @Override
        public void run(ApplicationArguments args) {
            // 환경변수 검증
            checkEnv("DB_PASSWORD", "데이터베이스 비밀번호");
            checkEnv("PII_ENCRYPTION_KEY", "주민번호 암호화 키");

            // 코드 테이블 건수 검증
            try {
                Long rankCount = jdbcTemplate.queryForObject(
                    "SELECT COUNT(*) FROM tb_rank_code", Long.class);
                Long branchCount = jdbcTemplate.queryForObject(
                    "SELECT COUNT(*) FROM tb_branch_code", Long.class);
                log.info("부팅 검증 - 계급 코드: {}건, 군구분 코드: {}건", rankCount, branchCount);
                if (rankCount == 0 || branchCount == 0) {
                    log.warn("코드 테이블이 비어 있습니다. 초기 데이터 적재가 필요합니다.");
                }
            } catch (Exception e) {
                log.warn("코드 테이블 조회 실패: {}", e.getMessage());
            }

            // admin 계정 확인
            try {
                Long adminCount = jdbcTemplate.queryForObject(
                    "SELECT COUNT(*) FROM tb_user WHERE username = 'admin'", Long.class);
                if (adminCount == 0) {
                    log.warn("admin 계정이 없습니다.");
                }
            } catch (Exception e) {
                log.warn("admin 계정 조회 실패: {}", e.getMessage());
            }

            log.info("부팅 검증 완료");
        }

        private void checkEnv(String name, String description) {
            if (System.getenv(name) == null && System.getProperty(name) == null) {
                log.warn("환경변수 {} ({}) 가 설정되지 않았습니다", name, description);
            }
        }
    }
    ```
    실패 시 로그 경고만 출력하고 정상 기동한다 (D-28 명시).
  </action>
  <verify>
    <automated>grep -n "healthcheck" docker-compose.prod.yml && grep -n "unless-stopped" docker-compose.prod.yml && grep -n "ApplicationRunner" backend/src/main/java/com/navy/casualty/common/init/StartupValidator.java</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.prod.yml의 app 서비스에 `healthcheck` 블록 존재 (test, interval, timeout, retries, start_period)
    - docker-compose.prod.yml의 db 서비스에 `healthcheck` 블록 존재 (`pg_isready`)
    - docker-compose.prod.yml의 app, db 모두 `restart: unless-stopped`
    - StartupValidator.java에 `implements ApplicationRunner`, `checkEnv("DB_PASSWORD"`, `checkEnv("PII_ENCRYPTION_KEY"` 존재
    - StartupValidator.java에 `tb_rank_code`, `tb_branch_code`, `tb_user` 조회 존재
    - `cd backend && ./gradlew compileJava` 성공
  </acceptance_criteria>
  <done>Docker healthcheck + restart 정책 설정 완료, ApplicationRunner 부팅 검증 구현 완료</done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew compileJava` 성공
- SecurityConfig.java에 보안 헤더 3종 설정
- application-prod.yml에 HikariCP, 로그, 세션, Actuator 설정
- docker-compose.prod.yml에 healthcheck + restart 정책
- StartupValidator가 부팅 시 환경변수, 코드 테이블, admin 계정 검증
</verification>

<success_criteria>
운영 환경에서 보안 헤더가 응답에 포함되고, HikariCP/로그/세션이 명시적으로 설정되며, Docker 컨테이너가 헬스체크와 자동 재시작을 지원하고, 부팅 시 필수 설정이 검증된다.
</success_criteria>

<output>
After completion, create `.planning/phases/08-verification-deploy/08-02-SUMMARY.md`
</output>
