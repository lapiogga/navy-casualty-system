---
phase: 08-verification-deploy
plan: 06
type: execute
wave: 3
depends_on: ["08-02", "08-03", "08-04", "08-05"]
files_modified:
  - backend/src/test/java/com/navy/casualty/integration/AuthFlowIntegrationTest.java
  - backend/src/test/java/com/navy/casualty/integration/DeadCrudIntegrationTest.java
  - backend/src/test/java/com/navy/casualty/integration/WoundedCrudIntegrationTest.java
  - backend/src/test/java/com/navy/casualty/integration/ReviewFlowIntegrationTest.java
  - backend/src/test/java/com/navy/casualty/integration/DocumentOutputIntegrationTest.java
  - backend/src/test/java/com/navy/casualty/integration/StatisticsIntegrationTest.java
  - backend/src/test/java/com/navy/casualty/integration/ExcelExportIntegrationTest.java
  - test/e2e/playwright.config.ts
  - test/e2e/specs/login.spec.ts
  - test/e2e/specs/dead-crud.spec.ts
  - test/e2e/specs/document-output.spec.ts
  - test/performance/load-test.jmx
  - test/security/owasp-zap-scan.sh
  - test/security/owasp-checklist.md
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - AUDIT-04
  - AUDIT-05
  - AUDIT-06
  - DEAD-01
  - DEAD-02
  - DEAD-03
  - DEAD-04
  - DEAD-05
  - DEAD-06
  - WOND-01
  - WOND-02
  - WOND-03
  - WOND-04
  - WOND-05
  - WOND-06
  - REVW-01
  - REVW-02
  - REVW-03
  - REVW-04
  - REVW-05
  - REVW-06
  - REVW-07
  - REVW-08
  - DOCU-01
  - DOCU-02
  - DOCU-03
  - DOCU-04
  - DOCU-05
  - DOCU-06
  - DOCU-07
  - DOCU-08
  - DOCU-09
  - STAT-01
  - STAT-02
  - STAT-03
  - STAT-04
  - STAT-05
  - STAT-06
  - STAT-07
must_haves:
  truths:
    - "API 통합 테스트 7종이 모두 GREEN으로 통과한다"
    - "Playwright E2E 테스트 3종이 브라우저에서 통과한다"
    - "OWASP ZAP 자동 스캔이 HIGH 취약점 0건이다"
    - "JMeter 성능 테스트에서 통계 API 응답시간이 5초 이내다"
    - "OWASP Top 10 수동 체크리스트가 완성되었다"
  artifacts:
    - path: "backend/src/test/java/com/navy/casualty/integration/AuthFlowIntegrationTest.java"
      provides: "인증 흐름 통합 테스트"
      contains: "@SpringBootTest"
    - path: "test/e2e/playwright.config.ts"
      provides: "Playwright 설정"
      contains: "baseURL"
    - path: "test/performance/load-test.jmx"
      provides: "JMeter 부하 테스트 스크립트"
      contains: "ThreadGroup"
    - path: "test/security/owasp-checklist.md"
      provides: "OWASP Top 10 수동 체크리스트"
      contains: "A01:2021"
  key_links:
    - from: "AuthFlowIntegrationTest"
      to: "/api/auth/login"
      via: "TestRestTemplate POST"
      pattern: "postForEntity.*login"
    - from: "playwright.config.ts"
      to: "http://localhost:8080"
      via: "baseURL"
      pattern: "baseURL"
---

<objective>
전체 기능 통합 검증 — API 통합 테스트 7종, Playwright E2E 3종, OWASP ZAP 보안 스캔, JMeter 성능 테스트, 보안 체크리스트를 완성한다.

Purpose: Phase 1~7 전체 기능의 정상 동작, 보안, 성능을 검증하여 운영 배포 가능 상태를 확인한다.
Output: 통합 테스트 스위트, E2E 테스트, 보안 스캔 스크립트, 성능 테스트 스크립트, 보안 체크리스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@backend/src/test/java/com/navy/casualty/
@backend/src/main/java/com/navy/casualty/security/controller/AuthController.java
@backend/src/main/java/com/navy/casualty/dead/controller/DeadController.java
@backend/src/main/java/com/navy/casualty/common/config/SecurityConfig.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: API 통합 테스트 7종 (@SpringBootTest + TestRestTemplate)</name>
  <files>
    backend/src/test/java/com/navy/casualty/integration/AuthFlowIntegrationTest.java,
    backend/src/test/java/com/navy/casualty/integration/DeadCrudIntegrationTest.java,
    backend/src/test/java/com/navy/casualty/integration/WoundedCrudIntegrationTest.java,
    backend/src/test/java/com/navy/casualty/integration/ReviewFlowIntegrationTest.java,
    backend/src/test/java/com/navy/casualty/integration/DocumentOutputIntegrationTest.java,
    backend/src/test/java/com/navy/casualty/integration/StatisticsIntegrationTest.java,
    backend/src/test/java/com/navy/casualty/integration/ExcelExportIntegrationTest.java
  </files>
  <read_first>
    backend/src/main/java/com/navy/casualty/security/controller/AuthController.java,
    backend/src/main/java/com/navy/casualty/dead/controller/DeadController.java,
    backend/src/main/java/com/navy/casualty/wounded/controller/WoundedController.java,
    backend/src/main/java/com/navy/casualty/review/controller/ReviewController.java,
    backend/src/main/java/com/navy/casualty/document/controller/DocumentController.java,
    backend/src/main/java/com/navy/casualty/statistics/controller/StatisticsController.java,
    backend/src/test/java/com/navy/casualty/dead/service/DeadServiceTest.java
  </read_first>
  <action>
    per D-01/D-02: @SpringBootTest(webEnvironment = RANDOM_PORT) + TestRestTemplate로 7종 통합 테스트 작성.
    각 테스트는 @ActiveProfiles("test") 사용. H2 인메모리 DB.

    공통 헬퍼: 로그인 후 세션 쿠키를 TestRestTemplate에 설정하는 유틸 메서드.
    ```java
    private HttpHeaders loginAndGetHeaders(String username, String password) {
        var loginDto = Map.of("username", username, "password", password);
        var resp = restTemplate.postForEntity("/api/auth/login", loginDto, String.class);
        var cookies = resp.getHeaders().get(HttpHeaders.SET_COOKIE);
        var headers = new HttpHeaders();
        if (cookies != null) headers.put(HttpHeaders.COOKIE, cookies);
        return headers;
    }
    ```

    **1. AuthFlowIntegrationTest:**
    - 로그인 성공 (200 + 세션 쿠키)
    - 로그인 실패 (401)
    - 5회 실패 후 계정 잠금 (AUTH-02)
    - 인증 없이 보호된 API 접근 시 401 (AUTH-06)
    - 로그아웃 후 세션 무효화 (AUTH-07)
    - /api/auth/me로 현재 사용자 정보 확인 (AUTH-01)

    **2. DeadCrudIntegrationTest:**
    - 로그인 후 사망자 등록 (POST /api/dead, 201)
    - 사망자 목록 조회 (GET /api/dead, 200, 페이징)
    - 사망자 수정 (PUT /api/dead/{id})
    - 사망자 논리 삭제 (DELETE /api/dead/{id}, 삭제 사유 필수)
    - 중복 등록 방지 (같은 군번 409) (DEAD-07)

    **3. WoundedCrudIntegrationTest:** Dead와 동일 패턴, /api/wounded

    **4. ReviewFlowIntegrationTest:**
    - 심사 등록 + 이력 자동 생성 (REVW-02/06)
    - 심사 수정 + 이력 스냅샷 (REVW-03/06/AUDIT-07)
    - 보훈청 통보 일시 기록 (REVW-08)
    - 심사 결과 -> 사망자/상이자 분류 자동 반영 검증 (REVW-07)

    **5. DocumentOutputIntegrationTest:**
    - 문서 출력 API 호출 (GET /api/documents/{type}/{id})
    - PDF 바이트 배열 반환 검증 (content-type: application/pdf)
    - 발급 이력 자동 기록 검증 (DOCU-08)

    **6. StatisticsIntegrationTest:**
    - 6종 통계 API 호출 (GET /api/statistics/*)
    - 응답 데이터 구조 검증
    - 응답 시간 5초 이내 assertion (STAT-07):
      ```java
      long start = System.currentTimeMillis();
      var resp = restTemplate.exchange(...);
      long elapsed = System.currentTimeMillis() - start;
      assertThat(elapsed).isLessThan(5000);
      ```

    **7. ExcelExportIntegrationTest:**
    - 사망자 Excel 다운로드 (GET /api/dead/excel)
    - content-type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet 검증
    - 파일 크기 > 0 검증
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "com.navy.casualty.integration.*" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - integration 패키지에 7개 테스트 클래스 존재
    - 각 클래스에 `@SpringBootTest(webEnvironment = WebEnvironment.RANDOM_PORT)` 어노테이션
    - AuthFlowIntegrationTest에 로그인 성공/실패/잠금/로그아웃 테스트 메서드 존재
    - DeadCrudIntegrationTest에 등록/조회/수정/삭제/중복방지 메서드 존재
    - StatisticsIntegrationTest에 `assertThat(elapsed).isLessThan(5000)` 패턴 존재
    - `./gradlew test --tests "com.navy.casualty.integration.*"` 전체 GREEN
  </acceptance_criteria>
  <done>API 통합 테스트 7종 작성 완료, 전체 GREEN 통과</done>
</task>

<task type="auto">
  <name>Task 2: Playwright E2E + OWASP ZAP 스캔 + JMeter 성능 + 보안 체크리스트</name>
  <files>
    test/e2e/playwright.config.ts,
    test/e2e/specs/login.spec.ts,
    test/e2e/specs/dead-crud.spec.ts,
    test/e2e/specs/document-output.spec.ts,
    test/performance/load-test.jmx,
    test/security/owasp-zap-scan.sh,
    test/security/owasp-checklist.md
  </files>
  <read_first>
    frontend/src/pages/LoginPage.tsx,
    frontend/src/pages/dead/DeadListPage.tsx,
    frontend/src/pages/document/DocumentIssueHistoryPage.tsx,
    frontend/package.json
  </read_first>
  <action>
    `npm install -D @playwright/test` (프로젝트 루트 또는 frontend/).
    `npx playwright install chromium`.
    `mkdir -p test/e2e/specs test/performance test/security`.

    per D-01/D-02: Playwright E2E 3종 작성.

    **playwright.config.ts:**
    ```typescript
    import { defineConfig } from '@playwright/test';
    export default defineConfig({
      testDir: './specs',
      baseURL: process.env.BASE_URL || 'http://localhost:8080',
      timeout: 30000,
      retries: 1,
      use: {
        headless: true,
        screenshot: 'only-on-failure',
      },
    });
    ```

    **login.spec.ts:**
    - 로그인 페이지 접근
    - 유효한 자격증명으로 로그인 성공 → /dead 리다이렉트
    - 잘못된 비밀번호 → 에러 메시지 표시
    - 로그아웃 후 로그인 페이지 리다이렉트

    **dead-crud.spec.ts:**
    - 로그인 → 사망자 목록 페이지
    - 등록 버튼 클릭 → Modal 열림 → 필수 필드 입력 → 저장
    - 목록에서 검색 → 결과 확인
    - 수정 → Modal → 변경 → 저장
    (주의: data-testid 속성이 없을 수 있으므로 Ant Design 셀렉터 패턴 사용: `.ant-btn`, `.ant-table`, `.ant-modal`)

    **document-output.spec.ts:**
    - 사망자 목록 → 문서 출력 버튼 클릭
    - 발급 목적 Modal → 목적 입력 → 확인
    - PDF 미리보기 iframe 표시 확인

    per D-03: OWASP ZAP 자동 스캔 스크립트:
    ```bash
    #!/bin/bash
    # test/security/owasp-zap-scan.sh
    # 전제: Docker 설치, 앱이 http://localhost:8080에서 실행 중
    TARGET_URL="${1:-http://host.docker.internal:8080}"
    docker run --rm -t zaproxy/zap-stable zap-baseline.py \
      -t "$TARGET_URL" \
      -r /dev/stdout \
      -l WARN \
      -I  # non-fatal return code
    echo "ZAP 스캔 완료. HIGH 취약점이 없어야 합니다."
    ```

    per D-03: OWASP Top 10 수동 체크리스트 (test/security/owasp-checklist.md):
    ```markdown
    # OWASP Top 10 (2021) 보안 체크리스트

    | # | Category | Check | Status | Notes |
    |---|----------|-------|--------|-------|
    | A01:2021 | Broken Access Control | RBAC @PreAuthorize 전 엔드포인트 적용 확인 | [ ] | |
    | A01:2021 | Broken Access Control | VIEWER가 삭제/수정 불가 확인 | [ ] | |
    | A02:2021 | Cryptographic Failures | 주민번호 AES-256-GCM 암호화 확인 | [ ] | |
    | A02:2021 | Cryptographic Failures | HTTPS/TLS 적용 확인 | [ ] | |
    | A03:2021 | Injection | SQL Injection: 파라미터화 쿼리 사용 확인 | [ ] | |
    | A03:2021 | Injection | XSS: React 기본 이스케이핑 + dangerouslySetInnerHTML 미사용 확인 | [ ] | |
    | A04:2021 | Insecure Design | 계정 잠금 (5회 실패) 확인 | [ ] | |
    | A05:2021 | Security Misconfiguration | Spring Actuator /health만 노출 확인 | [ ] | |
    | A05:2021 | Security Misconfiguration | 보안 헤더 (HSTS, X-Content-Type-Options) 확인 | [ ] | |
    | A06:2021 | Vulnerable Components | npm audit + Gradle 의존성 점검 | [ ] | |
    | A07:2021 | Auth Failures | 세션 타임아웃 30분 확인 | [ ] | |
    | A07:2021 | Auth Failures | 동시 세션 1개 제한 확인 | [ ] | |
    | A08:2021 | Software/Data Integrity | Flyway 마이그레이션 체인 검증 | [ ] | |
    | A09:2021 | Logging/Monitoring | 감사 로그 기록 확인 (로그인, CRUD, 출력) | [ ] | |
    | A10:2021 | SSRF | 서버에서 외부 URL 호출 없음 (에어갭) | [x] | N/A |
    ```

    per D-04: JMeter JMX 스크립트 (test/performance/load-test.jmx):
    XML 형태의 JMX 파일 생성. 구성:
    - Thread Group: 10 threads, ramp-up 10초, loop 5
    - HTTP Cookie Manager (세션 유지)
    - 시나리오:
      1. POST /api/auth/login (세션 획득)
      2. GET /api/dead?page=0&size=20 (사망자 목록)
      3. GET /api/statistics/by-branch (통계 — STAT-07 5초 기준)
      4. GET /api/statistics/by-month
      5. GET /api/statistics/by-year
      6. GET /api/statistics/by-unit
    - Response Assertion: 모든 응답 200
    - Duration Assertion: 통계 API 5000ms 이내
    - Summary Report listener

    실행 방법 (Docker):
    ```bash
    docker run --rm -v $(pwd)/test/performance:/test justb4/jmeter:5.6.3 \
      -n -t /test/load-test.jmx -l /test/results.jtl -e -o /test/report
    ```
  </action>
  <verify>
    <automated>ls test/e2e/playwright.config.ts test/e2e/specs/login.spec.ts test/performance/load-test.jmx test/security/owasp-checklist.md test/security/owasp-zap-scan.sh 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - test/e2e/playwright.config.ts 존재, `baseURL` 설정 포함
    - test/e2e/specs/ 내 3개 spec 파일 존재 (login, dead-crud, document-output)
    - login.spec.ts에 `page.goto('/login')`, `fill`, `click` 패턴 존재
    - test/performance/load-test.jmx 존재, `ThreadGroup` + `HTTPSamplerProxy` 포함
    - load-test.jmx에 10 threads 설정, /api/statistics/ 엔드포인트 포함
    - test/security/owasp-zap-scan.sh 존재, `zaproxy/zap-stable` 포함
    - test/security/owasp-checklist.md 존재, `A01:2021` ~ `A10:2021` 항목 포함
  </acceptance_criteria>
  <done>Playwright E2E 3종, OWASP ZAP 스캔 스크립트, JMeter 성능 테스트, OWASP 체크리스트 완성</done>
</task>

</tasks>

<verification>
- API 통합 테스트 7종 GREEN
- Playwright 설정 + E2E spec 3종 존재
- JMeter JMX 스크립트 존재
- OWASP ZAP 스캔 스크립트 존재
- OWASP Top 10 체크리스트 존재
</verification>

<success_criteria>
전체 기능이 API 통합 테스트로 검증되고, 브라우저 E2E 시나리오가 자동화되며, 보안 스캔 스크립트와 성능 테스트 스크립트가 즉시 실행 가능한 상태다.
</success_criteria>

<output>
After completion, create `.planning/phases/08-verification-deploy/08-06-SUMMARY.md`
</output>
