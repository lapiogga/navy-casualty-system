---
phase: 08-verification-deploy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/db/migration/V18__replace_mock_with_real_code_data.sql
  - backend/src/main/resources/db/migration/V19__add_password_changed_column.sql
  - backend/src/main/java/com/navy/casualty/security/entity/User.java
  - backend/src/main/java/com/navy/casualty/security/service/AuthService.java
  - backend/src/main/java/com/navy/casualty/security/controller/AuthController.java
  - backend/src/main/java/com/navy/casualty/admin/controller/AdminController.java
  - backend/src/main/java/com/navy/casualty/admin/dto/DataCheckResponse.java
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/pages/ChangePasswordPage.tsx
  - frontend/src/hooks/useAuth.ts
  - frontend/src/routes/index.tsx
autonomous: true
requirements:
  - INFRA-02
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "V18 마이그레이션이 실 코드 데이터를 적재한다"
    - "첫 로그인 시 비밀번호 변경을 강제한다"
    - "/api/admin/data-check가 코드 테이블 건수와 admin 계정 존재 여부를 반환한다"
  artifacts:
    - path: "backend/src/main/resources/db/migration/V18__replace_mock_with_real_code_data.sql"
      provides: "실 코드 데이터 적재"
      contains: "INSERT INTO"
    - path: "backend/src/main/resources/db/migration/V19__add_password_changed_column.sql"
      provides: "비밀번호 변경 플래그 컬럼"
      contains: "password_changed"
    - path: "frontend/src/pages/ChangePasswordPage.tsx"
      provides: "비밀번호 변경 화면"
      contains: "ChangePasswordPage"
  key_links:
    - from: "AuthController.java"
      to: "User.passwordChanged"
      via: "로그인 응답에 passwordChanged 플래그 포함"
      pattern: "passwordChanged"
    - from: "frontend/src/routes/index.tsx"
      to: "ChangePasswordPage"
      via: "passwordChanged=false이면 리다이렉트"
      pattern: "change-password"
---

<objective>
초기 데이터 적재(V18), 비밀번호 변경 강제(V19 + UI), 데이터 검증 API를 구현한다.

Purpose: 운영 환경에서 실 코드 데이터가 적재되고, 초기 비밀번호 변경이 강제되며, 데이터 정합성을 확인할 수 있다.
Output: Flyway V18~V19, 비밀번호 변경 화면, /api/admin/data-check API
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/main/resources/db/migration/V9__insert_mock_code_data.sql
@backend/src/main/java/com/navy/casualty/security/entity/User.java
@backend/src/main/java/com/navy/casualty/security/service/AuthService.java
@backend/src/main/java/com/navy/casualty/security/controller/AuthController.java
@frontend/src/hooks/useAuth.ts
@frontend/src/routes/index.tsx
@frontend/src/pages/LoginPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway V18 실 코드 데이터 + V19 password_changed 컬럼 + 백엔드 API</name>
  <files>
    backend/src/main/resources/db/migration/V18__replace_mock_with_real_code_data.sql,
    backend/src/main/resources/db/migration/V19__add_password_changed_column.sql,
    backend/src/main/java/com/navy/casualty/security/entity/User.java,
    backend/src/main/java/com/navy/casualty/security/service/AuthService.java,
    backend/src/main/java/com/navy/casualty/security/controller/AuthController.java,
    backend/src/main/java/com/navy/casualty/admin/controller/AdminController.java,
    backend/src/main/java/com/navy/casualty/admin/dto/DataCheckResponse.java
  </files>
  <read_first>
    backend/src/main/resources/db/migration/V9__insert_mock_code_data.sql,
    backend/src/main/java/com/navy/casualty/security/entity/User.java,
    backend/src/main/java/com/navy/casualty/security/service/AuthService.java,
    backend/src/main/java/com/navy/casualty/security/controller/AuthController.java,
    backend/src/main/java/com/navy/casualty/admin/controller/AdminController.java
  </read_first>
  <action>
    per D-25: V18__replace_mock_with_real_code_data.sql 생성.
    V9의 Mock 데이터를 참고하여 실 코드 데이터로 교체. 운영은 신규 DB 전제이므로 DELETE + INSERT 패턴:
    ```sql
    -- 계급 코드 실 데이터 (해군 계급 체계)
    DELETE FROM tb_rank_code;
    INSERT INTO tb_rank_code (code, name, sort_order) VALUES
    ('ADM', '대장', 1), ('VADM', '중장', 2), ('RADM', '소장', 3),
    ('CDRE', '준장', 4), ('CAPT', '대령', 5), ('CDR', '중령', 6),
    ('LCDR', '소령', 7), ('LT', '대위', 8), ('LTJG', '중위', 9),
    ('ENS', '소위', 10), ('WO', '준위', 11),
    ('SCPO', '원사', 12), ('CPO', '상사', 13), ('PO1', '중사', 14),
    ('PO2', '하사', 15), ('PO3', '병장', 16), ('SN', '상병', 17),
    ('SA', '일병', 18), ('SR', '이병', 19);

    -- 군구분 코드 (해군 기준)
    DELETE FROM tb_branch_code;
    INSERT INTO tb_branch_code (code, name, sort_order) VALUES
    ('NAVY_OFFICER', '해군장교', 1),
    ('NAVY_NCO', '해군부사관', 2),
    ('NAVY_ENLISTED', '해군병', 3),
    ('MARINE_OFFICER', '해병장교', 4),
    ('MARINE_NCO', '해병부사관', 5),
    ('MARINE_ENLISTED', '해병병', 6),
    ('CIVILIAN', '군무원', 7);

    -- 사망구분 기호 등 기타 코드 테이블도 V9 패턴 참고하여 교체
    ```
    (실제 데이터는 V9의 구조를 따르되 해군 공식 코드를 반영. V9 Mock 데이터가 이미 실제에 가깝다면 최소 변경)

    per D-27: V19__add_password_changed_column.sql 생성:
    ```sql
    ALTER TABLE tb_user ADD COLUMN password_changed BOOLEAN NOT NULL DEFAULT false;
    -- 기존 admin 계정도 false (첫 로그인 시 변경 강제)
    ```

    per D-27: User.java 엔티티에 `passwordChanged` 필드 추가:
    ```java
    @Column(name = "password_changed", nullable = false)
    private boolean passwordChanged = false;
    ```

    AuthService.java/AuthController.java 수정:
    - 로그인 성공 응답에 `passwordChanged` 플래그 포함
    - 비밀번호 변경 API 추가: `PUT /api/auth/change-password`
      - 요청: `{ currentPassword, newPassword }`
      - 현재 비밀번호 BCrypt 검증 후 새 비밀번호로 업데이트
      - passwordChanged = true로 설정
      - 새 비밀번호는 8자 이상, 영문+숫자 조합 검증

    per D-26: DataCheckResponse.java 생성 (record DTO):
    ```java
    public record DataCheckResponse(
        long rankCodeCount,
        long branchCodeCount,
        long deathTypeCodeCount,
        long veteransOfficeCount,
        boolean adminExists,
        String appVersion
    ) {}
    ```
    AdminController.java에 `GET /api/admin/data-check` 추가:
    - ADMIN 역할만 접근 가능
    - 각 코드 테이블 COUNT + admin 존재 여부 + app.version 반환
    - ApiResponse<DataCheckResponse> 형태로 반환
  </action>
  <verify>
    <automated>cd backend && ./gradlew compileJava && grep -n "password_changed" src/main/resources/db/migration/V19__add_password_changed_column.sql && grep -n "passwordChanged" src/main/java/com/navy/casualty/security/entity/User.java && grep -n "data-check" src/main/java/com/navy/casualty/admin/controller/AdminController.java</automated>
  </verify>
  <acceptance_criteria>
    - V18 SQL에 `DELETE FROM tb_rank_code` + `INSERT INTO tb_rank_code` 존재
    - V19 SQL에 `ALTER TABLE tb_user ADD COLUMN password_changed` 존재
    - User.java에 `private boolean passwordChanged` 필드 존재
    - AuthController.java에 `change-password` 매핑 존재
    - AdminController.java에 `data-check` 매핑 존재
    - DataCheckResponse.java에 `rankCodeCount`, `adminExists` 필드 존재
    - `./gradlew compileJava` 성공
  </acceptance_criteria>
  <done>V18 실 코드 데이터, V19 password_changed 컬럼, 비밀번호 변경 API, data-check API 구현 완료</done>
</task>

<task type="auto">
  <name>Task 2: 프론트엔드 비밀번호 변경 화면 + 리다이렉트</name>
  <files>
    frontend/src/pages/ChangePasswordPage.tsx,
    frontend/src/hooks/useAuth.ts,
    frontend/src/routes/index.tsx,
    frontend/src/pages/LoginPage.tsx
  </files>
  <read_first>
    frontend/src/hooks/useAuth.ts,
    frontend/src/routes/index.tsx,
    frontend/src/pages/LoginPage.tsx,
    frontend/src/components/auth/AuthGuard.tsx
  </read_first>
  <action>
    per D-27: ChangePasswordPage.tsx 생성 — Ant Design Form.
    레이아웃:
    - 중앙 정렬 Card (로그인 페이지와 유사한 스타일)
    - 제목: "비밀번호 변경" (첫 로그인 시에는 "초기 비밀번호를 변경해주세요" 안내 메시지)
    - 입력 필드: 현재 비밀번호, 새 비밀번호, 새 비밀번호 확인
    - 검증: 새 비밀번호 8자 이상, 영문+숫자 포함, 현재 비밀번호와 다름
    - 변경 버튼 클릭 시 `PUT /api/auth/change-password` 호출
    - 성공 시 메인 페이지(`/dead`)로 이동
    - 실패 시 에러 메시지 표시

    useAuth.ts 수정:
    - 로그인 응답에서 `passwordChanged` 필드를 상태에 저장
    - `useChangePassword` 뮤테이션 훅 추가 (또는 useAuth 내부 메서드)

    routes/index.tsx 수정:
    - `/change-password` 라우트 추가 (AuthGuard 내부, AppLayout 외부 — 사이드바 없이 전체 화면)
    - AuthGuard 또는 AppLayout에서 `passwordChanged === false`이면 `/change-password`로 리다이렉트

    LoginPage.tsx 수정:
    - 로그인 성공 후 `passwordChanged === false`이면 `/change-password`로 navigate
    - `passwordChanged === true`이면 기존대로 `/dead`로 navigate
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5 && grep -rn "change-password" src/routes/index.tsx && grep -rn "ChangePasswordPage" src/pages/ChangePasswordPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ChangePasswordPage.tsx 파일 존재, `Form`, `Input.Password`, `PUT`, `/api/auth/change-password` 포함
    - routes/index.tsx에 `path: 'change-password'` 라우트 존재
    - LoginPage.tsx에 `passwordChanged` 분기 로직 존재
    - `npx tsc --noEmit` TypeScript 컴파일 성공
  </acceptance_criteria>
  <done>비밀번호 변경 화면 구현 완료, 첫 로그인 시 비밀번호 변경 강제 리다이렉트 동작</done>
</task>

</tasks>

<verification>
- V18, V19 마이그레이션 파일 존재
- 백엔드 compileJava 성공
- 프론트엔드 tsc --noEmit 성공
- /api/auth/change-password API 존재
- /api/admin/data-check API 존재
- ChangePasswordPage 화면 존재
</verification>

<success_criteria>
운영 환경 초기 데이터가 적재되고, 첫 로그인 시 비밀번호 변경이 강제되며, 관리자가 데이터 정합성을 확인할 수 있다.
</success_criteria>

<output>
After completion, create `.planning/phases/08-verification-deploy/08-03-SUMMARY.md`
</output>
