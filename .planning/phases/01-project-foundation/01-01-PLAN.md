---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/build.gradle.kts
  - backend/settings.gradle.kts
  - backend/src/main/java/com/navy/casualty/CasualtyApplication.java
  - backend/src/main/java/com/navy/casualty/common/config/SecurityConfig.java
  - backend/src/main/java/com/navy/casualty/common/config/JpaAuditingConfig.java
  - backend/src/main/java/com/navy/casualty/common/config/QueryDslConfig.java
  - backend/src/main/java/com/navy/casualty/common/entity/BaseAuditEntity.java
  - backend/src/main/java/com/navy/casualty/common/dto/ApiResponse.java
  - backend/src/main/java/com/navy/casualty/common/exception/GlobalExceptionHandler.java
  - backend/src/main/resources/application.yml
  - backend/src/main/resources/application-dev.yml
  - backend/src/main/resources/application-prod.yml
  - backend/src/main/resources/db/migration/V1__init_schema.sql
  - backend/src/main/resources/db/migration/V2__create_code_tables.sql
  - backend/src/main/resources/db/migration/V3__create_dead_table.sql
  - backend/src/main/resources/db/migration/V4__create_wounded_table.sql
  - backend/src/main/resources/db/migration/V5__create_review_table.sql
  - backend/src/main/resources/db/migration/V6__create_audit_log_table.sql
  - backend/src/main/resources/db/migration/V7__create_document_issue_log.sql
  - backend/src/main/resources/db/migration/V8__create_users_roles.sql
  - backend/src/main/resources/db/migration/V9__insert_mock_code_data.sql
  - backend/src/test/java/com/navy/casualty/common/dto/ApiResponseTest.java
  - backend/src/test/java/com/navy/casualty/common/entity/BaseAuditEntityTest.java
  - backend/src/test/java/com/navy/casualty/FlywayMigrationTest.java
  - backend/src/test/resources/application-test.yml
autonomous: true
requirements: [INFRA-02, INFRA-03, INFRA-04]

must_haves:
  truths:
    - "Gradle 빌드가 성공하고 Spring Boot 앱이 시작된다"
    - "Flyway가 V1~V9 마이그레이션을 순서대로 실행한다"
    - "spring-session-jdbc SPRING_SESSION 테이블이 자동 생성된다"
    - "ApiResponse<T>가 D-05/D-06 형식에 맞게 JSON 직렬화된다"
    - "BaseAuditEntity soft delete 필드가 존재한다"
  artifacts:
    - path: "backend/build.gradle.kts"
      provides: "Spring Boot 3.5.13 + Java 21 빌드 설정"
      contains: "org.springframework.boot"
    - path: "backend/src/main/resources/db/migration/V1__init_schema.sql"
      provides: "DB 확장 및 공통 설정"
      min_lines: 5
    - path: "backend/src/main/resources/db/migration/V9__insert_mock_code_data.sql"
      provides: "Mock 초기 데이터"
      min_lines: 10
    - path: "backend/src/main/java/com/navy/casualty/common/dto/ApiResponse.java"
      provides: "공통 응답 래퍼"
      exports: ["ok", "error", "validationError"]
    - path: "backend/src/main/java/com/navy/casualty/common/entity/BaseAuditEntity.java"
      provides: "감사 기본 엔티티"
      contains: "deletedAt"
  key_links:
    - from: "backend/build.gradle.kts"
      to: "spring-session-jdbc"
      via: "Gradle 의존성"
      pattern: "spring-session-jdbc"
    - from: "backend/src/main/resources/application.yml"
      to: "Flyway 마이그레이션"
      via: "spring.flyway.locations"
      pattern: "classpath:db/migration"
    - from: "GlobalExceptionHandler.java"
      to: "ApiResponse.java"
      via: "validationError 팩토리 메서드"
      pattern: "ApiResponse.validationError"
---

<objective>
Spring Boot 3.5.13 + Java 21 백엔드 프로젝트를 생성하고, PostgreSQL 16 전체 DB 스키마(Flyway V1~V9)와 공통 코드(ApiResponse, BaseAuditEntity, GlobalExceptionHandler)를 완성한다.

Purpose: 이후 모든 Phase(2~8)가 이 백엔드 뼈대와 DB 스키마 위에서 동작한다. 패키지 구조, 응답 형식, soft delete 패턴이 여기서 확정된다.
Output: 빌드 가능한 Spring Boot 프로젝트 + Flyway 마이그레이션 9개 + 공통 코드 + 단위 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@.planning/phases/01-project-foundation/01-VALIDATION.md
@.planning/research/DOMAIN.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spring Boot 프로젝트 뼈대 + Gradle 빌드 + 공통 코드</name>
  <files>
    backend/build.gradle.kts,
    backend/settings.gradle.kts,
    backend/src/main/java/com/navy/casualty/CasualtyApplication.java,
    backend/src/main/java/com/navy/casualty/common/config/SecurityConfig.java,
    backend/src/main/java/com/navy/casualty/common/config/JpaAuditingConfig.java,
    backend/src/main/java/com/navy/casualty/common/config/QueryDslConfig.java,
    backend/src/main/java/com/navy/casualty/common/entity/BaseAuditEntity.java,
    backend/src/main/java/com/navy/casualty/common/dto/ApiResponse.java,
    backend/src/main/java/com/navy/casualty/common/exception/GlobalExceptionHandler.java,
    backend/src/main/resources/application.yml,
    backend/src/main/resources/application-dev.yml,
    backend/src/main/resources/application-prod.yml,
    backend/src/test/java/com/navy/casualty/common/dto/ApiResponseTest.java,
    backend/src/test/java/com/navy/casualty/common/entity/BaseAuditEntityTest.java,
    backend/src/test/java/com/navy/casualty/FlywayMigrationTest.java,
    backend/src/test/resources/application-test.yml
  </files>
  <read_first>
    .planning/phases/01-project-foundation/01-RESEARCH.md,
    .planning/phases/01-project-foundation/01-CONTEXT.md,
    .planning/research/ARCHITECTURE.md
  </read_first>
  <action>
1. Spring Initializr 또는 수동으로 backend/ 디렉토리에 Spring Boot 프로젝트 생성.

**build.gradle.kts** (per D-01, D-02 패키지 구조):
```kotlin
plugins {
    java
    id("org.springframework.boot") version "3.5.13"
    id("io.spring.dependency-management") version "1.1.7"
}

group = "com.navy"
version = "0.0.1-SNAPSHOT"

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

dependencies {
    implementation("org.springframework.boot:spring-boot-starter-web")
    implementation("org.springframework.boot:spring-boot-starter-data-jpa")
    implementation("org.springframework.boot:spring-boot-starter-validation")
    implementation("org.springframework.boot:spring-boot-starter-security")
    implementation("org.springframework.boot:spring-boot-starter-actuator")
    implementation("org.springframework.session:spring-session-jdbc")
    implementation("org.flywaydb:flyway-core")
    implementation("org.flywaydb:flyway-database-postgresql")
    implementation("com.querydsl:querydsl-jpa:5.1.0:jakarta")

    runtimeOnly("org.postgresql:postgresql")

    compileOnly("org.projectlombok:lombok")
    annotationProcessor("org.projectlombok:lombok")
    annotationProcessor("com.querydsl:querydsl-apt:5.1.0:jakarta")
    annotationProcessor("jakarta.persistence:jakarta.persistence-api")

    testImplementation("org.springframework.boot:spring-boot-starter-test")
    testRuntimeOnly("com.h2database:h2")
}

tasks.withType<Test> {
    useJUnitPlatform()
}
```

**settings.gradle.kts:**
```kotlin
rootProject.name = "navy-casualty"
```

Gradle Wrapper 포함 (gradle/wrapper/gradle-wrapper.properties, gradlew, gradlew.bat).

2. **CasualtyApplication.java**: `@SpringBootApplication` 메인 클래스. 패키지 `com.navy.casualty`.

3. **SecurityConfig.java** (per Research Pattern 4 - Phase 1 최소 설정):
- `@EnableWebSecurity`, CSRF disable, 모든 요청 permitAll
- `SessionCreationPolicy.IF_REQUIRED`
- 주석으로 "Phase 2에서 RBAC 적용 예정" 표시

4. **JpaAuditingConfig.java**:
- `@EnableJpaAuditing`
- `AuditorAware<String>` 빈 등록 (Phase 1에서는 "SYSTEM" 고정 반환, Phase 2에서 인증 사용자로 교체)

5. **QueryDslConfig.java**:
- `JPAQueryFactory` 빈 등록 (`new JPAQueryFactory(entityManager)`)

6. **BaseAuditEntity.java** (per Research Pattern 1):
- `@MappedSuperclass`, `@EntityListeners(AuditingEntityListener.class)`
- 필드: `createdAt`(LocalDateTime, @CreatedDate), `updatedAt`(LocalDateTime, @LastModifiedDate), `createdBy`(String, @CreatedBy), `lastModifiedBy`(String, @LastModifiedBy), `deletedAt`(LocalDateTime), `deletedBy`(String), `deleteReason`(String, length=500)
- 메서드: `softDelete(String deletedBy, String reason)`, `isDeleted()`
- `@Getter` Lombok

7. **ApiResponse.java** (per D-05, D-06):
- 필드: `boolean success`, `int status`, `String message`, `T data`, `List<FieldError> errors`
- `FieldError` 내부 클래스: `String field`, `String message`
- 팩토리 메서드: `ok(T data)`, `ok(T data, String message)`, `error(int status, String message)`, `validationError(List<FieldError> errors)` -- validationError는 status=400, message="유효성 검증 실패"
- `@Getter`, `@Builder`, `@AllArgsConstructor` Lombok

8. **GlobalExceptionHandler.java**:
- `@RestControllerAdvice`
- `@ExceptionHandler(MethodArgumentNotValidException.class)` -> `ApiResponse.validationError(...)` 반환 (400)
- `@ExceptionHandler(Exception.class)` -> `ApiResponse.error(500, "서버 내부 오류가 발생했습니다")` 반환
- 바인딩 에러에서 field + defaultMessage 추출하여 `FieldError` 리스트 생성

9. **application.yml**:
```yaml
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:navy_casualty}
    username: ${DB_USERNAME:navy_user}
    password: ${DB_PASSWORD:navy_pass}
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false
  session:
    store-type: jdbc
    jdbc:
      initialize-schema: always
    timeout: 30m
server:
  port: 8080
```

**application-dev.yml**: `spring.jpa.show-sql: true`, 로깅 레벨 DEBUG

**application-prod.yml**: `spring.session.jdbc.initialize-schema: never`, `spring.jpa.show-sql: false`, 로깅 레벨 WARN

10. **application-test.yml** (테스트용 H2):
```yaml
spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  session:
    store-type: none
```

11. **ApiResponseTest.java**: `ok()` 반환 시 success=true/status=200, `error(404, "not found")` 반환 시 success=false/status=404, `validationError()` 반환 시 status=400 + errors 리스트 검증.

12. **BaseAuditEntityTest.java**: `softDelete()` 호출 후 `isDeleted()==true`, `deletedAt!=null`, `deletedBy`/`deleteReason` 값 검증. 초기 상태 `isDeleted()==false` 검증.

13. **FlywayMigrationTest.java** (Wave 0 테스트 스텁, VALIDATION.md 참조):
- `@SpringBootTest` + `@ActiveProfiles("test")`를 사용하되, Flyway가 test 프로파일에서 비활성화되어 있으므로 SQL 파일의 존재 여부와 네이밍 규칙(V{N}__ 접두사)을 검증하는 단위 테스트로 작성
- V1~V9 파일이 classpath에 존재하는지 확인
- 각 파일이 비어있지 않은지 확인

14. **INFRA-04(오프라인 의존성 해결) 관련 사항:**
- 이 요구사항은 에어갭 Nexus/로컬 Maven 구성으로 시스템 외부 작업이며, CONTEXT.md의 Deferred Ideas에 등록되어 있음
- Phase 8 배포 준비 시 검증 예정
- build.gradle.kts에 `repositories { mavenLocal(); mavenCentral() }` 패턴을 적용하여 로컬 Maven 레포 우선 탐색 구조는 준비해 둠
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*ApiResponseTest" --tests "*BaseAuditEntityTest" --tests "*FlywayMigrationTest" -x integrationTest 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/build.gradle.kts contains 'id("org.springframework.boot") version "3.5.13"'
    - backend/build.gradle.kts contains 'spring-session-jdbc'
    - backend/build.gradle.kts contains 'querydsl-jpa:5.1.0:jakarta'
    - backend/build.gradle.kts contains 'flyway-database-postgresql'
    - backend/build.gradle.kts contains 'mavenLocal()'
    - CasualtyApplication.java contains '@SpringBootApplication'
    - SecurityConfig.java contains 'permitAll'
    - JpaAuditingConfig.java contains '@EnableJpaAuditing'
    - BaseAuditEntity.java contains 'deletedAt' AND 'softDelete' AND '@MappedSuperclass'
    - ApiResponse.java contains 'validationError' AND 'FieldError'
    - GlobalExceptionHandler.java contains 'MethodArgumentNotValidException' AND 'ApiResponse.validationError'
    - application.yml contains 'store-type: jdbc' AND 'classpath:db/migration'
    - application-test.yml contains 'jdbc:h2:mem:testdb'
    - ApiResponseTest.java exits 0
    - BaseAuditEntityTest.java exits 0
    - FlywayMigrationTest.java exits 0
  </acceptance_criteria>
  <done>
    Gradle 빌드 성공, ApiResponseTest + BaseAuditEntityTest + FlywayMigrationTest 통과, 패키지 구조가 D-01/D-02에 부합. INFRA-04는 에어갭 Nexus 구성으로 시스템 외부 작업이며 Phase 8 배포 준비 시 검증.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flyway 마이그레이션 V1~V9 (전체 DB 스키마 + Mock 데이터)</name>
  <files>
    backend/src/main/resources/db/migration/V1__init_schema.sql,
    backend/src/main/resources/db/migration/V2__create_code_tables.sql,
    backend/src/main/resources/db/migration/V3__create_dead_table.sql,
    backend/src/main/resources/db/migration/V4__create_wounded_table.sql,
    backend/src/main/resources/db/migration/V5__create_review_table.sql,
    backend/src/main/resources/db/migration/V6__create_audit_log_table.sql,
    backend/src/main/resources/db/migration/V7__create_document_issue_log.sql,
    backend/src/main/resources/db/migration/V8__create_users_roles.sql,
    backend/src/main/resources/db/migration/V9__insert_mock_code_data.sql
  </files>
  <read_first>
    .planning/research/DOMAIN.md,
    .planning/phases/01-project-foundation/01-CONTEXT.md,
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
DOMAIN.md와 REQUIREMENTS.md를 참조하여 PostgreSQL 16 SQL 작성. 모든 테이블에 created_at, updated_at, created_by, last_modified_by, deleted_at, deleted_by, delete_reason 감사 컬럼 포함 (BaseAuditEntity 매핑).

**V1__init_schema.sql:**
- `CREATE EXTENSION IF NOT EXISTS pgcrypto;` (UUID 생성용)
- 공통 설정 주석

**V2__create_code_tables.sql** (per D-03, D-04):
- `TB_RANK_CODE` (id BIGSERIAL PK, rank_name VARCHAR(50) NOT NULL, rank_group VARCHAR(20) NOT NULL, sort_order INT, 감사 컬럼)
  - rank_group: '병', '부사관', '준사관', '장교'
- `TB_BRANCH_CODE` (id BIGSERIAL PK, branch_name VARCHAR(50) NOT NULL UNIQUE, 감사 컬럼)
  - 군구분: 해군, 해병대
- `TB_DEATH_CODE` (id BIGSERIAL PK, code_symbol VARCHAR(10) NOT NULL UNIQUE, code_name VARCHAR(100) NOT NULL, 감사 컬럼)
  - D-04: 데이터 비워둠 (구조만 생성)
- `TB_DEATH_TYPE` (id BIGSERIAL PK, type_name VARCHAR(50) NOT NULL UNIQUE, 감사 컬럼)
  - 사망구분: 전사, 순직, 사고사, 병사, 자살, 기타
- `TB_UNIT_CODE` (id BIGSERIAL PK, unit_name VARCHAR(100) NOT NULL, parent_id BIGINT REFERENCES TB_UNIT_CODE(id), 감사 컬럼)
- `TB_VETERANS_OFFICE` (id BIGSERIAL PK, office_name VARCHAR(100) NOT NULL, office_type VARCHAR(20) NOT NULL, 감사 컬럼)
  - office_type: '보훈청', '보훈지청'

**V3__create_dead_table.sql** (DEAD-01~07):
- `TB_DEAD` (id BIGSERIAL PK, service_number VARCHAR(20) NOT NULL UNIQUE, name VARCHAR(50) NOT NULL, ssn_encrypted TEXT NOT NULL, birth_date DATE NOT NULL, rank_id BIGINT REFERENCES TB_RANK_CODE(id), branch_id BIGINT REFERENCES TB_BRANCH_CODE(id), unit_id BIGINT REFERENCES TB_UNIT_CODE(id), enlistment_date DATE, phone VARCHAR(20), death_type_id BIGINT REFERENCES TB_DEATH_TYPE(id), death_code_id BIGINT REFERENCES TB_DEATH_CODE(id), address TEXT, death_date DATE NOT NULL, status VARCHAR(20) NOT NULL DEFAULT 'REGISTERED', 감사 컬럼)
  - status enum 값: REGISTERED, CONFIRMED, NOTIFIED
  - UNIQUE 제약조건: service_number
  - INDEX: birth_date, name, status

**V4__create_wounded_table.sql** (WOND-01~07):
- `TB_WOUNDED` (id BIGSERIAL PK, service_number VARCHAR(20) NOT NULL UNIQUE, name VARCHAR(50) NOT NULL, ssn_encrypted TEXT NOT NULL, birth_date DATE NOT NULL, rank_id BIGINT REFERENCES TB_RANK_CODE(id), branch_id BIGINT REFERENCES TB_BRANCH_CODE(id), unit_id BIGINT REFERENCES TB_UNIT_CODE(id), enlistment_date DATE, phone VARCHAR(20), address TEXT, veterans_office_id BIGINT REFERENCES TB_VETERANS_OFFICE(id), disease_name VARCHAR(200), wound_type VARCHAR(20) NOT NULL, status VARCHAR(20) NOT NULL DEFAULT 'REGISTERED', 감사 컬럼)
  - wound_type: 'COMBAT_WOUND'(전공상), 'DUTY_WOUND'(공상), 'GENERAL_WOUND'(일반상이)
  - status: REGISTERED, UNDER_REVIEW, CONFIRMED, NOTIFIED

**V5__create_review_table.sql** (REVW-01~08):
- `TB_REVIEW` (id BIGSERIAL PK, review_round INT NOT NULL, review_date DATE, name VARCHAR(50) NOT NULL, service_number VARCHAR(20) NOT NULL, ssn_encrypted TEXT, birth_date DATE, enlistment_date DATE, unit_id BIGINT REFERENCES TB_UNIT_CODE(id), disease_name VARCHAR(200), unit_review_result VARCHAR(20), classification VARCHAR(20), notification_date TIMESTAMP, 감사 컬럼)
  - classification: 'COMBAT_WOUND', 'DUTY_WOUND', 'REJECTED', 'DEFERRED'
- `TB_REVIEW_HISTORY` (id BIGSERIAL PK, review_id BIGINT NOT NULL REFERENCES TB_REVIEW(id), review_round INT NOT NULL, snapshot JSONB NOT NULL, changed_at TIMESTAMP NOT NULL DEFAULT NOW(), changed_by VARCHAR(50))
  - 심사차수별 변경 이력 보존

**V6__create_audit_log_table.sql** (AUDIT-01~03):
- `TB_AUDIT_LOG` (id BIGSERIAL PK, user_id VARCHAR(50) NOT NULL, action VARCHAR(20) NOT NULL, target_table VARCHAR(50), target_id BIGINT, detail TEXT, ip_address VARCHAR(45), created_at TIMESTAMP NOT NULL DEFAULT NOW())
  - action: 'VIEW', 'CREATE', 'UPDATE', 'DELETE', 'PRINT', 'EXPORT'
  - append-only: 감사 컬럼 중 updated_at, deleted_at 등 없음 (수정/삭제 불가)
  - INDEX: created_at, user_id

**V7__create_document_issue_log.sql** (DOCU-08):
- `TB_DOCUMENT_ISSUE` (id BIGSERIAL PK, document_type VARCHAR(50) NOT NULL, target_table VARCHAR(50), target_id BIGINT, issue_purpose VARCHAR(500) NOT NULL, issued_by VARCHAR(50) NOT NULL, issued_at TIMESTAMP NOT NULL DEFAULT NOW())
  - INDEX: issued_at

**V8__create_users_roles.sql** (AUTH-01~07):
- `TB_USER` (id BIGSERIAL PK, username VARCHAR(50) NOT NULL UNIQUE, password VARCHAR(255) NOT NULL, name VARCHAR(50) NOT NULL, role VARCHAR(20) NOT NULL DEFAULT 'VIEWER', enabled BOOLEAN NOT NULL DEFAULT TRUE, account_locked BOOLEAN NOT NULL DEFAULT FALSE, failed_login_count INT NOT NULL DEFAULT 0, last_login_at TIMESTAMP, 감사 컬럼)
  - role: 'ADMIN', 'MANAGER', 'OPERATOR', 'VIEWER'
- 기본 관리자 계정은 Phase 2에서 생성

**V9__insert_mock_code_data.sql** (per D-03):
- TB_RANK_CODE에 10개 Mock 계급 삽입:
  - 병: 이등병(E-1), 일등병(E-2), 상등병(E-3), 병장(E-4)
  - 부사관: 하사(E-5), 중사(E-6), 상사(E-7), 원사(E-8)
  - 장교: 소위(O-1), 중위(O-2)
- TB_BRANCH_CODE에 2개: 해군, 해병대
- TB_DEATH_TYPE에 6개: 전사, 순직, 사고사, 병사, 자살, 기타
- TB_UNIT_CODE에 20개 Mock 부대 (해군작전사령부, 제1함대 등 주요 부대 수준)
- TB_VETERANS_OFFICE에 주요 보훈청/보훈지청 15개 (서울, 부산, 대전 등)
- TB_DEATH_CODE: 데이터 삽입 없음 (D-04 -- 공식 코드 미확정)
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*FlywayMigrationTest" -x integrationTest 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - V1__init_schema.sql contains 'pgcrypto'
    - V2__create_code_tables.sql contains 'TB_RANK_CODE' AND 'TB_BRANCH_CODE' AND 'TB_DEATH_CODE' AND 'TB_DEATH_TYPE' AND 'TB_UNIT_CODE' AND 'TB_VETERANS_OFFICE'
    - V3__create_dead_table.sql contains 'TB_DEAD' AND 'service_number' AND 'ssn_encrypted' AND 'birth_date' AND 'deleted_at'
    - V4__create_wounded_table.sql contains 'TB_WOUNDED' AND 'wound_type' AND 'veterans_office_id'
    - V5__create_review_table.sql contains 'TB_REVIEW' AND 'TB_REVIEW_HISTORY' AND 'snapshot JSONB'
    - V6__create_audit_log_table.sql contains 'TB_AUDIT_LOG' AND NOT contains 'deleted_at'
    - V7__create_document_issue_log.sql contains 'TB_DOCUMENT_ISSUE' AND 'issue_purpose'
    - V8__create_users_roles.sql contains 'TB_USER' AND 'account_locked' AND 'failed_login_count'
    - V9__insert_mock_code_data.sql contains INSERT statements for TB_RANK_CODE (at least 10 rows)
    - V9__insert_mock_code_data.sql contains INSERT for TB_UNIT_CODE (at least 20 rows)
    - V9__insert_mock_code_data.sql does NOT contain INSERT for TB_DEATH_CODE
  </acceptance_criteria>
  <done>
    Flyway V1~V9 마이그레이션 파일이 모두 존재하고, 구문 오류 없이 Gradle 빌드 통과. D-03(Mock 데이터), D-04(사망구분 코드 비워둠) 결정 준수.
  </done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew test --tests "*ApiResponseTest" --tests "*BaseAuditEntityTest" --tests "*FlywayMigrationTest"` 전체 통과
- build.gradle.kts에 Spring Boot 3.5.13, spring-session-jdbc, QueryDSL jakarta, Flyway postgresql 의존성 존재
- Flyway 마이그레이션 V1~V9 파일이 db/migration/ 디렉토리에 존재
- ApiResponse.java가 D-05/D-06 구조 준수
- BaseAuditEntity.java에 softDelete/deletedAt 패턴 존재
</verification>

<success_criteria>
- `./gradlew test --tests "*ApiResponseTest" --tests "*BaseAuditEntityTest" --tests "*FlywayMigrationTest"` 결과 BUILD SUCCESSFUL
- 모든 SQL 파일이 PostgreSQL 호환 구문
- INFRA-02(Flyway 마이그레이션) 요구사항 충족
- INFRA-03(spring-session-jdbc) 설정 완료
- INFRA-04: 에어갭 Nexus 구성은 시스템 외부 작업이며, mavenLocal() 우선 탐색 구조 준비됨. Phase 8 배포 준비 시 검증.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-01-SUMMARY.md`
</output>
