---
phase: 01-project-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/vite.config.ts
  - frontend/vitest.config.ts
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/api/client.ts
  - frontend/src/components/layout/AppLayout.tsx
  - frontend/src/components/layout/PlaceholderPage.tsx
  - frontend/src/pages/ErrorPage.tsx
  - frontend/src/pages/NotFoundPage.tsx
  - frontend/src/routes/index.tsx
  - frontend/src/test/setup.ts
  - frontend/src/__tests__/AppLayout.test.tsx
  - frontend/src/__tests__/routes.test.tsx
  - frontend/src/styles/global.css
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "프론트엔드 앱이 빌드되고 Vite 개발 서버가 시작된다"
    - "Ant Design Layout(Sider 220px + Header 64px + Content)이 렌더링된다"
    - "Sider 메뉴에 6개 항목(사망자관리, 상이자관리, 전공사상심사, 문서출력, 통계/현황, 시스템관리)이 표시된다"
    - "각 라우트 클릭 시 해당 PlaceholderPage로 이동한다"
    - "ConfigProvider에 한국어 로케일과 UI-SPEC.md 테마 토큰이 적용된다"
  artifacts:
    - path: "frontend/src/components/layout/AppLayout.tsx"
      provides: "Ant Design Layout 셸"
      min_lines: 40
    - path: "frontend/src/routes/index.tsx"
      provides: "React Router 라우트 설정"
      contains: "createBrowserRouter"
    - path: "frontend/src/api/client.ts"
      provides: "Axios 인스턴스"
      contains: "axios.create"
    - path: "frontend/vite.config.ts"
      provides: "Vite 빌드 + 프록시 설정"
      contains: "/api"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/routes/index.tsx"
      via: "RouterProvider"
      pattern: "RouterProvider"
    - from: "frontend/src/main.tsx"
      to: "antd ConfigProvider"
      via: "ConfigProvider locale={koKR}"
      pattern: "koKR"
    - from: "frontend/src/routes/index.tsx"
      to: "frontend/src/components/layout/AppLayout.tsx"
      via: "element: <AppLayout />"
      pattern: "AppLayout"
---

<objective>
React 18 + TypeScript + Ant Design 5.x 프론트엔드 프로젝트를 생성하고, Layout 셸(Sider + Header + Content), 라우팅, Axios 클라이언트, TanStack Query 설정을 완성한다.

Purpose: 이후 Phase(3~7)에서 각 메뉴의 실제 CRUD 화면을 이 Layout과 라우트 구조 안에 추가한다. 프론트엔드 번들은 Dockerfile에 포함되어 INFRA-01(Docker Compose 단일 명령 배포)의 일부를 구성한다.
Output: 빌드 가능한 React 프로젝트 + AppLayout + 6개 라우트(placeholder) + 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-UI-SPEC.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vite + React 프로젝트 초기화 + Ant Design ConfigProvider + Axios</name>
  <files>
    frontend/package.json,
    frontend/tsconfig.json,
    frontend/tsconfig.app.json,
    frontend/tsconfig.node.json,
    frontend/vite.config.ts,
    frontend/vitest.config.ts,
    frontend/index.html,
    frontend/src/main.tsx,
    frontend/src/App.tsx,
    frontend/src/api/client.ts,
    frontend/src/styles/global.css,
    frontend/src/test/setup.ts
  </files>
  <read_first>
    .planning/phases/01-project-foundation/01-UI-SPEC.md,
    .planning/phases/01-project-foundation/01-RESEARCH.md
  </read_first>
  <action>
1. Vite로 React + TypeScript 프로젝트 생성:
```bash
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install antd @ant-design/icons
npm install @tanstack/react-query axios react-router-dom dayjs
npm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom
```

2. **vite.config.ts** (Research Code Examples 참조):
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
      },
    },
  },
});
```

3. **vitest.config.ts**:
```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: './src/test/setup.ts',
    css: true,
  },
});
```

4. **src/test/setup.ts**:
```typescript
import '@testing-library/jest-dom';
```

5. **package.json scripts** (기존 Vite 기본값에 추가):
```json
{
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  }
}
```

6. **src/main.tsx**:
- `ConfigProvider` wrapping: `locale={koKR}`, theme token 설정 (UI-SPEC.md 기준):
  - `colorPrimary: '#1677ff'`, `colorError: '#ff4d4f'`, `colorSuccess: '#52c41a'`, `colorWarning: '#faad14'`, `fontSize: 14`, `fontWeightStrong: 600`
- `QueryClientProvider` wrapping: 기본 QueryClient 생성
- `RouterProvider` wrapping: `router` import from `./routes`

7. **src/api/client.ts**:
```typescript
import axios from 'axios';

const apiClient = axios.create({
  baseURL: '/api',
  timeout: 30000,
  headers: { 'Content-Type': 'application/json' },
});

// 세션 만료 시 로그인 페이지로 리다이렉트 (Phase 2에서 완성)
apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);

export default apiClient;
```

8. **src/styles/global.css**: 최소 리셋 스타일. `body { margin: 0; font-family: system-ui, -apple-system, "Segoe UI", sans-serif; }`
  </action>
  <verify>
    <automated>cd frontend && npm run test:run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json contains "antd" AND "@ant-design/icons" AND "@tanstack/react-query" AND "axios" AND "react-router-dom" AND "dayjs"
    - frontend/package.json contains "react": with version starting "^18"
    - frontend/vite.config.ts contains "'/api'" AND "target: 'http://localhost:8080'"
    - frontend/vitest.config.ts contains "jsdom" AND "setup.ts"
    - frontend/src/main.tsx contains "ConfigProvider" AND "koKR" AND "QueryClientProvider" AND "RouterProvider"
    - frontend/src/main.tsx contains "colorPrimary: '#1677ff'"
    - frontend/src/api/client.ts contains "axios.create" AND "baseURL: '/api'" AND "status === 401"
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    프론트엔드 프로젝트가 빌드되고, ConfigProvider에 한국어 로케일 + UI-SPEC.md 테마 토큰이 적용됨
  </done>
</task>

<task type="auto">
  <name>Task 2: AppLayout + 라우팅 + PlaceholderPage + 테스트</name>
  <files>
    frontend/src/components/layout/AppLayout.tsx,
    frontend/src/components/layout/PlaceholderPage.tsx,
    frontend/src/pages/ErrorPage.tsx,
    frontend/src/pages/NotFoundPage.tsx,
    frontend/src/routes/index.tsx,
    frontend/src/__tests__/AppLayout.test.tsx,
    frontend/src/__tests__/routes.test.tsx
  </files>
  <read_first>
    .planning/phases/01-project-foundation/01-UI-SPEC.md,
    .planning/phases/01-project-foundation/01-RESEARCH.md,
    frontend/src/main.tsx
  </read_first>
  <action>
1. **AppLayout.tsx** (UI-SPEC.md Layout Contract 엄격 준수):
```tsx
import { Layout, Menu } from 'antd';
import {
  TeamOutlined,
  MedicineBoxOutlined,
  AuditOutlined,
  PrinterOutlined,
  BarChartOutlined,
  SettingOutlined,
} from '@ant-design/icons';
import { Outlet, useNavigate, useLocation } from 'react-router-dom';

const { Sider, Header, Content } = Layout;

const menuItems = [
  { key: '/dead', icon: <TeamOutlined />, label: '사망자 관리' },
  { key: '/wounded', icon: <MedicineBoxOutlined />, label: '상이자 관리' },
  { key: '/review', icon: <AuditOutlined />, label: '전공사상심사' },
  { key: '/document', icon: <PrinterOutlined />, label: '문서 출력' },
  { key: '/statistics', icon: <BarChartOutlined />, label: '통계/현황' },
  { key: '/admin', icon: <SettingOutlined />, label: '시스템 관리' },
];
```

Layout 구조 (UI-SPEC.md 명시 값):
- `<Layout style={{ minHeight: '100vh' }}>`
- `<Sider width={220} theme="dark" collapsible>` -- 상단 로고 영역에 시스템 제목 표시
- `<Header style={{ background: '#fff', height: 64, padding: '0 24px', display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>` -- 좌측: "해군 사상자 관리 시스템" 텍스트, 우측: Phase 2용 placeholder (빈 span)
- `<Content style={{ margin: 16, background: '#fff', padding: 24 }}>` -- `<Outlet />`
- Menu `onClick`으로 `navigate(key)`, `selectedKeys`를 `useLocation().pathname`으로 설정

2. **PlaceholderPage.tsx**:
- props: `{ title: string }`
- Ant Design `Result` 또는 `Empty` 컴포넌트 사용
- heading: `title`, body: "이 기능은 아직 개발 중입니다." (UI-SPEC.md 카피)

3. **ErrorPage.tsx**:
- "페이지를 표시할 수 없습니다. 관리자에게 문의하세요." (UI-SPEC.md 카피)

4. **NotFoundPage.tsx**:
- heading: "요청한 페이지를 찾을 수 없습니다"
- body: "URL을 확인하거나 메뉴에서 다시 선택하세요." (UI-SPEC.md 카피)

5. **routes/index.tsx** (Research Code Examples 참조):
```tsx
import { createBrowserRouter, Navigate } from 'react-router-dom';

const router = createBrowserRouter([
  {
    path: '/',
    element: <AppLayout />,
    errorElement: <ErrorPage />,
    children: [
      { index: true, element: <Navigate to="/dead" replace /> },
      { path: 'dead', element: <PlaceholderPage title="사망자 관리" /> },
      { path: 'wounded', element: <PlaceholderPage title="상이자 관리" /> },
      { path: 'review', element: <PlaceholderPage title="전공사상심사" /> },
      { path: 'document', element: <PlaceholderPage title="문서 출력" /> },
      { path: 'statistics', element: <PlaceholderPage title="통계/현황" /> },
      { path: 'admin', element: <PlaceholderPage title="시스템 관리" /> },
    ],
  },
  { path: '/login', element: <PlaceholderPage title="로그인" /> },
  { path: '*', element: <NotFoundPage /> },
]);

export default router;
```

6. **AppLayout.test.tsx**:
- Ant Design Layout.Sider가 렌더링되는지 확인
- "해군 사상자 관리 시스템" 텍스트 존재 확인
- 6개 메뉴 라벨("사망자 관리", "상이자 관리", "전공사상심사", "문서 출력", "통계/현황", "시스템 관리") 존재 확인

7. **routes.test.tsx**:
- router 객체에 '/dead', '/wounded', '/review', '/document', '/statistics', '/admin' 라우트 존재 확인
- '/' 인덱스 라우트가 Navigate to="/dead"인지 확인
  </action>
  <verify>
    <automated>cd frontend && npm run test:run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - AppLayout.tsx contains 'width={220}' or 'width: 220'
    - AppLayout.tsx contains "해군 사상자 관리 시스템"
    - AppLayout.tsx contains 'TeamOutlined' AND 'MedicineBoxOutlined' AND 'AuditOutlined' AND 'PrinterOutlined' AND 'BarChartOutlined' AND 'SettingOutlined'
    - AppLayout.tsx contains '<Outlet'
    - PlaceholderPage.tsx contains "이 기능은 아직 개발 중입니다"
    - ErrorPage.tsx contains "관리자에게 문의하세요"
    - NotFoundPage.tsx contains "요청한 페이지를 찾을 수 없습니다"
    - routes/index.tsx contains "createBrowserRouter" AND '/dead' AND '/wounded' AND '/review' AND '/document' AND '/statistics' AND '/admin' AND '/login'
    - `npm run test:run` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    AppLayout이 UI-SPEC.md Layout Contract대로 렌더링되고, 6개 메뉴와 라우팅이 동작하며, 테스트 통과
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` 성공
- `cd frontend && npm run test:run` 전체 통과
- AppLayout.tsx가 UI-SPEC.md Layout 사양(Sider 220px dark, Header 64px white, Content margin 16 padding 24) 준수
- 6개 메뉴 항목이 UI-SPEC.md Sider Menu Labels와 일치
</verification>

<success_criteria>
- `npm run build` BUILD SUCCESS
- `npm run test:run` 전체 테스트 통과
- Vite 개발 서버 시작 가능 (`npm run dev`)
- UI-SPEC.md 계약의 모든 카피, 레이아웃 사양 반영
- INFRA-01 기여: 프론트엔드 번들이 Dockerfile의 frontend-builder 스테이지에서 빌드 가능
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-02-SUMMARY.md`
</output>
