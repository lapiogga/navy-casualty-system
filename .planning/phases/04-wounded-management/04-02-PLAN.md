---
phase: 04-wounded-management
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - frontend/src/types/wounded.ts
  - frontend/src/api/wounded.ts
  - frontend/src/pages/wounded/WoundedListPage.tsx
  - frontend/src/pages/wounded/WoundedFormModal.tsx
  - frontend/src/pages/wounded/WoundedDeleteModal.tsx
  - frontend/src/routes/index.tsx
autonomous: true
requirements: [WOND-01, WOND-02, WOND-03, WOND-04, WOND-06, WOND-07]

must_haves:
  truths:
    - "상이자 관리 메뉴 클릭 시 /wounded 경로에서 WoundedListPage가 렌더링된다"
    - "검색 폼에서 군구분/군번/성명/생년월일/계급/소속/상이구분/상태 복합 검색이 가능하다"
    - "검색 폼 접기/펼치기 토글이 동작한다"
    - "조회 버튼 클릭 시 1페이지로 리셋되며 서버사이드 페이징 Table이 갱신된다"
    - "등록 Modal에서 보훈청명 Select, 병명 Input, 상이구분 Select 필드가 존재한다"
    - "등록 버튼 클릭 시 Modal이 열리고, 주민번호 체크섬 검증 후 등록 API를 호출한다"
    - "수정 버튼 클릭 시 기존 데이터가 채워진 Modal이 열린다"
    - "삭제 버튼 클릭 시 삭제 사유 입력 Modal이 열린다"
    - "등록/수정/삭제 후 목록이 자동 갱신된다 (invalidateQueries)"
    - "상태 컬럼에 4단계 상태 Tag 표시 (REGISTERED/UNDER_REVIEW/CONFIRMED/NOTIFIED)"
  artifacts:
    - path: "frontend/src/types/wounded.ts"
      provides: "WoundedRecord, WoundedSearchParams, WoundedCreateForm, WoundedUpdateForm 타입"
      contains: "WoundedRecord"
    - path: "frontend/src/api/wounded.ts"
      provides: "react-query 훅 (useWoundedList, useCreateWounded, useUpdateWounded, useDeleteWounded, useVeteransOffices)"
      contains: "useWoundedList"
    - path: "frontend/src/pages/wounded/WoundedListPage.tsx"
      provides: "상이자 목록 + 검색 폼 화면"
      contains: "useWoundedList"
    - path: "frontend/src/pages/wounded/WoundedFormModal.tsx"
      provides: "등록/수정 Modal (보훈청 Select + 병명 + 상이구분)"
      contains: "veteransOfficeId"
  key_links:
    - from: "WoundedListPage.tsx"
      to: "api/wounded.ts"
      via: "useWoundedList hook"
      pattern: "useWoundedList"
    - from: "WoundedFormModal.tsx"
      to: "api/wounded.ts"
      via: "useCreateWounded / useUpdateWounded hooks"
      pattern: "useCreateWounded|useUpdateWounded"
    - from: "WoundedFormModal.tsx"
      to: "api/wounded.ts"
      via: "useVeteransOffices hook for Select dropdown"
      pattern: "useVeteransOffices"
    - from: "routes/index.tsx"
      to: "WoundedListPage.tsx"
      via: "route definition"
      pattern: "path.*wounded.*WoundedListPage"
---

<objective>
상이자 관리 프론트엔드 전체를 구현한다: TypeScript 타입, react-query API 훅, 목록 페이지(검색 폼 + 서버사이드 Table), 등록/수정/삭제 Modal, 라우팅 연결. Phase 3 Dead 프론트엔드 패턴을 복제하되 상이자 고유 필드(보훈청명 Select, 병명, 상이구분)와 4단계 상태를 반영한다.

Purpose: Phase 4 프론트엔드. 백엔드 API(04-01)와 결합하여 상이자 CRUD 전체 흐름을 완성한다.
Output: /wounded 경로에 상이자 관리 화면 완성
</objective>

<execution_context>
@C:\Users\User\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\User\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-wounded-management/04-01-SUMMARY.md

<interfaces>
<!-- 기존 프론트엔드 인프라 (Phase 2에서 생성) -->

From frontend/src/api/client.ts:
```typescript
import axios from 'axios';
const apiClient = axios.create({ baseURL: '/api', timeout: 30000 });
// 401 -> /login 리다이렉트 인터셉터 포함
export default apiClient;
```

From frontend/src/hooks/useAuth.tsx:
```typescript
interface AuthContextValue {
  user: AuthUser | null; loading: boolean;
  login, logout, isAdmin, isManagerOrAbove, hasRole(role: Role): boolean;
}
```

From frontend/src/types/auth.ts:
```typescript
export type Role = 'ADMIN' | 'MANAGER' | 'OPERATOR' | 'VIEWER';
```

From frontend/src/routes/index.tsx (현재 상태):
```typescript
{ path: 'wounded', element: <PlaceholderPage title="상이자 관리" /> },
// -> WoundedListPage로 교체 필요
```

From frontend/src/utils/rrnValidation.ts:
```typescript
export function validateRrn(rrn: string | null | undefined): boolean;
// 주민번호 체크섬 검증 함수 (Phase 3에서 생성, 재사용)
```

From frontend/src/types/dead.ts (참조 패턴):
```typescript
export interface PageData<T> { content: T[]; totalElements: number; totalPages: number; size: number; number: number; }
export interface RankCode { id: number; rankName: string; rankGroup: string; sortOrder: number; }
export interface BranchCode { id: number; branchName: string; }
export interface UnitCode { id: number; unitName: string; parentId: number | null; }
```

From frontend/src/api/dead.ts (참조 패턴):
```typescript
// deadKeys, useDeadList, useCreateDead, useUpdateDead, useDeleteDead, useUpdateDeadStatus
// useRanks, useBranches, useUnits 훅 -> 상이자에서도 동일 코드 훅 재사용
// getErrorMessage 헬퍼: axios.isAxiosError 타입 가드 패턴
```

From frontend/src/pages/dead/DeadListPage.tsx (참조 패턴):
```typescript
// 검색 폼 Card 접기/펼치기 + 조회/초기화 + 서버사이드 Table + Tag 상태 컬럼
// 등록/수정/삭제 Modal 연동 패턴
```

From backend API 계약 (04-01에서 생성):
- GET /api/wounded?branchId=1&name=홍&page=0&size=10 -> ApiResponse<Page<WoundedResponse>>
- POST /api/wounded -> 201 ApiResponse<WoundedResponse>
- PUT /api/wounded/{id} -> ApiResponse<WoundedResponse>
- DELETE /api/wounded/{id} body: {reason: "..."} -> ApiResponse<Void>
- PUT /api/wounded/{id}/status?status=UNDER_REVIEW -> ApiResponse<Void>
- GET /api/codes/veterans-offices -> ApiResponse<List<VeteransOffice>>
- GET /api/codes/ranks, branches, units -> 기존 코드 훅 재사용
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript 타입 + react-query API 훅 (보훈청 포함)</name>
  <files>
    frontend/src/types/wounded.ts
    frontend/src/api/wounded.ts
  </files>
  <read_first>
    frontend/src/types/dead.ts
    frontend/src/api/dead.ts
    frontend/src/api/client.ts
    frontend/src/types/auth.ts
    frontend/src/hooks/useAuth.tsx
    frontend/src/utils/rrnValidation.ts
    frontend/package.json
  </read_first>
  <action>
    1. frontend/src/types/wounded.ts 생성 (dead.ts 복제 + 차이점):
       ```typescript
       // PageData, RankCode, BranchCode, UnitCode는 dead.ts에서 이미 정의됨 -> re-export
       export type { PageData, RankCode, BranchCode, UnitCode } from './dead';

       export interface VeteransOffice {
         id: number;
         officeName: string;
         officeType: string;
       }

       export type WoundType = 'COMBAT_WOUND' | 'DUTY_WOUND' | 'GENERAL_WOUND';

       export interface WoundedRecord {
         id: number;
         serviceNumber: string;
         name: string;
         ssnMasked: string;
         birthDate: string;
         rankName: string | null;
         branchName: string | null;
         unitName: string | null;
         enlistmentDate: string | null;
         phone: string | null;
         address: string | null;
         veteransOfficeName: string | null;
         diseaseName: string | null;
         woundTypeName: string | null;
         status: 'REGISTERED' | 'UNDER_REVIEW' | 'CONFIRMED' | 'NOTIFIED';
         createdAt: string;
       }

       export interface WoundedSearchParams {
         branchId?: number;
         serviceNumber?: string;
         name?: string;
         birthDate?: string;
         rankId?: number;
         unitId?: number;
         woundType?: WoundType;
         status?: string;
         page?: number;
         size?: number;
       }

       export interface WoundedCreateForm {
         serviceNumber: string;
         name: string;
         ssn: string;
         birthDate: string;
         rankId?: number;
         branchId?: number;
         unitId?: number;
         enlistmentDate?: string;
         phone?: string;
         address?: string;
         veteransOfficeId?: number;
         diseaseName?: string;
         woundType: WoundType;
       }

       export interface WoundedUpdateForm {
         name: string;
         ssn: string;
         birthDate: string;
         rankId?: number;
         branchId?: number;
         unitId?: number;
         enlistmentDate?: string;
         phone?: string;
         address?: string;
         veteransOfficeId?: number;
         diseaseName?: string;
         woundType: WoundType;
       }
       ```

    2. frontend/src/api/wounded.ts 생성 (dead.ts 복제 + 차이점):
       ** 중요: onError 콜백에서 `any` 타입 사용 금지. axios.isAxiosError() 타입 가드 사용. **
       ** 코드 테이블 훅(useRanks, useBranches, useUnits)은 dead.ts에서 이미 정의됨 -> import해서 re-export **
       ```typescript
       import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
       import axios from 'axios';
       import { message } from 'antd';
       import apiClient from './client';
       import type { WoundedRecord, WoundedSearchParams, WoundedCreateForm, WoundedUpdateForm, VeteransOffice } from '../types/wounded';
       import type { PageData } from '../types/dead';

       // 코드 테이블 훅 re-export (dead.ts에서 정의된 것 재사용)
       export { useRanks, useBranches, useUnits } from './dead';

       interface ApiResponse<T> { success: boolean; status: number; message: string; data: T; }

       function getErrorMessage(error: unknown, fallback: string): string {
         if (axios.isAxiosError<{ message: string }>(error)) {
           return error.response?.data?.message || fallback;
         }
         return fallback;
       }

       export const woundedKeys = {
         all: ['wounded'] as const,
         lists: () => [...woundedKeys.all, 'list'] as const,
         list: (params: WoundedSearchParams) => [...woundedKeys.lists(), params] as const,
       };

       export function useWoundedList(params: WoundedSearchParams) {
         return useQuery({
           queryKey: woundedKeys.list(params),
           queryFn: async () => {
             const res = await apiClient.get<ApiResponse<PageData<WoundedRecord>>>('/wounded', { params });
             return res.data.data;
           },
         });
       }

       export function useCreateWounded() {
         const qc = useQueryClient();
         return useMutation({
           mutationFn: (data: WoundedCreateForm) => apiClient.post('/wounded', data).then(r => r.data),
           onSuccess: () => { qc.invalidateQueries({ queryKey: woundedKeys.lists() }); message.success('등록되었습니다'); },
           onError: (error: unknown) => { message.error(getErrorMessage(error, '등록에 실패했습니다')); },
         });
       }

       export function useUpdateWounded() {
         const qc = useQueryClient();
         return useMutation({
           mutationFn: ({ id, data }: { id: number; data: WoundedUpdateForm }) => apiClient.put(`/wounded/${id}`, data).then(r => r.data),
           onSuccess: () => { qc.invalidateQueries({ queryKey: woundedKeys.lists() }); message.success('수정되었습니다'); },
           onError: (error: unknown) => { message.error(getErrorMessage(error, '수정에 실패했습니다')); },
         });
       }

       export function useDeleteWounded() {
         const qc = useQueryClient();
         return useMutation({
           mutationFn: ({ id, reason }: { id: number; reason: string }) => apiClient.delete(`/wounded/${id}`, { data: { reason } }).then(r => r.data),
           onSuccess: () => { qc.invalidateQueries({ queryKey: woundedKeys.lists() }); message.success('삭제되었습니다'); },
           onError: (error: unknown) => { message.error(getErrorMessage(error, '삭제에 실패했습니다')); },
         });
       }

       export function useUpdateWoundedStatus() {
         const qc = useQueryClient();
         return useMutation({
           mutationFn: ({ id, status }: { id: number; status: string }) => apiClient.put(`/wounded/${id}/status`, null, { params: { status } }).then(r => r.data),
           onSuccess: () => { qc.invalidateQueries({ queryKey: woundedKeys.lists() }); message.success('상태가 변경되었습니다'); },
         });
       }

       // 보훈청 코드 훅 (상이자 전용)
       export function useVeteransOffices() {
         return useQuery({
           queryKey: ['codes', 'veterans-offices'],
           queryFn: () => apiClient.get<ApiResponse<VeteransOffice[]>>('/codes/veterans-offices').then(r => r.data.data),
         });
       }
       ```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/types/wounded.ts 파일 존재
    - grep "WoundedRecord" frontend/src/types/wounded.ts 결과 있음
    - grep "VeteransOffice" frontend/src/types/wounded.ts 결과 있음
    - grep "WoundType" frontend/src/types/wounded.ts 결과 있음
    - grep "COMBAT_WOUND" frontend/src/types/wounded.ts 결과 있음
    - grep "UNDER_REVIEW" frontend/src/types/wounded.ts 결과 있음
    - grep "veteransOfficeId" frontend/src/types/wounded.ts 결과 있음
    - frontend/src/api/wounded.ts 파일 존재
    - grep "useWoundedList" frontend/src/api/wounded.ts 결과 있음
    - grep "useCreateWounded" frontend/src/api/wounded.ts 결과 있음
    - grep "useUpdateWounded" frontend/src/api/wounded.ts 결과 있음
    - grep "useDeleteWounded" frontend/src/api/wounded.ts 결과 있음
    - grep "useVeteransOffices" frontend/src/api/wounded.ts 결과 있음
    - grep "isAxiosError" frontend/src/api/wounded.ts 결과 있음
    - grep -c "any" frontend/src/api/wounded.ts 결과 0 (any 타입 없음)
    - grep "useRanks.*useBranches.*useUnits" 또는 개별 re-export 확인
    - npx tsc --noEmit 에러 없음
  </acceptance_criteria>
  <done>상이자 TypeScript 타입(WoundedRecord, VeteransOffice, WoundType 포함), react-query API 훅 7개(CRUD 4 + 상태변경 1 + 보훈청 1 + 코드 re-export) 완성. any 타입 없이 isAxiosError 타입 가드 사용.</done>
</task>

<task type="auto">
  <name>Task 2: 상이자 목록 페이지 + 등록/수정 Modal + 삭제 Modal + 라우팅 연결</name>
  <files>
    frontend/src/pages/wounded/WoundedListPage.tsx
    frontend/src/pages/wounded/WoundedFormModal.tsx
    frontend/src/pages/wounded/WoundedDeleteModal.tsx
    frontend/src/routes/index.tsx
  </files>
  <read_first>
    frontend/src/api/wounded.ts
    frontend/src/types/wounded.ts
    frontend/src/utils/rrnValidation.ts
    frontend/src/hooks/useAuth.tsx
    frontend/src/pages/dead/DeadListPage.tsx
    frontend/src/pages/dead/DeadFormModal.tsx
    frontend/src/pages/dead/DeadDeleteModal.tsx
    frontend/src/routes/index.tsx
    frontend/src/api/dead.ts
  </read_first>
  <action>
    1. frontend/src/pages/wounded/WoundedListPage.tsx 생성 (DeadListPage.tsx 복제 + 차이점):
       - 검색 폼 Card 접기/펼치기 (searchExpanded state)
       - 검색 필드: 군구분 Select(useBranches), 군번 Input, 성명 Input, 생년월일 DatePicker, 계급 Select(useRanks), 소속 Select(useUnits), 상이구분 Select(3개 옵션: 전공상/공상/일반상이), 상태 Select(4개 옵션: REGISTERED/UNDER_REVIEW/CONFIRMED/NOTIFIED)
       - 상이구분 Select 옵션:
         ```tsx
         <Select placeholder="상이구분 선택" allowClear>
           <Select.Option value="COMBAT_WOUND">전공상</Select.Option>
           <Select.Option value="DUTY_WOUND">공상</Select.Option>
           <Select.Option value="GENERAL_WOUND">일반상이</Select.Option>
         </Select>
         ```
       - 조회 버튼: setPage(1) + 검색 파라미터 설정
       - Ant Design Table columns:
         - 번호, 군구분, 군번, 성명, 주민번호(마스킹), 계급, 소속, 보훈청명, 병명, 상이구분, 상태, 등록일, 관리
         - Dead 대비 추가 컬럼: 보훈청명(veteransOfficeName), 병명(diseaseName), 상이구분(woundTypeName)
         - Dead 대비 제거 컬럼: 사망일자, 사망구분, 사망코드
         - 상태 Tag 색상: REGISTERED=blue, UNDER_REVIEW=orange, CONFIRMED=green, NOTIFIED=gold
       - 관리 컬럼: OPERATOR 이상 수정, MANAGER 이상 삭제/상태변경
       - 상단: 등록 버튼 + Excel 다운로드 버튼(disabled placeholder, Plan 03에서 활성화)
       - useWoundedList(searchParams) 훅 사용

    2. frontend/src/pages/wounded/WoundedFormModal.tsx 생성 (DeadFormModal.tsx 복제 + 차이점):
       - props: open, onClose, editRecord?(WoundedRecord | null)
       - Ant Design Modal + Form:
         - 군번 Input(수정시 disabled), 성명 Input, 주민번호 Input(validateRrn 검증)
         - 생년월일 DatePicker(주민번호 자동 추출)
         - 계급 Select(useRanks), 군구분 Select(useBranches), 소속 Select(useUnits)
         - 입대일자 DatePicker, 전화번호 Input, 주소 Input
         - ** 상이자 고유 필드 **:
           - 보훈청명 Select (useVeteransOffices): `<Select placeholder="보훈청 선택" allowClear showSearch optionFilterProp="children">` 보훈청/보훈지청 전체 목록
           - 병명 Input (diseaseName, 최대 200자)
           - 상이구분 Select (woundType, 필수): 전공상/공상/일반상이
         - Dead 대비 제거 필드: 사망구분 Select, 사망코드 Select, 사망일자 DatePicker
         - 필수 필드: 군번, 성명, 주민번호, 생년월일, 상이구분
       - onOk: useCreateWounded.mutate() 또는 useUpdateWounded.mutate()
       - 날짜: dayjs 변환, "YYYY-MM-DD" 형식 전송

    3. frontend/src/pages/wounded/WoundedDeleteModal.tsx 생성 (DeadDeleteModal.tsx 복제):
       - props: open, onClose, recordId(number | null)
       - 삭제 사유 TextArea (필수)
       - useDeleteWounded.mutate({ id, reason })

    4. frontend/src/routes/index.tsx 수정:
       - `import WoundedListPage from '../pages/wounded/WoundedListPage';` 추가
       - `{ path: 'wounded', element: <PlaceholderPage title="상이자 관리" /> }` 를
         `{ path: 'wounded', element: <WoundedListPage /> }` 로 교체
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/wounded/WoundedListPage.tsx 존재
    - grep "useWoundedList" WoundedListPage.tsx 결과 있음
    - grep "searchExpanded" WoundedListPage.tsx 결과 있음
    - grep "setPage.*1" WoundedListPage.tsx 결과 있음 (검색 시 1페이지 리셋)
    - grep "COMBAT_WOUND\|DUTY_WOUND\|GENERAL_WOUND" WoundedListPage.tsx 결과 있음
    - grep "UNDER_REVIEW" WoundedListPage.tsx 결과 있음
    - grep "veteransOfficeName" WoundedListPage.tsx 결과 있음
    - grep "diseaseName" WoundedListPage.tsx 결과 있음
    - grep "woundTypeName" WoundedListPage.tsx 결과 있음
    - frontend/src/pages/wounded/WoundedFormModal.tsx 존재
    - grep "validateRrn" WoundedFormModal.tsx 결과 있음
    - grep "useCreateWounded\|useUpdateWounded" WoundedFormModal.tsx 결과 있음
    - grep "useVeteransOffices" WoundedFormModal.tsx 결과 있음
    - grep "veteransOfficeId" WoundedFormModal.tsx 결과 있음
    - grep "diseaseName" WoundedFormModal.tsx 결과 있음
    - grep "woundType" WoundedFormModal.tsx 결과 있음
    - frontend/src/pages/wounded/WoundedDeleteModal.tsx 존재
    - grep "useDeleteWounded" WoundedDeleteModal.tsx 결과 있음
    - grep "WoundedListPage" frontend/src/routes/index.tsx 결과 있음
    - grep "PlaceholderPage.*상이자" frontend/src/routes/index.tsx 결과 없음 (교체 완료)
    - npx tsc --noEmit 에러 없음
  </acceptance_criteria>
  <done>상이자 목록 페이지(검색 폼 접기/펼치기 + 서버사이드 Table + 4단계 상태 Tag + 보훈청/병명/상이구분 컬럼), 등록/수정 Modal(보훈청 Select + 병명 + 상이구분 + 주민번호 검증), 삭제 Modal(사유 필수), 라우팅 연결 완성.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 에러 없음
- /wounded 경로에 WoundedListPage 연결됨
- 검색 폼 접기/펼치기, 조회 시 1페이지 리셋, 서버사이드 페이징
- 등록 Modal에 보훈청 Select + 병명 + 상이구분 + 주민번호 체크섬 검증
- 4단계 상태 Tag (REGISTERED/UNDER_REVIEW/CONFIRMED/NOTIFIED)
- API 훅 onError에 any 타입 없음
</verification>

<success_criteria>
- 상이자 목록 화면이 /wounded 경로에서 렌더링됨
- 검색 폼 8개 필드(군구분/군번/성명/생년월일/계급/소속/상이구분/상태) + 접기/펼치기
- Table에 보훈청명, 병명, 상이구분 컬럼 포함
- 등록 Modal에서 보훈청 Select(showSearch), 병명 Input, 상이구분 Select(필수)
- 삭제 Modal에서 사유 필수 입력
- 모든 CRUD + 상태변경 후 목록 자동 갱신
- TypeScript 컴파일 성공
</success_criteria>

<output>
After completion, create `.planning/phases/04-wounded-management/04-02-SUMMARY.md`
</output>
