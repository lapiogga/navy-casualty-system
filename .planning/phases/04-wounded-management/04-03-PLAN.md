---
phase: 04-wounded-management
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - backend/src/main/java/com/navy/casualty/wounded/service/WoundedExcelService.java
  - backend/src/main/java/com/navy/casualty/wounded/controller/WoundedController.java
  - backend/src/main/java/com/navy/casualty/wounded/repository/WoundedRepositoryCustom.java
  - backend/src/main/java/com/navy/casualty/wounded/repository/WoundedRepositoryImpl.java
  - backend/src/test/java/com/navy/casualty/wounded/WoundedExcelServiceTest.java
  - frontend/src/api/wounded.ts
  - frontend/src/pages/wounded/WoundedListPage.tsx
autonomous: true
requirements: [WOND-05]

must_haves:
  truths:
    - "GET /api/wounded/excel 이 검색 조건 적용된 Excel 파일을 다운로드한다"
    - "Excel 파일에서 주민번호가 역할별로 마스킹되어 있다"
    - "Excel 파일에 보훈청명, 병명, 상이구분 컬럼이 포함되어 있다"
    - "프론트엔드 Excel 다운로드 버튼이 Blob으로 파일을 받아 다운로드 트리거한다"
  artifacts:
    - path: "backend/src/main/java/com/navy/casualty/wounded/service/WoundedExcelService.java"
      provides: "SXSSFWorkbook Excel 생성 (보훈청/병명/상이구분 포함)"
      contains: "SXSSFWorkbook"
    - path: "frontend/src/api/wounded.ts"
      provides: "useExportWoundedExcel 훅"
      contains: "useExportWoundedExcel"
  key_links:
    - from: "WoundedController"
      to: "WoundedExcelService"
      via: "DI injection"
      pattern: "woundedExcelService"
    - from: "WoundedExcelService"
      to: "RrnMaskingUtil"
      via: "주민번호 마스킹"
      pattern: "RrnMaskingUtil\\.mask"
    - from: "WoundedExcelService"
      to: "VeteransOfficeRepository"
      via: "보훈청명 조회"
      pattern: "veteransOfficeRepository"
    - from: "WoundedListPage.tsx"
      to: "api/wounded.ts useExportWoundedExcel"
      via: "Excel 다운로드 버튼"
      pattern: "useExportWoundedExcel"
---

<objective>
상이자 Excel 내보내기 기능을 구현한다: SXSSFWorkbook 기반 WoundedExcelService, Controller 엔드포인트, 프론트엔드 다운로드 버튼. Phase 3 DeadExcelService 패턴을 복제하되 상이자 고유 컬럼(보훈청명, 병명, 상이구분)을 반영한다.

Purpose: WOND-05 요구사항. 검색 조건이 적용된 상이자 현황을 Excel로 내보내되, 주민번호는 역할별 마스킹 적용.
Output: GET /api/wounded/excel 엔드포인트 + 프론트엔드 다운로드 버튼
</objective>

<execution_context>
@C:\Users\User\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\User\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-wounded-management/04-01-SUMMARY.md

<interfaces>
<!-- 04-01에서 생성된 인터페이스 -->

From WoundedRepositoryCustom.java (04-01):
```java
public interface WoundedRepositoryCustom {
    Page<Wounded> search(WoundedSearchRequest request, Pageable pageable);
    List<Wounded> searchAll(WoundedSearchRequest request);
}
```

From WoundedController.java (04-01):
```java
@RestController @RequestMapping("/api/wounded")
// GET /excel 엔드포인트 추가 필요
```

From Wounded.java (04-01):
```java
// 필드: serviceNumber, name, ssnEncrypted, ssnHash, birthDate, rankId, branchId, unitId,
//       enlistmentDate, phone, address, veteransOfficeId, diseaseName, woundType, status
```

From DeadExcelService.java (참조 패턴):
```java
@Service @RequiredArgsConstructor @Transactional(readOnly = true)
public class DeadExcelService {
    // SXSSFWorkbook(100) 패턴
    // 코드 테이블 Map 캐시
    // RrnMaskingUtil.mask(ssn, role)
    // @AuditLog(action = "EXPORT", targetTable = "TB_DEAD")
}
```

From frontend/src/api/dead.ts (참조 패턴):
```typescript
export function useExportDeadExcel() {
    return useMutation({
        mutationFn: async (params) => {
            const res = await apiClient.get('/dead/excel', { params, responseType: 'blob' });
            // Blob -> 파일 다운로드 트리거
        },
    });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WoundedExcelService + Controller Excel 엔드포인트 + 테스트</name>
  <files>
    backend/src/main/java/com/navy/casualty/wounded/service/WoundedExcelService.java
    backend/src/main/java/com/navy/casualty/wounded/controller/WoundedController.java
    backend/src/test/java/com/navy/casualty/wounded/WoundedExcelServiceTest.java
  </files>
  <read_first>
    backend/src/main/java/com/navy/casualty/dead/service/DeadExcelService.java
    backend/src/main/java/com/navy/casualty/dead/controller/DeadController.java
    backend/src/main/java/com/navy/casualty/wounded/entity/Wounded.java
    backend/src/main/java/com/navy/casualty/wounded/entity/WoundType.java
    backend/src/main/java/com/navy/casualty/wounded/dto/WoundedSearchRequest.java
    backend/src/main/java/com/navy/casualty/wounded/repository/WoundedRepository.java
    backend/src/main/java/com/navy/casualty/wounded/repository/WoundedRepositoryCustom.java
    backend/src/main/java/com/navy/casualty/wounded/controller/WoundedController.java
    backend/src/main/java/com/navy/casualty/code/entity/VeteransOffice.java
    backend/src/main/java/com/navy/casualty/code/repository/VeteransOfficeRepository.java
    backend/src/main/java/com/navy/casualty/common/crypto/RrnMaskingUtil.java
    backend/src/main/java/com/navy/casualty/audit/annotation/AuditLog.java
    backend/src/test/java/com/navy/casualty/dead/DeadExcelServiceTest.java
    backend/build.gradle.kts
  </read_first>
  <action>
    1. WoundedExcelService.java 생성 (DeadExcelService 복제 + 차이점):
       ```java
       package com.navy.casualty.wounded.service;

       @Service @RequiredArgsConstructor @Transactional(readOnly = true)
       public class WoundedExcelService {
           private final WoundedRepository woundedRepository;
           private final RankCodeRepository rankCodeRepository;
           private final BranchCodeRepository branchCodeRepository;
           private final UnitCodeRepository unitCodeRepository;
           private final VeteransOfficeRepository veteransOfficeRepository;

           private static final String[] HEADERS = {
               "번호", "군구분", "군번", "성명", "주민등록번호", "생년월일",
               "계급", "소속", "입대일자", "전화번호", "주소",
               "보훈청명", "병명", "상이구분", "상태"
           };
           // Dead 대비: 사망구분/사망코드/사망일자 제거, 보훈청명/병명/상이구분 추가

           @AuditLog(action = "EXPORT", targetTable = "TB_WOUNDED")
           public void exportExcel(WoundedSearchRequest request, HttpServletResponse response) throws IOException {
               String userRole = getCurrentUserRole();
               List<Wounded> list = woundedRepository.searchAll(request);

               Map<Long, String> rankMap = rankCodeRepository.findAll().stream()
                   .collect(Collectors.toMap(RankCode::getId, RankCode::getRankName));
               Map<Long, String> branchMap = branchCodeRepository.findAll().stream()
                   .collect(Collectors.toMap(BranchCode::getId, BranchCode::getBranchName));
               Map<Long, String> unitMap = unitCodeRepository.findAll().stream()
                   .collect(Collectors.toMap(UnitCode::getId, UnitCode::getUnitName));
               Map<Long, String> officeMap = veteransOfficeRepository.findAll().stream()
                   .collect(Collectors.toMap(VeteransOffice::getId, VeteransOffice::getOfficeName));

               try (SXSSFWorkbook workbook = new SXSSFWorkbook(100)) {
                   Sheet sheet = workbook.createSheet("상이자 현황");
                   Row headerRow = sheet.createRow(0);
                   CellStyle headerStyle = createHeaderStyle(workbook);
                   for (int i = 0; i < HEADERS.length; i++) {
                       Cell cell = headerRow.createCell(i);
                       cell.setCellValue(HEADERS[i]);
                       cell.setCellStyle(headerStyle);
                   }

                   for (int i = 0; i < list.size(); i++) {
                       Wounded w = list.get(i);
                       Row row = sheet.createRow(i + 1);
                       row.createCell(0).setCellValue(i + 1);
                       row.createCell(1).setCellValue(w.getBranchId() != null ? branchMap.getOrDefault(w.getBranchId(), "") : "");
                       row.createCell(2).setCellValue(w.getServiceNumber());
                       row.createCell(3).setCellValue(w.getName());
                       row.createCell(4).setCellValue(RrnMaskingUtil.mask(w.getSsnEncrypted(), userRole));
                       row.createCell(5).setCellValue(w.getBirthDate() != null ? w.getBirthDate().toString() : "");
                       row.createCell(6).setCellValue(w.getRankId() != null ? rankMap.getOrDefault(w.getRankId(), "") : "");
                       row.createCell(7).setCellValue(w.getUnitId() != null ? unitMap.getOrDefault(w.getUnitId(), "") : "");
                       row.createCell(8).setCellValue(w.getEnlistmentDate() != null ? w.getEnlistmentDate().toString() : "");
                       row.createCell(9).setCellValue(w.getPhone() != null ? w.getPhone() : "");
                       row.createCell(10).setCellValue(w.getAddress() != null ? w.getAddress() : "");
                       row.createCell(11).setCellValue(w.getVeteransOfficeId() != null ? officeMap.getOrDefault(w.getVeteransOfficeId(), "") : "");
                       row.createCell(12).setCellValue(w.getDiseaseName() != null ? w.getDiseaseName() : "");
                       row.createCell(13).setCellValue(w.getWoundType() != null ? w.getWoundType().getLabel() : "");
                       row.createCell(14).setCellValue(w.getStatus().name());
                   }

                   response.setContentType("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet");
                   response.setHeader("Content-Disposition", "attachment; filename=wounded_list_" + LocalDate.now() + ".xlsx");
                   workbook.write(response.getOutputStream());
               }
           }

           private String getCurrentUserRole() { /* SecurityContextHolder 패턴 */ }
           private CellStyle createHeaderStyle(SXSSFWorkbook workbook) { /* DeadExcelService와 동일 */ }
       }
       ```

    2. WoundedController.java 수정 (GET /excel 엔드포인트 추가):
       - WoundedExcelService 의존성을 생성자 주입에 추가
       ```java
       @GetMapping("/excel")
       @PreAuthorize("hasRole('VIEWER')")
       public void exportExcel(WoundedSearchRequest request, HttpServletResponse response) throws IOException {
           woundedExcelService.exportExcel(request, response);
       }
       ```

    3. WoundedExcelServiceTest.java 생성 (DeadExcelServiceTest 참고):
       - @SpringBootTest @Transactional
       - MockHttpServletResponse 로 Content-Type, Content-Disposition 헤더 검증
       - SXSSFWorkbook 생성 + "상이자 현황" 시트명 확인
       - 헤더 행에 "보훈청명", "병명", "상이구분" 포함 검증
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "com.navy.casualty.wounded.WoundedExcelServiceTest" -x processTestAot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/main/java/com/navy/casualty/wounded/service/WoundedExcelService.java 존재
    - grep "SXSSFWorkbook" WoundedExcelService.java 결과 있음
    - grep "RrnMaskingUtil.mask" WoundedExcelService.java 결과 있음
    - grep "@AuditLog.*EXPORT.*TB_WOUNDED" WoundedExcelService.java 결과 있음
    - grep "veteransOfficeRepository" WoundedExcelService.java 결과 있음
    - grep "getLabel" WoundedExcelService.java 결과 있음 (WoundType.getLabel())
    - grep "상이자 현황" WoundedExcelService.java 결과 있음
    - grep "보훈청명" WoundedExcelService.java 결과 있음
    - grep "병명" WoundedExcelService.java 결과 있음
    - grep "상이구분" WoundedExcelService.java 결과 있음
    - grep "/excel" WoundedController.java 결과 있음
    - grep "woundedExcelService" WoundedController.java 결과 있음
    - WoundedExcelServiceTest.java 존재
  </acceptance_criteria>
  <done>SXSSFWorkbook 기반 상이자 Excel 내보내기 서비스 완성. 보훈청명/병명/상이구분 컬럼 포함, 주민번호 역할별 마스킹, 감사로그 EXPORT 기록.</done>
</task>

<task type="auto">
  <name>Task 2: 프론트엔드 Excel 다운로드 훅 + WoundedListPage 다운로드 버튼 연결</name>
  <files>
    frontend/src/api/wounded.ts
    frontend/src/pages/wounded/WoundedListPage.tsx
  </files>
  <read_first>
    frontend/src/api/wounded.ts
    frontend/src/pages/wounded/WoundedListPage.tsx
    frontend/src/api/dead.ts
    frontend/src/pages/dead/DeadListPage.tsx
    frontend/src/api/client.ts
    frontend/src/types/wounded.ts
  </read_first>
  <action>
    1. frontend/src/api/wounded.ts에 useExportWoundedExcel 훅 추가 (dead.ts의 useExportDeadExcel 복제):
       ```typescript
       export function useExportWoundedExcel() {
         return useMutation({
           mutationFn: async (params: WoundedSearchParams) => {
             const res = await apiClient.get('/wounded/excel', {
               params,
               responseType: 'blob',
             });
             const url = window.URL.createObjectURL(new Blob([res.data]));
             const link = document.createElement('a');
             link.href = url;
             link.download = `wounded_list_${new Date().toISOString().slice(0, 10)}.xlsx`;
             document.body.appendChild(link);
             link.click();
             document.body.removeChild(link);
             window.URL.revokeObjectURL(url);
           },
           onSuccess: () => message.success('Excel 다운로드가 완료되었습니다'),
           onError: () => message.error('Excel 다운로드에 실패했습니다'),
         });
       }
       ```

    2. frontend/src/pages/wounded/WoundedListPage.tsx 수정:
       - import { useExportWoundedExcel } from '../../api/wounded' 추가
       - const exportExcel = useExportWoundedExcel(); 선언
       - 기존 disabled placeholder 버튼을 실제 버튼으로 교체:
         ```tsx
         <Button
           icon={<DownloadOutlined />}
           onClick={() => exportExcel.mutate(searchParams)}
           loading={exportExcel.isPending}
         >
           Excel 다운로드
         </Button>
         ```
       - DownloadOutlined 아이콘이 이미 import되어 있는지 확인, 없으면 추가
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "useExportWoundedExcel" frontend/src/api/wounded.ts 결과 있음
    - grep "responseType.*blob" frontend/src/api/wounded.ts 결과 있음
    - grep "wounded_list_" frontend/src/api/wounded.ts 결과 있음
    - grep "useExportWoundedExcel" frontend/src/pages/wounded/WoundedListPage.tsx 결과 있음
    - grep "DownloadOutlined" frontend/src/pages/wounded/WoundedListPage.tsx 결과 있음
    - npx tsc --noEmit 에러 없음
  </acceptance_criteria>
  <done>프론트엔드 상이자 Excel 다운로드 기능 완성. Blob 응답 처리 + 파일 다운로드 트리거 + 로딩 상태 표시.</done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew test --tests "com.navy.casualty.wounded.*" -x processTestAot` 전체 통과
- `cd frontend && npx tsc --noEmit` 에러 없음
- WoundedExcelService에 SXSSFWorkbook + RrnMaskingUtil.mask + @AuditLog(EXPORT) + 보훈청/병명/상이구분
- WoundedListPage에 Excel 다운로드 버튼이 활성 상태
</verification>

<success_criteria>
- GET /api/wounded/excel 이 검색 조건 적용된 .xlsx 파일을 반환
- Excel에 보훈청명, 병명, 상이구분 컬럼 포함
- Excel에 주민번호 역할별 마스킹 적용
- 프론트엔드에서 Excel 다운로드 버튼 클릭 시 파일 다운로드
- 감사 로그에 EXPORT 액션 기록
</success_criteria>

<output>
After completion, create `.planning/phases/04-wounded-management/04-03-SUMMARY.md`
</output>
