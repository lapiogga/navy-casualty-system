---
phase: 03-dead-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/build.gradle.kts
  - backend/src/main/java/com/navy/casualty/dead/entity/Dead.java
  - backend/src/main/java/com/navy/casualty/dead/entity/DeadStatus.java
  - backend/src/main/java/com/navy/casualty/dead/dto/DeadCreateRequest.java
  - backend/src/main/java/com/navy/casualty/dead/dto/DeadUpdateRequest.java
  - backend/src/main/java/com/navy/casualty/dead/dto/DeadSearchRequest.java
  - backend/src/main/java/com/navy/casualty/dead/dto/DeadResponse.java
  - backend/src/main/java/com/navy/casualty/dead/dto/DeadDeleteRequest.java
  - backend/src/main/java/com/navy/casualty/dead/repository/DeadRepository.java
  - backend/src/main/java/com/navy/casualty/dead/repository/DeadRepositoryCustom.java
  - backend/src/main/java/com/navy/casualty/dead/repository/DeadRepositoryImpl.java
  - backend/src/main/java/com/navy/casualty/dead/service/DeadService.java
  - backend/src/main/java/com/navy/casualty/dead/controller/DeadController.java
  - backend/src/main/java/com/navy/casualty/code/entity/RankCode.java
  - backend/src/main/java/com/navy/casualty/code/entity/BranchCode.java
  - backend/src/main/java/com/navy/casualty/code/entity/DeathType.java
  - backend/src/main/java/com/navy/casualty/code/entity/DeathCode.java
  - backend/src/main/java/com/navy/casualty/code/entity/UnitCode.java
  - backend/src/main/java/com/navy/casualty/code/repository/RankCodeRepository.java
  - backend/src/main/java/com/navy/casualty/code/repository/BranchCodeRepository.java
  - backend/src/main/java/com/navy/casualty/code/repository/DeathTypeRepository.java
  - backend/src/main/java/com/navy/casualty/code/repository/DeathCodeRepository.java
  - backend/src/main/java/com/navy/casualty/code/repository/UnitCodeRepository.java
  - backend/src/main/java/com/navy/casualty/code/controller/CodeController.java
  - backend/src/test/java/com/navy/casualty/dead/DeadStatusTest.java
  - backend/src/test/java/com/navy/casualty/dead/DeadServiceTest.java
  - backend/src/test/java/com/navy/casualty/dead/DeadControllerTest.java
autonomous: true
requirements: [DEAD-01, DEAD-02, DEAD-03, DEAD-04, DEAD-06, DEAD-07]

must_haves:
  truths:
    - "GET /api/dead?name=홍길동&page=0&size=10 이 검색 조건에 맞는 사망자 목록을 페이징으로 반환한다"
    - "POST /api/dead 로 사망자를 등록하면 201과 DeadResponse를 반환한다"
    - "PUT /api/dead/{id} 로 사망자 정보를 수정할 수 있다"
    - "DELETE /api/dead/{id} 로 논리 삭제 시 삭제 사유가 필수이다"
    - "동일 군번으로 중복 등록 시 409 에러를 반환한다"
    - "상태 전이가 REGISTERED->CONFIRMED->NOTIFIED 순서로만 가능하다"
    - "코드 테이블 API(/api/codes/*)가 Select 드롭다운용 데이터를 반환한다"
  artifacts:
    - path: "backend/src/main/java/com/navy/casualty/dead/entity/Dead.java"
      provides: "사망자 JPA 엔티티"
      contains: "@Entity"
    - path: "backend/src/main/java/com/navy/casualty/dead/repository/DeadRepositoryImpl.java"
      provides: "QueryDSL 동적 검색"
      contains: "BooleanBuilder"
    - path: "backend/src/main/java/com/navy/casualty/dead/service/DeadService.java"
      provides: "비즈니스 로직 (CRUD + 중복검증 + 상태전이)"
      contains: "@AuditLog"
    - path: "backend/src/main/java/com/navy/casualty/dead/controller/DeadController.java"
      provides: "REST API 엔드포인트"
      contains: "@RestController"
    - path: "backend/src/main/java/com/navy/casualty/code/controller/CodeController.java"
      provides: "코드 테이블 조회 API"
      contains: "@GetMapping"
  key_links:
    - from: "DeadController"
      to: "DeadService"
      via: "DI injection"
      pattern: "private final DeadService"
    - from: "DeadService"
      to: "DeadRepositoryImpl"
      via: "QueryDSL search"
      pattern: "deadRepository\\.search"
    - from: "Dead.java"
      to: "BaseAuditEntity"
      via: "extends"
      pattern: "extends BaseAuditEntity"
    - from: "Dead.java"
      to: "PiiEncryptionConverter"
      via: "@Convert annotation"
      pattern: "@Convert.*PiiEncryptionConverter"
---

<objective>
사망자 백엔드 REST API 전체를 구현한다: Dead 엔티티, QueryDSL 동적 검색 Repository, 비즈니스 Service, REST Controller, 코드 테이블 Entity/API.

Purpose: Phase 3의 핵심 백엔드 인프라. 프론트엔드와 Excel 내보내기가 이 API에 의존한다.
Output: /api/dead CRUD 엔드포인트 + /api/codes/* 코드 조회 엔드포인트 + 테스트
</objective>

<execution_context>
@C:\Users\User\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\User\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-dead-management/03-RESEARCH.md

<interfaces>
<!-- 기존 인프라: 실행자가 참조해야 할 핵심 인터페이스 -->

From backend/src/main/java/com/navy/casualty/common/entity/BaseAuditEntity.java:
```java
@MappedSuperclass
public abstract class BaseAuditEntity {
    private LocalDateTime createdAt;
    private LocalDateTime updatedAt;
    private String createdBy;
    private String lastModifiedBy;
    private LocalDateTime deletedAt;
    private String deletedBy;
    private String deleteReason;
    public void softDelete(String deletedBy, String reason) { ... }
    public boolean isDeleted() { ... }
}
```

From backend/src/main/java/com/navy/casualty/common/dto/ApiResponse.java:
```java
public class ApiResponse<T> {
    private final boolean success;
    private final int status;
    private final String message;
    private final T data;
    public static <T> ApiResponse<T> ok(T data) { ... }
    public static <T> ApiResponse<T> error(int status, String message) { ... }
}
```

From backend/src/main/java/com/navy/casualty/common/crypto/PiiEncryptionConverter.java:
- Hibernate AttributeConverter, `@Convert(converter = PiiEncryptionConverter.class)` 로 엔티티 필드에 적용

From backend/src/main/java/com/navy/casualty/common/crypto/RrnMaskingUtil.java:
```java
public static String mask(String rrn, String role) { ... }
// ROLE_VIEWER -> "******-*******"
// ROLE_OPERATOR -> 앞 8자리 + "******"
// ROLE_MANAGER/ADMIN -> 전체 노출
```

From backend/src/main/java/com/navy/casualty/audit/annotation/AuditLog.java:
```java
@Target(ElementType.METHOD) @Retention(RetentionPolicy.RUNTIME)
public @interface AuditLog {
    String action();           // VIEW, CREATE, UPDATE, DELETE, PRINT, EXPORT
    String targetTable() default "";  // "TB_DEAD" 등
}
```

From backend/src/main/java/com/navy/casualty/common/config/QueryDslConfig.java:
- JPAQueryFactory 빈 등록 완료

From DB V3__create_dead_table.sql:
```sql
CREATE TABLE TB_DEAD (
    id BIGSERIAL PRIMARY KEY,
    service_number VARCHAR(20) NOT NULL UNIQUE,
    name VARCHAR(50) NOT NULL,
    ssn_encrypted TEXT NOT NULL,
    birth_date DATE NOT NULL,
    rank_id BIGINT REFERENCES TB_RANK_CODE(id),
    branch_id BIGINT REFERENCES TB_BRANCH_CODE(id),
    unit_id BIGINT REFERENCES TB_UNIT_CODE(id),
    enlistment_date DATE,
    phone VARCHAR(20),
    death_type_id BIGINT REFERENCES TB_DEATH_TYPE(id),
    death_code_id BIGINT REFERENCES TB_DEATH_CODE(id),
    address TEXT,
    death_date DATE NOT NULL,
    status VARCHAR(20) NOT NULL DEFAULT 'REGISTERED',
    -- BaseAuditEntity 컬럼들 포함
);
```

From DB V2__create_code_tables.sql:
- TB_RANK_CODE(id, rank_name, rank_group, sort_order)
- TB_BRANCH_CODE(id, branch_name)
- TB_DEATH_TYPE(id, type_name)
- TB_DEATH_CODE(id, code_symbol, code_name)
- TB_UNIT_CODE(id, unit_name, parent_id)
- TB_VETERANS_OFFICE(id, office_name, office_type)

From backend/build.gradle.kts:
- QueryDSL 5.1.0:jakarta 이미 포함
- Apache POI 미포함 (Plan 03에서 추가)

From SecurityConfig.java:
- @EnableMethodSecurity 활성화됨
- 역할 계층: ADMIN > MANAGER > OPERATOR > VIEWER
- /api/** 는 authenticated 필요
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 코드 테이블 엔티티 + Dead 엔티티/DTO + DeadStatus + QueryDSL Repository + DeadService + 단위 테스트</name>
  <files>
    backend/src/main/java/com/navy/casualty/code/entity/RankCode.java
    backend/src/main/java/com/navy/casualty/code/entity/BranchCode.java
    backend/src/main/java/com/navy/casualty/code/entity/DeathType.java
    backend/src/main/java/com/navy/casualty/code/entity/DeathCode.java
    backend/src/main/java/com/navy/casualty/code/entity/UnitCode.java
    backend/src/main/java/com/navy/casualty/code/repository/RankCodeRepository.java
    backend/src/main/java/com/navy/casualty/code/repository/BranchCodeRepository.java
    backend/src/main/java/com/navy/casualty/code/repository/DeathTypeRepository.java
    backend/src/main/java/com/navy/casualty/code/repository/DeathCodeRepository.java
    backend/src/main/java/com/navy/casualty/code/repository/UnitCodeRepository.java
    backend/src/main/java/com/navy/casualty/dead/entity/Dead.java
    backend/src/main/java/com/navy/casualty/dead/entity/DeadStatus.java
    backend/src/main/java/com/navy/casualty/dead/dto/DeadCreateRequest.java
    backend/src/main/java/com/navy/casualty/dead/dto/DeadUpdateRequest.java
    backend/src/main/java/com/navy/casualty/dead/dto/DeadSearchRequest.java
    backend/src/main/java/com/navy/casualty/dead/dto/DeadResponse.java
    backend/src/main/java/com/navy/casualty/dead/dto/DeadDeleteRequest.java
    backend/src/main/java/com/navy/casualty/dead/repository/DeadRepository.java
    backend/src/main/java/com/navy/casualty/dead/repository/DeadRepositoryCustom.java
    backend/src/main/java/com/navy/casualty/dead/repository/DeadRepositoryImpl.java
    backend/src/main/java/com/navy/casualty/dead/service/DeadService.java
    backend/src/test/java/com/navy/casualty/dead/DeadStatusTest.java
    backend/src/test/java/com/navy/casualty/dead/DeadServiceTest.java
  </files>
  <read_first>
    backend/src/main/java/com/navy/casualty/common/entity/BaseAuditEntity.java
    backend/src/main/java/com/navy/casualty/common/dto/ApiResponse.java
    backend/src/main/java/com/navy/casualty/common/crypto/PiiEncryptionConverter.java
    backend/src/main/java/com/navy/casualty/common/crypto/RrnMaskingUtil.java
    backend/src/main/java/com/navy/casualty/audit/annotation/AuditLog.java
    backend/src/main/java/com/navy/casualty/common/config/QueryDslConfig.java
    backend/src/main/java/com/navy/casualty/user/entity/User.java
    backend/src/main/java/com/navy/casualty/user/dto/UserResponse.java
    backend/src/main/java/com/navy/casualty/user/service/UserService.java
    backend/src/main/resources/db/migration/V2__create_code_tables.sql
    backend/src/main/resources/db/migration/V3__create_dead_table.sql
    backend/build.gradle.kts
  </read_first>
  <behavior>
    - DeadStatus.canTransitionTo: REGISTERED->CONFIRMED true, CONFIRMED->NOTIFIED true, REGISTERED->NOTIFIED false, NOTIFIED->* false
    - DeadService.create: 군번 중복 시 IllegalArgumentException("이미 등록된 군번입니다")
    - DeadService.create: 정상 등록 시 status=REGISTERED, DeadResponse 반환
    - DeadService.update: 존재하지 않는 id 시 IllegalArgumentException("사망자를 찾을 수 없습니다")
    - DeadService.softDelete: 삭제 후 isDeleted()=true
    - DeadService.updateStatus: 허용되지 않은 전이 시 IllegalStateException
  </behavior>
  <action>
    1. 코드 테이블 JPA 엔티티 5개 생성 (Phase 2 User 엔티티 패턴 참조):
       - RankCode: @Entity @Table(name="TB_RANK_CODE"), 필드: id(Long), rankName(String), rankGroup(String), sortOrder(Integer), extends BaseAuditEntity, @SQLRestriction("deleted_at IS NULL")
       - BranchCode: @Entity @Table(name="TB_BRANCH_CODE"), 필드: id(Long), branchName(String)
       - DeathType: @Entity @Table(name="TB_DEATH_TYPE"), 필드: id(Long), typeName(String)
       - DeathCode: @Entity @Table(name="TB_DEATH_CODE"), 필드: id(Long), codeSymbol(String), codeName(String)
       - UnitCode: @Entity @Table(name="TB_UNIT_CODE"), 필드: id(Long), unitName(String), parentId(Long)
       - 모든 코드 엔티티는 extends BaseAuditEntity, @SQLRestriction("deleted_at IS NULL"), @Getter, @NoArgsConstructor(access=PROTECTED)
    
    2. 코드 테이블 JpaRepository 5개 생성 (인터페이스만):
       - RankCodeRepository extends JpaRepository<RankCode, Long> { List<RankCode> findAllByOrderBySortOrderAsc(); }
       - BranchCodeRepository extends JpaRepository<BranchCode, Long>
       - DeathTypeRepository extends JpaRepository<DeathType, Long>
       - DeathCodeRepository extends JpaRepository<DeathCode, Long>
       - UnitCodeRepository extends JpaRepository<UnitCode, Long> { List<UnitCode> findByParentIdIsNullOrderByUnitNameAsc(); }
    
    3. DeadStatus enum 생성:
       ```java
       public enum DeadStatus {
           REGISTERED, CONFIRMED, NOTIFIED;
           public boolean canTransitionTo(DeadStatus next) {
               return switch (this) {
                   case REGISTERED -> next == CONFIRMED;
                   case CONFIRMED -> next == NOTIFIED;
                   case NOTIFIED -> false;
               };
           }
       }
       ```
    
    4. Dead 엔티티 생성 (03-RESEARCH.md 코드 예시 그대로):
       - extends BaseAuditEntity
       - @Entity @Table(name="TB_DEAD") @SQLRestriction("deleted_at IS NULL")
       - @Convert(converter = PiiEncryptionConverter.class) 적용: ssnEncrypted 필드
       - @Enumerated(EnumType.STRING) @Builder.Default status = DeadStatus.REGISTERED
       - updateStatus(DeadStatus) 메서드: canTransitionTo 검증
       - update(DeadUpdateRequest) 메서드: 필드별 setter 역할 (this.name = request.name() 등)
    
    5. record DTO 5개 생성 (Phase 2 record DTO 패턴 참조):
       - DeadCreateRequest: @NotBlank serviceNumber, @NotBlank name, @NotBlank ssn(주민번호 평문), @NotNull birthDate(LocalDate), rankId(Long), branchId(Long), unitId(Long), enlistmentDate(LocalDate), phone(String), deathTypeId(Long), deathCodeId(Long), address(String), @NotNull deathDate(LocalDate)
       - DeadUpdateRequest: 동일 필드 (serviceNumber 제외 -- 군번은 수정 불가)
       - DeadSearchRequest: branchId(Long), serviceNumber(String), name(String), birthDate(LocalDate), rankId(Long), unitId(Long), deathTypeId(Long), status(DeadStatus) -- 모두 nullable
       - DeadResponse: id, serviceNumber, name, ssnMasked(String), birthDate, rankName(String), branchName(String), unitName(String), enlistmentDate, phone, deathTypeName(String), deathCodeSymbol(String), address, deathDate, status(String), createdAt(LocalDateTime)
         - static DeadResponse from(Dead dead, String maskedSsn, String rankName, String branchName, String unitName, String deathTypeName, String deathCodeSymbol) 팩토리 메서드
       - DeadDeleteRequest: @NotBlank reason(String)
    
    6. Repository 생성:
       - DeadRepository extends JpaRepository<Dead, Long>, DeadRepositoryCustom { boolean existsByServiceNumber(String serviceNumber); }
       - DeadRepositoryCustom: Page<Dead> search(DeadSearchRequest request, Pageable pageable);
       - DeadRepositoryImpl implements DeadRepositoryCustom:
         - @RequiredArgsConstructor, private final JPAQueryFactory queryFactory
         - search() 메서드: QDead 사용, BooleanBuilder로 8개 조건(branchId, serviceNumber contains, name contains, birthDate eq, rankId eq, unitId eq, deathTypeId eq, status eq) 동적 조합
         - Pageable offset/limit + orderBy(dead.id.desc()) + total count
    
    7. DeadService 생성 (@Service @RequiredArgsConstructor @Transactional(readOnly=true)):
       - 의존성: DeadRepository, RankCodeRepository, BranchCodeRepository, DeathTypeRepository, DeathCodeRepository, UnitCodeRepository
       - search(DeadSearchRequest, Pageable): Page<DeadResponse> 반환. SecurityContextHolder에서 역할 추출하여 RrnMaskingUtil.mask() 적용. 코드 테이블에서 이름 조회 (findById().map().orElse(""))
       - @AuditLog(action="CREATE", targetTable="TB_DEAD") @Transactional create(DeadCreateRequest): 군번 중복 검증(existsByServiceNumber), Dead.builder()로 생성, save 후 DeadResponse.from() 반환
       - @AuditLog(action="UPDATE", targetTable="TB_DEAD") @Transactional update(Long id, DeadUpdateRequest): findById 후 dead.update(request), DeadResponse.from() 반환
       - @AuditLog(action="DELETE", targetTable="TB_DEAD") @Transactional softDelete(Long id, String reason): findById 후 dead.softDelete(SecurityUtils에서 사용자명, reason)
       - @AuditLog(action="UPDATE", targetTable="TB_DEAD") @Transactional updateStatus(Long id, DeadStatus newStatus): findById 후 dead.updateStatus(newStatus)
       - private String getCurrentUserRole(): SecurityContextHolder.getContext().getAuthentication().getAuthorities() 에서 ROLE_XXX 추출
       - private DeadResponse toResponse(Dead dead): 코드 테이블 조회 + 마스킹 적용하여 DeadResponse.from() 호출
    
    8. 엔티티 작성 후 `./gradlew compileJava` 실행하여 QDead 클래스 생성 확인.
    
    9. 테스트 작성:
       - DeadStatusTest: canTransitionTo 모든 경우 검증 (6 케이스)
       - DeadServiceTest (@SpringBootTest, @Transactional, H2):
         - create 성공 케이스
         - create 군번 중복 시 IllegalArgumentException
         - softDelete 후 isDeleted 확인
         - updateStatus 허용되지 않은 전이 시 IllegalStateException
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "com.navy.casualty.dead.DeadStatusTest" --tests "com.navy.casualty.dead.DeadServiceTest" -x processTestAot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "@Entity" backend/src/main/java/com/navy/casualty/dead/entity/Dead.java 에 결과 있음
    - grep -r "BooleanBuilder" backend/src/main/java/com/navy/casualty/dead/repository/DeadRepositoryImpl.java 에 결과 있음
    - grep -r "@AuditLog" backend/src/main/java/com/navy/casualty/dead/service/DeadService.java 에 결과 있음 (최소 3개: CREATE, UPDATE, DELETE)
    - grep -r "existsByServiceNumber" backend/src/main/java/com/navy/casualty/dead/repository/DeadRepository.java 에 결과 있음
    - grep -r "canTransitionTo" backend/src/main/java/com/navy/casualty/dead/entity/DeadStatus.java 에 결과 있음
    - grep -r "PiiEncryptionConverter" backend/src/main/java/com/navy/casualty/dead/entity/Dead.java 에 결과 있음
    - 코드 테이블 엔티티 5개 파일 존재: RankCode.java, BranchCode.java, DeathType.java, DeathCode.java, UnitCode.java
    - 코드 테이블 리포지토리 5개 파일 존재
    - DeadStatusTest, DeadServiceTest 파일 존재
  </acceptance_criteria>
  <done>Dead 엔티티 + 코드 테이블 엔티티 + QueryDSL Repository + DeadService 완성. 상태 전이, 중복 검증, 논리 삭제 모두 구현. 단위 테스트 통과.</done>
</task>

<task type="auto">
  <name>Task 2: DeadController REST API + CodeController 코드 조회 API + 통합 테스트</name>
  <files>
    backend/src/main/java/com/navy/casualty/dead/controller/DeadController.java
    backend/src/main/java/com/navy/casualty/code/controller/CodeController.java
    backend/src/test/java/com/navy/casualty/dead/DeadControllerTest.java
  </files>
  <read_first>
    backend/src/main/java/com/navy/casualty/dead/service/DeadService.java
    backend/src/main/java/com/navy/casualty/dead/dto/DeadCreateRequest.java
    backend/src/main/java/com/navy/casualty/dead/dto/DeadUpdateRequest.java
    backend/src/main/java/com/navy/casualty/dead/dto/DeadSearchRequest.java
    backend/src/main/java/com/navy/casualty/dead/dto/DeadResponse.java
    backend/src/main/java/com/navy/casualty/dead/dto/DeadDeleteRequest.java
    backend/src/main/java/com/navy/casualty/user/controller/UserController.java
    backend/src/main/java/com/navy/casualty/audit/controller/AuditLogController.java
    backend/src/test/java/com/navy/casualty/user/UserControllerTest.java
    backend/src/main/java/com/navy/casualty/security/SecurityConfig.java
  </read_first>
  <action>
    1. DeadController 생성 (03-RESEARCH.md 코드 예시 참조, Phase 2 UserController 패턴):
       - @RestController @RequestMapping("/api/dead") @RequiredArgsConstructor
       - GET / : @PreAuthorize("hasRole('VIEWER')"), DeadSearchRequest + Pageable 파라미터, ResponseEntity<ApiResponse<Page<DeadResponse>>> 반환
       - POST / : @PreAuthorize("hasRole('OPERATOR')"), @Valid @RequestBody DeadCreateRequest, 201 반환
       - PUT /{id} : @PreAuthorize("hasRole('OPERATOR')"), @Valid @RequestBody DeadUpdateRequest
       - DELETE /{id} : @PreAuthorize("hasRole('MANAGER')"), @Valid @RequestBody DeadDeleteRequest
       - PUT /{id}/status : @PreAuthorize("hasRole('MANAGER')"), @RequestParam DeadStatus status
    
    2. CodeController 생성:
       - @RestController @RequestMapping("/api/codes") @RequiredArgsConstructor
       - @PreAuthorize("hasRole('VIEWER')") 클래스 레벨
       - GET /ranks : ApiResponse<List<RankCode>> 반환 (findAllByOrderBySortOrderAsc)
       - GET /branches : ApiResponse<List<BranchCode>> 반환 (findAll)
       - GET /death-types : ApiResponse<List<DeathType>> 반환 (findAll)
       - GET /death-codes : ApiResponse<List<DeathCode>> 반환 (findAll)
       - GET /units : ApiResponse<List<UnitCode>> 반환 (findAll)
    
    3. DeadControllerTest 생성 (@SpringBootTest @AutoConfigureMockMvc, Phase 2 UserControllerTest 패턴):
       - @WithMockUser(roles="OPERATOR"): POST /api/dead 등록 성공 201
       - @WithMockUser(roles="OPERATOR"): POST /api/dead 군번 중복 시 409
       - @WithMockUser(roles="VIEWER"): GET /api/dead 검색 200
       - @WithMockUser(roles="VIEWER"): POST /api/dead 시 403 (권한 부족)
       - @WithMockUser(roles="MANAGER"): DELETE /api/dead/{id} 삭제 사유 포함 200
       - @WithMockUser(roles="MANAGER"): PUT /api/dead/{id}/status 상태 변경 200
    
    4. GlobalExceptionHandler에 IllegalArgumentException -> 409 CONFLICT 매핑 추가 (이미 400 매핑이 있다면 확인 후 추가):
       - 군번 중복 등 비즈니스 검증 실패를 409로 반환하기 위함
       - backend/src/main/java/com/navy/casualty/common/exception/GlobalExceptionHandler.java 수정
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "com.navy.casualty.dead.DeadControllerTest" -x processTestAot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "@RestController" backend/src/main/java/com/navy/casualty/dead/controller/DeadController.java 에 결과 있음
    - grep -r "@RequestMapping.*\/api\/dead" backend/src/main/java/com/navy/casualty/dead/controller/DeadController.java 에 결과 있음
    - grep -r "@PreAuthorize" backend/src/main/java/com/navy/casualty/dead/controller/DeadController.java 에 최소 5개 엔드포인트
    - grep -r "@RestController" backend/src/main/java/com/navy/casualty/code/controller/CodeController.java 에 결과 있음
    - grep -r "\/api\/codes" backend/src/main/java/com/navy/casualty/code/controller/CodeController.java 에 결과 있음
    - grep -r "@GetMapping" backend/src/main/java/com/navy/casualty/code/controller/CodeController.java 에 최소 5개
    - DeadControllerTest.java 파일 존재, @SpringBootTest 포함
  </acceptance_criteria>
  <done>DeadController 6개 엔드포인트 + CodeController 5개 엔드포인트 완성. @PreAuthorize 권한 가드 적용. 통합 테스트 통과.</done>
</task>

</tasks>

<verification>
- `./gradlew test --tests "com.navy.casualty.dead.*" --tests "com.navy.casualty.code.*" -x processTestAot` 전체 통과
- `./gradlew compileJava` 성공 (QDead 생성 포함)
- 코드 테이블 엔티티 5개, 리포지토리 5개 존재
- Dead 엔티티에 @Convert(PiiEncryptionConverter), @SQLRestriction 적용
- DeadService에 @AuditLog 어노테이션 4개 이상
</verification>

<success_criteria>
- GET /api/dead 복합 검색 + 서버사이드 페이징 동작
- POST /api/dead 등록 시 군번 중복 검증 + 주민번호 암호화 저장
- PUT /api/dead/{id} 수정, DELETE /api/dead/{id} 논리 삭제 (사유 필수)
- PUT /api/dead/{id}/status 상태 전이 (REGISTERED->CONFIRMED->NOTIFIED)
- GET /api/codes/* 5개 코드 테이블 조회
- 모든 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-dead-management/03-01-SUMMARY.md`
</output>
