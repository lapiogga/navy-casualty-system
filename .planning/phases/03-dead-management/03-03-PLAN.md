---
phase: 03-dead-management
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - backend/build.gradle.kts
  - backend/src/main/java/com/navy/casualty/dead/service/DeadExcelService.java
  - backend/src/main/java/com/navy/casualty/dead/controller/DeadController.java
  - backend/src/main/java/com/navy/casualty/dead/repository/DeadRepositoryCustom.java
  - backend/src/main/java/com/navy/casualty/dead/repository/DeadRepositoryImpl.java
  - backend/src/test/java/com/navy/casualty/dead/DeadExcelServiceTest.java
  - frontend/src/api/dead.ts
  - frontend/src/pages/dead/DeadListPage.tsx
autonomous: true
requirements: [DEAD-05]

must_haves:
  truths:
    - "GET /api/dead/excel 이 검색 조건 적용된 Excel 파일을 다운로드한다"
    - "Excel 파일에서 주민번호가 역할별로 마스킹되어 있다"
    - "프론트엔드 Excel 다운로드 버튼이 Blob으로 파일을 받아 다운로드 트리거한다"
  artifacts:
    - path: "backend/src/main/java/com/navy/casualty/dead/service/DeadExcelService.java"
      provides: "SXSSFWorkbook Excel 생성"
      contains: "SXSSFWorkbook"
    - path: "backend/build.gradle.kts"
      provides: "Apache POI 의존성"
      contains: "poi-ooxml"
  key_links:
    - from: "DeadController"
      to: "DeadExcelService"
      via: "DI injection"
      pattern: "deadExcelService"
    - from: "DeadExcelService"
      to: "RrnMaskingUtil"
      via: "주민번호 마스킹"
      pattern: "RrnMaskingUtil\\.mask"
    - from: "DeadListPage.tsx"
      to: "api/dead.ts useExportDeadExcel"
      via: "Excel 다운로드 버튼"
      pattern: "useExportDeadExcel"
---

<objective>
사망자 Excel 내보내기 기능을 구현한다: Apache POI 의존성 추가, SXSSFWorkbook 기반 DeadExcelService, Controller 엔드포인트, 프론트엔드 다운로드 버튼.

Purpose: DEAD-05 요구사항. 검색 조건이 적용된 사망자 현황을 Excel로 내보내되, 주민번호는 역할별 마스킹 적용.
Output: GET /api/dead/excel 엔드포인트 + 프론트엔드 다운로드 버튼
</objective>

<execution_context>
@C:\Users\User\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\User\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dead-management/03-RESEARCH.md
@.planning/phases/03-dead-management/03-01-SUMMARY.md
@.planning/phases/03-dead-management/03-02-SUMMARY.md

<interfaces>
<!-- 03-01에서 생성된 인터페이스 -->

From DeadRepositoryCustom.java (03-01):
```java
public interface DeadRepositoryCustom {
    Page<Dead> search(DeadSearchRequest request, Pageable pageable);
    // searchAll 메서드 추가 필요: List<Dead> searchAll(DeadSearchRequest request);
}
```

From DeadController.java (03-01):
```java
@RestController @RequestMapping("/api/dead")
// GET /excel 엔드포인트 추가 필요
```

From RrnMaskingUtil.java:
```java
public static String mask(String rrn, String role) { ... }
```

From SecurityContextHolder 패턴:
```java
SecurityContextHolder.getContext().getAuthentication().getAuthorities()
// -> Collection<GrantedAuthority> 에서 ROLE_XXX 추출
```

From 03-RESEARCH.md DeadExcelService 패턴:
- SXSSFWorkbook(100) 메모리 윈도우
- response.setContentType("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
- response.setHeader("Content-Disposition", "attachment; filename=dead_list_날짜.xlsx")

From frontend/src/api/dead.ts (03-02):
```typescript
// useExportDeadExcel 훅 추가 필요
export const deadKeys = { ... };
```

From frontend/src/pages/dead/DeadListPage.tsx (03-02):
// Excel 다운로드 버튼 placeholder -> 실제 기능 연결
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apache POI 의존성 + DeadExcelService + searchAll Repository 메서드 + Controller 엔드포인트 + 테스트</name>
  <files>
    backend/build.gradle.kts
    backend/src/main/java/com/navy/casualty/dead/service/DeadExcelService.java
    backend/src/main/java/com/navy/casualty/dead/repository/DeadRepositoryCustom.java
    backend/src/main/java/com/navy/casualty/dead/repository/DeadRepositoryImpl.java
    backend/src/main/java/com/navy/casualty/dead/controller/DeadController.java
    backend/src/test/java/com/navy/casualty/dead/DeadExcelServiceTest.java
  </files>
  <read_first>
    backend/build.gradle.kts
    backend/src/main/java/com/navy/casualty/dead/service/DeadService.java
    backend/src/main/java/com/navy/casualty/dead/repository/DeadRepositoryCustom.java
    backend/src/main/java/com/navy/casualty/dead/repository/DeadRepositoryImpl.java
    backend/src/main/java/com/navy/casualty/dead/controller/DeadController.java
    backend/src/main/java/com/navy/casualty/dead/entity/Dead.java
    backend/src/main/java/com/navy/casualty/dead/dto/DeadSearchRequest.java
    backend/src/main/java/com/navy/casualty/common/crypto/RrnMaskingUtil.java
    backend/src/main/java/com/navy/casualty/audit/annotation/AuditLog.java
    backend/src/main/java/com/navy/casualty/code/repository/RankCodeRepository.java
    backend/src/main/java/com/navy/casualty/code/repository/BranchCodeRepository.java
  </read_first>
  <action>
    1. backend/build.gradle.kts에 Apache POI 의존성 추가:
       - dependencies 블록에 `implementation("org.apache.poi:poi-ooxml:5.3.0")` 추가
       - 기존 의존성 위치 참고하여 implementation 그룹에 삽입
    
    2. DeadRepositoryCustom에 searchAll 메서드 추가:
       ```java
       List<Dead> searchAll(DeadSearchRequest request);
       ```
    
    3. DeadRepositoryImpl에 searchAll 구현 추가:
       - 기존 search()의 BooleanBuilder 로직을 재사용 (private 메서드로 추출)
       - 페이징 없이 전체 결과 반환
       - orderBy(dead.id.desc())
    
    4. DeadExcelService 생성:
       ```java
       @Service
       @RequiredArgsConstructor
       public class DeadExcelService {
           private final DeadRepository deadRepository;
           private final RankCodeRepository rankCodeRepository;
           private final BranchCodeRepository branchCodeRepository;
           private final DeathTypeRepository deathTypeRepository;
           private final DeathCodeRepository deathCodeRepository;
           private final UnitCodeRepository unitCodeRepository;
           
           @AuditLog(action = "EXPORT", targetTable = "TB_DEAD")
           public void exportExcel(DeadSearchRequest request, HttpServletResponse response) throws IOException {
               String userRole = getCurrentUserRole(); // SecurityContextHolder에서 추출
               List<Dead> list = deadRepository.searchAll(request);
               
               try (SXSSFWorkbook workbook = new SXSSFWorkbook(100)) {
                   Sheet sheet = workbook.createSheet("사망자 현황");
                   
                   // 헤더 행: 번호, 군구분, 군번, 성명, 주민등록번호, 생년월일, 계급, 소속, 입대일자, 전화번호, 사망구분, 사망코드, 주소, 사망일자, 상태
                   String[] headers = {"번호", "군구분", "군번", "성명", "주민등록번호", "생년월일", "계급", "소속", "입대일자", "전화번호", "사망구분", "사망코드", "주소", "사망일자", "상태"};
                   Row headerRow = sheet.createRow(0);
                   CellStyle headerStyle = createHeaderStyle(workbook);
                   for (int i = 0; i < headers.length; i++) {
                       Cell cell = headerRow.createCell(i);
                       cell.setCellValue(headers[i]);
                       cell.setCellStyle(headerStyle);
                   }
                   
                   // 데이터 행: 코드 테이블 조회 캐시 (Map으로 한 번에 로드)
                   Map<Long, String> rankMap = rankCodeRepository.findAll().stream().collect(Collectors.toMap(RankCode::getId, RankCode::getRankName));
                   Map<Long, String> branchMap = branchCodeRepository.findAll().stream().collect(Collectors.toMap(BranchCode::getId, BranchCode::getBranchName));
                   Map<Long, String> deathTypeMap = deathTypeRepository.findAll().stream().collect(Collectors.toMap(DeathType::getId, DeathType::getTypeName));
                   Map<Long, String> deathCodeMap = deathCodeRepository.findAll().stream().collect(Collectors.toMap(DeathCode::getId, DeathCode::getCodeSymbol));
                   Map<Long, String> unitMap = unitCodeRepository.findAll().stream().collect(Collectors.toMap(UnitCode::getId, UnitCode::getUnitName));
                   
                   for (int i = 0; i < list.size(); i++) {
                       Dead dead = list.get(i);
                       Row row = sheet.createRow(i + 1);
                       row.createCell(0).setCellValue(i + 1);
                       row.createCell(1).setCellValue(dead.getBranchId() != null ? branchMap.getOrDefault(dead.getBranchId(), "") : "");
                       row.createCell(2).setCellValue(dead.getServiceNumber());
                       row.createCell(3).setCellValue(dead.getName());
                       row.createCell(4).setCellValue(RrnMaskingUtil.mask(dead.getSsnEncrypted(), userRole)); // 역할별 마스킹
                       row.createCell(5).setCellValue(dead.getBirthDate() != null ? dead.getBirthDate().toString() : "");
                       row.createCell(6).setCellValue(dead.getRankId() != null ? rankMap.getOrDefault(dead.getRankId(), "") : "");
                       row.createCell(7).setCellValue(dead.getUnitId() != null ? unitMap.getOrDefault(dead.getUnitId(), "") : "");
                       row.createCell(8).setCellValue(dead.getEnlistmentDate() != null ? dead.getEnlistmentDate().toString() : "");
                       row.createCell(9).setCellValue(dead.getPhone() != null ? dead.getPhone() : "");
                       row.createCell(10).setCellValue(dead.getDeathTypeId() != null ? deathTypeMap.getOrDefault(dead.getDeathTypeId(), "") : "");
                       row.createCell(11).setCellValue(dead.getDeathCodeId() != null ? deathCodeMap.getOrDefault(dead.getDeathCodeId(), "") : "");
                       row.createCell(12).setCellValue(dead.getAddress() != null ? dead.getAddress() : "");
                       row.createCell(13).setCellValue(dead.getDeathDate() != null ? dead.getDeathDate().toString() : "");
                       row.createCell(14).setCellValue(dead.getStatus().name());
                   }
                   
                   response.setContentType("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet");
                   response.setHeader("Content-Disposition", "attachment; filename=dead_list_" + LocalDate.now() + ".xlsx");
                   workbook.write(response.getOutputStream());
               }
           }
           
           private String getCurrentUserRole() {
               return SecurityContextHolder.getContext().getAuthentication()
                       .getAuthorities().stream().findFirst()
                       .map(GrantedAuthority::getAuthority).orElse("ROLE_VIEWER");
           }
           
           private CellStyle createHeaderStyle(SXSSFWorkbook workbook) {
               CellStyle style = workbook.createCellStyle();
               Font font = workbook.createFont();
               font.setBold(true);
               style.setFont(font);
               style.setFillForegroundColor(IndexedColors.GREY_25_PERCENT.getIndex());
               style.setFillPattern(FillPatternType.SOLID_FOREGROUND);
               return style;
           }
       }
       ```
    
    5. DeadController에 GET /excel 엔드포인트 추가:
       ```java
       @GetMapping("/excel")
       @PreAuthorize("hasRole('VIEWER')")
       public void exportExcel(DeadSearchRequest request, HttpServletResponse response) throws IOException {
           deadExcelService.exportExcel(request, response);
       }
       ```
       - DeadExcelService를 생성자 주입에 추가
    
    6. DeadExcelServiceTest 생성 (@SpringBootTest):
       - 테스트: SXSSFWorkbook 생성 + 헤더 행 검증
       - MockHttpServletResponse로 출력 검증: Content-Type, Content-Disposition 헤더 확인
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "com.navy.casualty.dead.DeadExcelServiceTest" -x processTestAot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "poi-ooxml" backend/build.gradle.kts 에 결과 있음
    - grep "SXSSFWorkbook" backend/src/main/java/com/navy/casualty/dead/service/DeadExcelService.java 에 결과 있음
    - grep "RrnMaskingUtil.mask" backend/src/main/java/com/navy/casualty/dead/service/DeadExcelService.java 에 결과 있음
    - grep "@AuditLog.*EXPORT" backend/src/main/java/com/navy/casualty/dead/service/DeadExcelService.java 에 결과 있음
    - grep "searchAll" backend/src/main/java/com/navy/casualty/dead/repository/DeadRepositoryCustom.java 에 결과 있음
    - grep "/excel" backend/src/main/java/com/navy/casualty/dead/controller/DeadController.java 에 결과 있음
    - DeadExcelServiceTest.java 파일 존재
  </acceptance_criteria>
  <done>Apache POI 의존성 추가, SXSSFWorkbook 기반 Excel 내보내기 서비스 완성, 주민번호 역할별 마스킹 적용, 감사 로그 EXPORT 기록. 테스트 통과.</done>
</task>

<task type="auto">
  <name>Task 2: 프론트엔드 Excel 다운로드 훅 + DeadListPage 다운로드 버튼 연결</name>
  <files>
    frontend/src/api/dead.ts
    frontend/src/pages/dead/DeadListPage.tsx
  </files>
  <read_first>
    frontend/src/api/dead.ts
    frontend/src/pages/dead/DeadListPage.tsx
    frontend/src/api/client.ts
    frontend/src/types/dead.ts
  </read_first>
  <action>
    1. frontend/src/api/dead.ts에 useExportDeadExcel 훅 추가:
       ```typescript
       export function useExportDeadExcel() {
         return useMutation({
           mutationFn: async (params: DeadSearchParams) => {
             const res = await apiClient.get('/dead/excel', {
               params,
               responseType: 'blob',
             });
             const url = window.URL.createObjectURL(new Blob([res.data]));
             const link = document.createElement('a');
             link.href = url;
             link.download = `dead_list_${new Date().toISOString().slice(0, 10)}.xlsx`;
             document.body.appendChild(link);
             link.click();
             document.body.removeChild(link);
             window.URL.revokeObjectURL(url);
           },
           onSuccess: () => message.success('Excel 다운로드가 완료되었습니다'),
           onError: () => message.error('Excel 다운로드에 실패했습니다'),
         });
       }
       ```
    
    2. frontend/src/pages/dead/DeadListPage.tsx 수정:
       - import { useExportDeadExcel } from '../../api/dead' 추가
       - const exportExcel = useExportDeadExcel(); 선언
       - 기존 disabled placeholder 버튼을 실제 버튼으로 교체:
         ```tsx
         <Button
           icon={<DownloadOutlined />}
           onClick={() => exportExcel.mutate(searchParams)}
           loading={exportExcel.isPending}
         >
           Excel 다운로드
         </Button>
         ```
       - DownloadOutlined 아이콘 import 추가: `import { PlusOutlined, DownloadOutlined } from '@ant-design/icons';`
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "useExportDeadExcel" frontend/src/api/dead.ts 에 결과 있음
    - grep "responseType.*blob" frontend/src/api/dead.ts 에 결과 있음
    - grep "useExportDeadExcel" frontend/src/pages/dead/DeadListPage.tsx 에 결과 있음
    - grep "DownloadOutlined" frontend/src/pages/dead/DeadListPage.tsx 에 결과 있음
    - grep "disabled" frontend/src/pages/dead/DeadListPage.tsx 에서 Excel 관련 disabled 없음
    - npx tsc --noEmit 에러 없음
  </acceptance_criteria>
  <done>프론트엔드 Excel 다운로드 기능 완성. Blob 응답 처리 + 파일 다운로드 트리거 + 로딩 상태 표시. TypeScript 컴파일 성공.</done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew test --tests "com.navy.casualty.dead.*" -x processTestAot` 전체 통과
- `cd frontend && npx tsc --noEmit` 에러 없음
- build.gradle.kts에 poi-ooxml 의존성 존재
- DeadExcelService에 SXSSFWorkbook + RrnMaskingUtil.mask + @AuditLog(EXPORT) 적용
- DeadListPage에 Excel 다운로드 버튼이 활성 상태
</verification>

<success_criteria>
- GET /api/dead/excel 이 검색 조건 적용된 .xlsx 파일을 반환
- Excel 파일에 주민번호가 역할별 마스킹 적용
- 프론트엔드에서 Excel 다운로드 버튼 클릭 시 파일 다운로드
- 감사 로그에 EXPORT 액션 기록
</success_criteria>

<output>
After completion, create `.planning/phases/03-dead-management/03-03-SUMMARY.md`
</output>
