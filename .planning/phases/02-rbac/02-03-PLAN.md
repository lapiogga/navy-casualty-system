---
phase: 02-rbac
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/pages/admin/AdminPage.tsx
  - frontend/src/pages/admin/UserManagement.tsx
  - frontend/src/pages/admin/AuditLogViewer.tsx
  - frontend/src/hooks/useAuth.ts
  - frontend/src/types/auth.ts
  - frontend/src/utils/rrnMask.ts
  - frontend/src/routes/index.tsx
  - frontend/src/components/layout/AppLayout.tsx
  - frontend/src/api/client.ts
  - frontend/src/__tests__/LoginPage.test.tsx
  - frontend/src/__tests__/AdminPage.test.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-05
  - AUTH-06
  - AUDIT-05

must_haves:
  truths:
    - "로그인 화면이 좌/우 분할 레이아웃으로 렌더링된다 (D-01)"
    - "좌측에 '해군 사상자 관리 시스템' 텍스트만 표시된다 (D-03)"
    - "로그인 실패 시 '사용자 ID 또는 비밀번호가 올바르지 않습니다' 메시지만 표시된다 (D-02)"
    - "미인증 상태에서 /dead 접근 시 /login으로 리다이렉트된다"
    - "AppLayout Header 우측에 사용자명 + 역할 + 로그아웃 버튼이 표시된다"
    - "ADMIN이 아닌 사용자는 시스템 관리 메뉴가 보이지 않는다 (AUTH-06)"
    - "관리자 화면에 사용자 관리 탭과 감사 로그 탭이 있다 (D-06)"
  artifacts:
    - path: "frontend/src/pages/LoginPage.tsx"
      provides: "분할 레이아웃 로그인 화면"
      min_lines: 50
    - path: "frontend/src/hooks/useAuth.ts"
      provides: "인증 상태 관리 + login/logout 함수"
    - path: "frontend/src/pages/admin/AdminPage.tsx"
      provides: "Tabs: 사용자 관리 / 감사 로그"
    - path: "frontend/src/pages/admin/UserManagement.tsx"
      provides: "ADMIN 전용 사용자 CRUD Table + Modal"
    - path: "frontend/src/pages/admin/AuditLogViewer.tsx"
      provides: "감사 로그 검색 + Table (읽기 전용)"
  key_links:
    - from: "LoginPage.tsx"
      to: "/api/auth/login"
      via: "apiClient.post"
      pattern: "apiClient\\.post.*auth/login"
    - from: "useAuth.ts"
      to: "/api/auth/me"
      via: "apiClient.get"
      pattern: "apiClient\\.get.*auth/me"
    - from: "AppLayout.tsx"
      to: "useAuth"
      via: "hook import"
      pattern: "useAuth"
    - from: "routes/index.tsx"
      to: "LoginPage"
      via: "route element"
      pattern: "LoginPage"
---

<objective>
프론트엔드 인증 흐름(로그인 화면, 라우트 가드, AppLayout 수정)과 관리자 화면(사용자 관리, 감사 로그)을 구현한다.

Purpose: D-01~D-03 로그인 화면 UX 결정사항 구현, AUTH-06 역할별 메뉴 필터링, D-06~D-07 관리자 화면 구현.
Output: LoginPage, useAuth, 라우트 가드, AdminPage(UserManagement + AuditLogViewer), AppLayout 업데이트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rbac/02-CONTEXT.md
@.planning/phases/02-rbac/02-RESEARCH.md
@.planning/phases/02-rbac/02-01-SUMMARY.md

<interfaces>
From 02-01-PLAN (인증 API):
```
POST /api/auth/login  {username, password} -> {success, data: {username, name, role}}
POST /api/auth/logout -> {success}
GET  /api/auth/me     -> {success, data: {username, name, role}}
POST /api/admin/users/{id}/force-logout -> {success} (ADMIN only)
```

From frontend/src/api/client.ts:
```typescript
// baseURL: '/api', 401 시 /login 리다이렉트 이미 구현
import apiClient from '../api/client';
```

From frontend/src/components/layout/AppLayout.tsx:
```typescript
// Header 우측에 사용자 정보 영역 예약됨: {/* Phase 2: 사용자 정보 표시 영역 */}
// Sider에 6개 메뉴 (dead, wounded, review, document, statistics, admin)
```

From frontend/src/routes/index.tsx:
```typescript
// /login -> PlaceholderPage (교체 필요)
// /admin -> PlaceholderPage (교체 필요)
// AppLayout 하위에 모든 메인 라우트
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 인증 타입 + useAuth 훅 + 로그인 페이지 + 라우트 가드</name>
  <files>
    frontend/src/types/auth.ts,
    frontend/src/hooks/useAuth.ts,
    frontend/src/utils/rrnMask.ts,
    frontend/src/pages/LoginPage.tsx,
    frontend/src/routes/index.tsx,
    frontend/src/components/layout/AppLayout.tsx,
    frontend/src/api/client.ts,
    frontend/src/__tests__/LoginPage.test.tsx
  </files>
  <read_first>
    frontend/src/routes/index.tsx,
    frontend/src/components/layout/AppLayout.tsx,
    frontend/src/api/client.ts,
    frontend/src/pages/ErrorPage.tsx,
    frontend/src/main.tsx
  </read_first>
  <action>
    1. **frontend/src/types/auth.ts**:
       ```typescript
       export type Role = 'ADMIN' | 'MANAGER' | 'OPERATOR' | 'VIEWER';

       export interface AuthUser {
         username: string;
         name: string;
         role: Role;
       }

       export interface LoginRequest {
         username: string;
         password: string;
       }
       ```

    2. **frontend/src/hooks/useAuth.ts**:
       - React Context 기반 (createContext + Provider + useContext)
       - 상태: `user: AuthUser | null`, `loading: boolean`
       - `login(username, password)`: apiClient.post('/auth/login') -> user 설정, 성공 시 /dead로 navigate
       - `logout()`: apiClient.post('/auth/logout') -> user null, /login으로 navigate
       - `checkSession()`: apiClient.get('/auth/me') -> user 설정 (실패 시 null)
       - AuthProvider 컴포넌트: 마운트 시 checkSession() 호출하여 기존 세션 복원
       - `isAdmin`, `isManagerOrAbove` 등 역할 확인 헬퍼
       - export: AuthProvider, useAuth

    3. **frontend/src/utils/rrnMask.ts** (D-04 프론트엔드 마스킹):
       ```typescript
       import type { Role } from '../types/auth';

       export function maskRrn(rrn: string | null | undefined, role: Role): string {
         if (!rrn) return '';
         switch (role) {
           case 'VIEWER': return '******-*******';
           case 'OPERATOR': return rrn.substring(0, 8) + '******';
           default: return rrn; // MANAGER, ADMIN
         }
       }
       ```

    4. **frontend/src/pages/LoginPage.tsx** (D-01, D-02, D-03):
       - 전체 화면 좌/우 분할 (display: flex, height: 100vh)
       - **좌측** (flex: 1): 중앙 정렬 텍스트만 (D-03)
         - `<h1>해군 사상자 관리 시스템</h1>`
         - `<p>해군본부 인사참모부</p>`
         - 배경: `#f5f5f5` (밝은 회색)
       - **우측** (flex: 1): Ant Design Form 중앙 정렬 (width: 360px)
         - Form.Item name="username" + Input(prefix UserOutlined, placeholder "사용자 ID", size "large")
         - Form.Item name="password" + Input.Password(prefix LockOutlined, placeholder "비밀번호", size "large")
         - rules: [{ required: true, message: "사용자 ID를 입력하세요" }], [{ required: true, message: "비밀번호를 입력하세요" }]
         - 에러 Alert: `{error && <Alert type="error" message={error} />}` -- D-02 에러 메시지만 표시
         - Button type="primary" htmlType="submit" loading={loading} block size="large" 텍스트: "로그인"
       - onFinish: useAuth().login(username, password) 호출. catch 시 `setError('사용자 ID 또는 비밀번호가 올바르지 않습니다')` -- D-02 모호한 메시지 고정
       - AppLayout 밖에서 렌더링 (라우터에서 별도 경로)

    5. **frontend/src/routes/index.tsx 수정**:
       - LoginPage import 및 `/login` 경로에 `element: <LoginPage />` (PlaceholderPage 교체)
       - AdminPage import 및 `/admin` 경로에 `element: <AdminPage />` (PlaceholderPage 교체)
       - AppLayout을 AuthProvider로 감싸기: 최상위에 AuthProvider 배치
       - 라우트 가드: AppLayout children에 인증 체크 추가. useAuth().user가 null이면 Navigate to="/login". 로딩 중이면 스피너.
       - `/admin` 라우트에 ADMIN 역할 가드 추가

    6. **frontend/src/components/layout/AppLayout.tsx 수정**:
       - Header 우측에 사용자 정보 표시: `{user?.name} ({user?.role})` + 로그아웃 버튼 (Button 또는 Dropdown)
       - Sider 메뉴에서 역할 기반 필터링: ADMIN이 아니면 `/admin` (시스템 관리) 메뉴 숨기기 -- AUTH-06
       - useAuth() 훅 사용하여 현재 사용자 정보 조회

    7. **frontend/src/api/client.ts 수정**:
       - 401 인터셉터: `/login` 경로에서는 리다이렉트하지 않음 (무한 루프 방지)
       - `if (window.location.pathname !== '/login') { window.location.href = '/login'; }`

    8. **frontend/src/__tests__/LoginPage.test.tsx**:
       - LoginPage 렌더링 테스트: "해군 사상자 관리 시스템" 텍스트 존재
       - "해군본부 인사참모부" 텍스트 존재
       - "사용자 ID" placeholder 존재
       - "비밀번호" placeholder 존재
       - "로그인" 버튼 존재
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - LoginPage.tsx에 `해군 사상자 관리 시스템` 텍스트 포함
    - LoginPage.tsx에 `해군본부 인사참모부` 텍스트 포함
    - LoginPage.tsx에 `사용자 ID 또는 비밀번호가 올바르지 않습니다` 포함 (D-02)
    - LoginPage.tsx에 `height: '100vh'` 또는 `100vh` 포함 (전체 화면)
    - useAuth.ts에 `apiClient.post` 포함 (login 호출)
    - useAuth.ts에 `apiClient.get` 또는 `/auth/me` 포함 (세션 확인)
    - rrnMask.ts에 `******-*******` 포함 (VIEWER 마스킹)
    - routes/index.tsx에 `LoginPage` import 포함
    - AppLayout.tsx에 `useAuth` 포함 (사용자 정보 표시)
    - AppLayout.tsx에 역할 기반 메뉴 필터링 로직 포함 (ADMIN 체크)
    - client.ts에 `/login` 경로 체크 포함 (무한 루프 방지)
    - `npx vitest run` 종료코드 0
  </acceptance_criteria>
  <done>
    로그인 화면이 D-01/D-02/D-03 사양대로 분할 레이아웃으로 렌더링.
    useAuth 훅이 login/logout/checkSession 제공.
    라우트 가드가 미인증 사용자를 /login으로 리다이렉트.
    AppLayout에 사용자 정보 + 로그아웃 버튼 표시.
    ADMIN이 아닌 사용자에게 시스템 관리 메뉴 숨김.
  </done>
</task>

<task type="auto">
  <name>Task 2: 관리자 화면 -- 사용자 관리 + 감사 로그 조회 + 스모크 테스트</name>
  <files>
    frontend/src/pages/admin/AdminPage.tsx,
    frontend/src/pages/admin/UserManagement.tsx,
    frontend/src/pages/admin/AuditLogViewer.tsx,
    frontend/src/__tests__/AdminPage.test.tsx
  </files>
  <read_first>
    frontend/src/components/layout/AppLayout.tsx,
    frontend/src/hooks/useAuth.ts,
    frontend/src/types/auth.ts,
    frontend/src/api/client.ts
  </read_first>
  <action>
    1. **AdminPage.tsx** (D-06):
       - Ant Design Tabs 컴포넌트 사용
       - 탭 2개: `items={[{key:'users', label:'사용자 관리', children: <UserManagement />}, {key:'audit', label:'감사 로그', children: <AuditLogViewer />}]}`
       - 기본 선택 탭: 사용자 관리

    2. **UserManagement.tsx** (AUTH-05, AUTH-07):
       - **목록 Table** (Ant Design Table):
         - columns: 사용자ID(username), 이름(name), 역할(role), 상태(enabled? "활성":"비활성"), 잠금(accountLocked? "잠금":"정상"), 최종로그인(lastLoginAt), 관리(actions)
         - actions 열: [역할 변경] [잠금 해제] [강제 로그아웃] [비활성화] 버튼
         - 데이터: apiClient.get('/admin/users') -> Table dataSource
         - 서버사이드 페이징: pagination={{ current, pageSize, total, onChange }}
       - **사용자 생성 Modal**:
         - 상단에 [사용자 추가] 버튼
         - Modal 내 Form: username(Input), name(Input), password(Input.Password), role(Select: ADMIN/MANAGER/OPERATOR/VIEWER)
         - 등록: apiClient.post('/admin/users', formData)
       - **역할 변경**: Select 컴포넌트로 역할 선택 -> apiClient.put('/admin/users/{id}/role', {role})
       - **잠금 해제**: Popconfirm -> apiClient.post('/admin/users/{id}/unlock')
       - **강제 로그아웃**: Popconfirm -> apiClient.post('/admin/users/{id}/force-logout')
       - 각 작업 후 목록 새로고침

    3. **AuditLogViewer.tsx** (D-06, D-07):
       - **검색 필터** (Form, inline layout):
         - 기간: DatePicker.RangePicker (시작일~종료일) -- dayjs 사용
         - 사용자ID: Input
         - 작업유형: Select (options: VIEW, CREATE, UPDATE, DELETE, PRINT, EXPORT + 전체)
         - [조회] 버튼
       - **목록 Table** (Ant Design Table, 읽기 전용):
         - columns: 일시(createdAt), 사용자ID(userId), 작업유형(action), 대상테이블(targetTable), 대상ID(targetId), 상세내용(detail), IP주소(ipAddress)
         - 서버사이드 페이징
         - 데이터: apiClient.get('/admin/audit-logs', { params: { startDate, endDate, userId, action, page, size } })
       - 수정/삭제 기능 없음 (읽기 전용)

    4. **frontend/src/__tests__/AdminPage.test.tsx** (스모크 테스트):
       - AdminPage 렌더링 테스트: Tabs 컴포넌트 렌더링 확인
       - "사용자 관리" 탭 텍스트 존재 확인
       - "감사 로그" 탭 텍스트 존재 확인
       - 기본 탭이 "사용자 관리"로 선택되어 있는지 확인
       - AuthProvider mock으로 감싸서 렌더링 (useAuth 의존성)

    **참고: 백엔드 Admin API 엔드포인트가 아직 없을 수 있음.**
    Plan 04에서 백엔드 Admin API를 구현하므로, 이 태스크에서는 프론트엔드만 구현하고 API 호출 경로만 정확히 맞춘다:
    - GET /api/admin/users (목록)
    - POST /api/admin/users (생성)
    - PUT /api/admin/users/{id}/role (역할 변경)
    - POST /api/admin/users/{id}/unlock (잠금 해제)
    - POST /api/admin/users/{id}/force-logout (강제 로그아웃)
    - GET /api/admin/audit-logs (감사 로그 검색)
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - AdminPage.tsx에 Ant Design `Tabs` 또는 `tabs` import 포함
    - AdminPage.tsx에 `사용자 관리` 텍스트 포함
    - AdminPage.tsx에 `감사 로그` 텍스트 포함
    - UserManagement.tsx에 `/admin/users` API 경로 포함
    - UserManagement.tsx에 `force-logout` 경로 포함
    - UserManagement.tsx에 `unlock` 경로 포함
    - AuditLogViewer.tsx에 `RangePicker` 또는 `DatePicker` 포함
    - AuditLogViewer.tsx에 `/admin/audit-logs` API 경로 포함
    - AuditLogViewer.tsx에 `action` 필터 Select 포함 (VIEW, CREATE, UPDATE, DELETE)
    - AdminPage.test.tsx에 Tabs 렌더링 스모크 테스트 존재
    - `npx vitest run` 종료코드 0
  </acceptance_criteria>
  <done>
    AdminPage에 Tabs(사용자 관리, 감사 로그) 렌더링 (D-06).
    UserManagement에 사용자 목록 Table + 생성 Modal + 역할 변경/잠금 해제/강제 로그아웃 기능.
    AuditLogViewer에 D-07 필터 3개(기간, 사용자ID, 작업유형) + 읽기 전용 Table.
    AdminPage.test.tsx 스모크 테스트 통과.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run` 전체 통과
- LoginPage가 분할 레이아웃으로 렌더링
- 라우트 가드 동작 (미인증 -> /login)
- AppLayout Header에 사용자 정보 표시
- AdminPage Tabs 2개 렌더링
</verification>

<success_criteria>
- D-01/D-02/D-03 로그인 화면 사양 충족
- useAuth 훅으로 인증 상태 관리
- AUTH-06 역할별 메뉴 필터링 동작
- D-06/D-07 관리자 화면 사양 충족
- 모든 프론트엔드 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/02-rbac/02-03-SUMMARY.md`
</output>
