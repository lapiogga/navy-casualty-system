---
phase: 02-rbac
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - backend/src/main/java/com/navy/casualty/user/controller/UserController.java
  - backend/src/main/java/com/navy/casualty/user/dto/UserCreateRequest.java
  - backend/src/main/java/com/navy/casualty/user/dto/UserUpdateRoleRequest.java
  - backend/src/main/java/com/navy/casualty/user/dto/UserResponse.java
  - backend/src/main/java/com/navy/casualty/audit/controller/AuditLogController.java
  - backend/src/main/java/com/navy/casualty/audit/dto/AuditLogSearchRequest.java
  - backend/src/main/java/com/navy/casualty/audit/dto/AuditLogResponse.java
  - backend/src/test/java/com/navy/casualty/user/UserControllerTest.java
  - backend/src/test/java/com/navy/casualty/audit/AuditLogControllerTest.java
autonomous: true
requirements:
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - AUDIT-01
  - AUDIT-03
  - AUDIT-07

must_haves:
  truths:
    - "ADMIN이 GET /api/admin/users로 사용자 목록을 조회할 수 있다"
    - "ADMIN이 POST /api/admin/users로 새 사용자를 생성할 수 있다"
    - "ADMIN이 PUT /api/admin/users/{id}/role로 역할을 변경할 수 있다"
    - "ADMIN이 POST /api/admin/users/{id}/unlock으로 잠금을 해제할 수 있다"
    - "ADMIN이 GET /api/admin/audit-logs로 감사 로그를 검색할 수 있다"
    - "NON-ADMIN 사용자는 /api/admin/** 접근 시 403 응답을 받는다"
  artifacts:
    - path: "backend/src/main/java/com/navy/casualty/user/controller/UserController.java"
      provides: "ADMIN 전용 사용자 CRUD API"
      contains: "@PreAuthorize"
    - path: "backend/src/main/java/com/navy/casualty/audit/controller/AuditLogController.java"
      provides: "ADMIN 전용 감사 로그 검색 API"
      contains: "@PreAuthorize"
  key_links:
    - from: "UserController"
      to: "UserService"
      via: "의존성 주입"
      pattern: "userService"
    - from: "AuditLogController"
      to: "AuditLogService"
      via: "의존성 주입"
      pattern: "auditLogService"
---

<objective>
ADMIN 전용 관리자 백엔드 API(사용자 CRUD + 감사 로그 검색)를 구현한다.

Purpose: 프론트엔드 관리자 화면(02-03)이 호출할 백엔드 API 구현. AUTH-05(역할 부여), AUTH-07(강제 로그아웃), AUDIT-01(감사 로그 검색) 요구사항 충족.
Output: UserController, AuditLogController, 관련 DTO, 통합 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rbac/02-CONTEXT.md
@.planning/phases/02-rbac/02-RESEARCH.md
@.planning/phases/02-rbac/02-01-SUMMARY.md
@.planning/phases/02-rbac/02-02-SUMMARY.md

<interfaces>
From 02-01 (User 엔티티 + UserService):
```java
// UserService
User findById(Long id)
List<User> findAll()
User createUser(String username, String password, String name, String role)
void updateRole(Long userId, String role)
void toggleEnabled(Long userId)

// AuthService
void unlockAccount(Long userId)  // account_locked=false, failed_login_count=0
```

From 02-01 (SecurityConfig):
```java
// /api/admin/** -> hasRole("ADMIN") 이미 설정
// SessionRegistry -> sessionRegistry() @Bean
```

From 02-02 (AuditLogService):
```java
// AuditLogService
void save(AuditLogEntry entry)
Page<AuditLogEntry> search(startDate, endDate, userId, action, Pageable pageable)
```

From ApiResponse.java:
```java
public static <T> ApiResponse<T> ok(T data)
public static <T> ApiResponse<T> error(int status, String message)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ADMIN 사용자 관리 API (UserController + DTO)</name>
  <files>
    backend/src/main/java/com/navy/casualty/user/controller/UserController.java,
    backend/src/main/java/com/navy/casualty/user/dto/UserCreateRequest.java,
    backend/src/main/java/com/navy/casualty/user/dto/UserUpdateRoleRequest.java,
    backend/src/main/java/com/navy/casualty/user/dto/UserResponse.java,
    backend/src/test/java/com/navy/casualty/user/UserControllerTest.java
  </files>
  <read_first>
    backend/src/main/java/com/navy/casualty/user/service/UserService.java,
    backend/src/main/java/com/navy/casualty/auth/service/AuthService.java,
    backend/src/main/java/com/navy/casualty/security/SecurityConfig.java,
    backend/src/main/java/com/navy/casualty/common/dto/ApiResponse.java,
    backend/src/main/java/com/navy/casualty/user/entity/User.java
  </read_first>
  <behavior>
    - GET /api/admin/users -> 200 + 사용자 목록 (password 제외)
    - POST /api/admin/users {username, password, name, role} -> 201 + 생성된 사용자
    - POST /api/admin/users 중복 username -> 409
    - PUT /api/admin/users/{id}/role {role: "MANAGER"} -> 200
    - PUT /api/admin/users/{id}/role {role: "INVALID"} -> 400
    - POST /api/admin/users/{id}/unlock -> 200
    - DELETE /api/admin/users/{id} -> 200 (soft delete)
    - NON-ADMIN 접근 -> 403
  </behavior>
  <action>
    1. **UserResponse.java** (user/dto):
       ```java
       public record UserResponse(
           Long id,
           String username,
           String name,
           String role,
           boolean enabled,
           boolean accountLocked,
           int failedLoginCount,
           LocalDateTime lastLoginAt,
           LocalDateTime createdAt
       ) {
           public static UserResponse from(User user) {
               return new UserResponse(
                   user.getId(), user.getUsername(), user.getName(),
                   user.getRole(), user.isEnabled(), user.isAccountLocked(),
                   user.getFailedLoginCount(), user.getLastLoginAt(),
                   user.getCreatedAt()
               );
           }
       }
       ```

    2. **UserCreateRequest.java** (user/dto):
       ```java
       public record UserCreateRequest(
           @NotBlank String username,
           @NotBlank @Size(min = 8) String password,
           @NotBlank String name,
           @NotBlank @Pattern(regexp = "ADMIN|MANAGER|OPERATOR|VIEWER") String role
       ) {}
       ```

    3. **UserUpdateRoleRequest.java** (user/dto):
       ```java
       public record UserUpdateRoleRequest(
           @NotBlank @Pattern(regexp = "ADMIN|MANAGER|OPERATOR|VIEWER") String role
       ) {}
       ```

    4. **UserController.java** (user/controller):
       - @RestController @RequestMapping("/api/admin/users")
       - @PreAuthorize("hasRole('ADMIN')") 클래스 레벨
       - **GET /** -> userService.findAll() -> List<UserResponse> -> ApiResponse.ok()
       - **POST /** -> userService.createUser(req.username(), req.password(), req.name(), req.role()) -> UserResponse -> ApiResponse(201)
         - 중복 username 시 DataIntegrityViolationException catch -> ApiResponse.error(409, "이미 존재하는 사용자 ID입니다")
       - **PUT /{id}/role** -> userService.updateRole(id, req.role()) -> ApiResponse.ok()
       - **POST /{id}/unlock** -> authService.unlockAccount(id) -> ApiResponse.ok()
       - **POST /{id}/force-logout** -> SessionRegistry.getAllSessions(username, false).forEach(expireNow) -> ApiResponse.ok()
         - (주: 강제 로그아웃은 02-01의 AuthController에 이미 구현되었을 수 있음. UserController에서 구현하거나 기존 것을 활용)
       - **PUT /{id}/enabled** -> userService.toggleEnabled(id) -> ApiResponse.ok()

    5. **UserControllerTest.java**:
       - @SpringBootTest @AutoConfigureMockMvc @Transactional
       - @WithMockUser(roles="ADMIN") 사용자 목록 조회 -> 200
       - @WithMockUser(roles="ADMIN") 사용자 생성 -> 201
       - @WithMockUser(roles="ADMIN") 역할 변경 -> 200
       - @WithMockUser(roles="ADMIN") 잠금 해제 -> 200
       - @WithMockUser(roles="OPERATOR") 접근 -> 403
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*UserControllerTest*" -x bootJar</automated>
  </verify>
  <acceptance_criteria>
    - UserController.java에 `@RequestMapping("/api/admin/users")` 포함
    - UserController.java에 `@PreAuthorize("hasRole('ADMIN')")` 포함
    - UserController.java에 `@GetMapping` 포함 (목록 조회)
    - UserController.java에 `@PostMapping` 포함 (사용자 생성)
    - UserController.java에 `/{id}/role` 경로 포함
    - UserController.java에 `/{id}/unlock` 경로 포함
    - UserCreateRequest.java에 `@NotBlank` 포함
    - UserCreateRequest.java에 `@Size(min = 8)` 포함 (비밀번호 최소 길이)
    - UserResponse.java에 `password` 필드 없음
    - `./gradlew test --tests "*UserControllerTest*"` 종료코드 0
  </acceptance_criteria>
  <done>
    ADMIN 전용 사용자 CRUD API 완성 (목록, 생성, 역할 변경, 잠금 해제, 강제 로그아웃).
    NON-ADMIN 403 차단 확인.
    UserControllerTest 전체 통과.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ADMIN 감사 로그 검색 API (AuditLogController + DTO)</name>
  <files>
    backend/src/main/java/com/navy/casualty/audit/controller/AuditLogController.java,
    backend/src/main/java/com/navy/casualty/audit/dto/AuditLogSearchRequest.java,
    backend/src/main/java/com/navy/casualty/audit/dto/AuditLogResponse.java,
    backend/src/test/java/com/navy/casualty/audit/AuditLogControllerTest.java
  </files>
  <read_first>
    backend/src/main/java/com/navy/casualty/audit/service/AuditLogService.java,
    backend/src/main/java/com/navy/casualty/audit/entity/AuditLogEntry.java,
    backend/src/main/java/com/navy/casualty/audit/repository/AuditLogRepository.java,
    backend/src/main/java/com/navy/casualty/common/dto/ApiResponse.java
  </read_first>
  <behavior>
    - GET /api/admin/audit-logs?startDate=2026-01-01&endDate=2026-12-31 -> 200 + 페이징 목록
    - GET /api/admin/audit-logs?userId=admin -> 200 + admin 사용자 로그만
    - GET /api/admin/audit-logs?action=CREATE -> 200 + CREATE 작업만
    - GET /api/admin/audit-logs (필터 없음) -> 200 + 전체 최신순
    - NON-ADMIN 접근 -> 403
  </behavior>
  <action>
    1. **AuditLogResponse.java** (audit/dto):
       ```java
       public record AuditLogResponse(
           Long id,
           String userId,
           String action,
           String targetTable,
           Long targetId,
           String detail,
           String ipAddress,
           LocalDateTime createdAt
       ) {
           public static AuditLogResponse from(AuditLogEntry entry) {
               return new AuditLogResponse(
                   entry.getId(), entry.getUserId(), entry.getAction(),
                   entry.getTargetTable(), entry.getTargetId(),
                   entry.getDetail(), entry.getIpAddress(), entry.getCreatedAt()
               );
           }
       }
       ```

    2. **AuditLogSearchRequest.java** (audit/dto):
       ```java
       public record AuditLogSearchRequest(
           @DateTimeFormat(iso = DateTimeFormat.ISO.DATE) LocalDate startDate,
           @DateTimeFormat(iso = DateTimeFormat.ISO.DATE) LocalDate endDate,
           String userId,
           String action  // VIEW, CREATE, UPDATE, DELETE, PRINT, EXPORT
       ) {}
       ```

    3. **AuditLogController.java** (audit/controller):
       - @RestController @RequestMapping("/api/admin/audit-logs")
       - @PreAuthorize("hasRole('ADMIN')")
       - **GET /** (@ModelAttribute AuditLogSearchRequest search, Pageable pageable):
         - D-07 필터: startDate~endDate(기간), userId(사용자ID), action(작업유형)
         - AuditLogService.search(search.startDate(), search.endDate(), search.userId(), search.action(), pageable)
         - Page<AuditLogResponse> 반환 -> ApiResponse.ok()
         - 기본 정렬: createdAt DESC (최신순)
       - 수정/삭제 엔드포인트 없음 (AUDIT-02 append-only)

    4. **AuditLogControllerTest.java**:
       - @SpringBootTest @AutoConfigureMockMvc @Transactional
       - @WithMockUser(roles="ADMIN") 감사 로그 조회 -> 200
       - @WithMockUser(roles="ADMIN") 기간 필터 조회 -> 200
       - @WithMockUser(roles="ADMIN") 작업유형 필터 조회 -> 200
       - @WithMockUser(roles="OPERATOR") 접근 -> 403
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*AuditLogControllerTest*" -x bootJar</automated>
  </verify>
  <acceptance_criteria>
    - AuditLogController.java에 `@RequestMapping("/api/admin/audit-logs")` 포함
    - AuditLogController.java에 `@PreAuthorize("hasRole('ADMIN')")` 포함
    - AuditLogController.java에 `@GetMapping` 포함
    - AuditLogController.java에 수정/삭제 엔드포인트 없음 (POST/PUT/DELETE 없음)
    - AuditLogSearchRequest.java에 `startDate` 필드 포함
    - AuditLogSearchRequest.java에 `userId` 필드 포함
    - AuditLogSearchRequest.java에 `action` 필드 포함
    - AuditLogResponse.java에 `from(AuditLogEntry)` 팩토리 메서드 포함
    - `./gradlew test --tests "*AuditLogControllerTest*"` 종료코드 0
  </acceptance_criteria>
  <done>
    ADMIN 전용 감사 로그 검색 API 완성.
    D-07 필터 3개 (기간, 사용자ID, 작업유형) 지원.
    append-only 원칙 -- 수정/삭제 API 없음.
    AuditLogControllerTest 전체 통과.
  </done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew test --tests "*UserControllerTest*" --tests "*AuditLogControllerTest*"` 전체 통과
- ADMIN 사용자 CRUD API 동작
- 감사 로그 검색 API 동작
- NON-ADMIN 403 차단 확인
</verification>

<success_criteria>
- 사용자 관리 API 6개 엔드포인트 동작 (목록, 생성, 역할변경, 잠금해제, 강제로그아웃, 활성화)
- 감사 로그 검색 API 동작 (D-07 필터 3개)
- @PreAuthorize ADMIN 가드 모든 엔드포인트에 적용
- 모든 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/02-rbac/02-04-SUMMARY.md`
</output>
