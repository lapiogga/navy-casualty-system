---
phase: 02-rbac
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/navy/casualty/common/crypto/PiiEncryptionConverter.java
  - backend/src/main/java/com/navy/casualty/common/crypto/RrnMaskingUtil.java
  - backend/src/main/java/com/navy/casualty/audit/annotation/AuditLog.java
  - backend/src/main/java/com/navy/casualty/audit/aspect/AuditLogAspect.java
  - backend/src/main/java/com/navy/casualty/audit/entity/AuditLogEntry.java
  - backend/src/main/java/com/navy/casualty/audit/repository/AuditLogRepository.java
  - backend/src/main/java/com/navy/casualty/audit/service/AuditLogService.java
  - backend/src/main/resources/db/migration/V11__audit_log_append_only_rule.sql
  - backend/src/test/java/com/navy/casualty/common/crypto/PiiEncryptionConverterTest.java
  - backend/src/test/java/com/navy/casualty/common/crypto/RrnMaskingUtilTest.java
  - backend/src/test/java/com/navy/casualty/audit/AuditLogAspectTest.java
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - AUDIT-06

must_haves:
  truths:
    - "주민번호가 DB에 AES-256-GCM으로 암호화되어 저장된다"
    - "VIEWER 역할은 주민번호를 ******-******* 으로 본다"
    - "OPERATOR 역할은 주민번호 앞 8자리만 보고 뒤 6자리는 ****** 으로 본다"
    - "MANAGER/ADMIN 역할은 주민번호 전체를 볼 수 있다"
    - "@AuditLog 어노테이션이 붙은 서비스 메서드 호출 시 TB_AUDIT_LOG에 자동 기록된다"
    - "TB_AUDIT_LOG에 UPDATE/DELETE SQL이 차단된다 (append-only)"
  artifacts:
    - path: "backend/src/main/java/com/navy/casualty/common/crypto/PiiEncryptionConverter.java"
      provides: "AES-256-GCM Hibernate AttributeConverter"
      contains: "implements AttributeConverter<String, String>"
    - path: "backend/src/main/java/com/navy/casualty/common/crypto/RrnMaskingUtil.java"
      provides: "역할별 3단계 주민번호 마스킹"
      contains: "public static String mask"
    - path: "backend/src/main/java/com/navy/casualty/audit/aspect/AuditLogAspect.java"
      provides: "AOP 감사 로그 자동 기록"
      contains: "@Around"
    - path: "backend/src/main/resources/db/migration/V11__audit_log_append_only_rule.sql"
      provides: "TB_AUDIT_LOG UPDATE/DELETE 차단 RULE"
      contains: "CREATE RULE"
  key_links:
    - from: "PiiEncryptionConverter"
      to: "PII_ENCRYPTION_KEY 환경변수"
      via: "System.getenv"
      pattern: "System\\.getenv\\(\"PII_ENCRYPTION_KEY\"\\)"
    - from: "AuditLogAspect"
      to: "AuditLogRepository"
      via: "save()"
      pattern: "auditLogRepository\\.save"
    - from: "RrnMaskingUtil"
      to: "role 파라미터"
      via: "switch expression"
      pattern: "ROLE_VIEWER.*ROLE_OPERATOR"
---

<objective>
주민번호 AES-256-GCM 암호화, 역할별 마스킹 유틸, AOP 감사 로그 자동 기록을 구현한다.

Purpose: 개인정보보호법(AUDIT-04, AUDIT-05) 및 감사 로그(AUDIT-01~03) 법적 요구사항 충족. Phase 3~8의 모든 서비스 메서드가 @AuditLog를 사용한다.
Output: PiiEncryptionConverter, RrnMaskingUtil, @AuditLog + AuditLogAspect, append-only RULE
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rbac/02-CONTEXT.md
@.planning/phases/02-rbac/02-RESEARCH.md

<interfaces>
From V6__create_audit_log_table.sql (TB_AUDIT_LOG 스키마):
```sql
CREATE TABLE TB_AUDIT_LOG (
    id BIGSERIAL PRIMARY KEY,
    user_id VARCHAR(50) NOT NULL,
    action VARCHAR(20) NOT NULL,       -- VIEW, CREATE, UPDATE, DELETE, PRINT, EXPORT
    target_table VARCHAR(50),
    target_id BIGINT,
    detail TEXT,
    ip_address VARCHAR(45),
    created_at TIMESTAMP NOT NULL DEFAULT NOW()
);
```

From BaseAuditEntity.java:
```java
public abstract class BaseAuditEntity {
    // soft delete 메서드 이미 존재 -- AUDIT-06 충족
    public void softDelete(String deletedBy, String reason)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PII 암호화 컨버터 + 주민번호 마스킹 유틸</name>
  <files>
    backend/src/main/java/com/navy/casualty/common/crypto/PiiEncryptionConverter.java,
    backend/src/main/java/com/navy/casualty/common/crypto/RrnMaskingUtil.java,
    backend/src/test/java/com/navy/casualty/common/crypto/PiiEncryptionConverterTest.java,
    backend/src/test/java/com/navy/casualty/common/crypto/RrnMaskingUtilTest.java
  </files>
  <read_first>
    backend/src/main/resources/db/migration/V3__create_dead_table.sql,
    backend/src/main/resources/db/migration/V4__create_wounded_table.sql,
    backend/src/main/resources/application.yml
  </read_first>
  <behavior>
    - PiiEncryptionConverter: "900101-1234567" -> 암호화 -> Base64 문자열 (null이 아님)
    - PiiEncryptionConverter: 암호화된 값 -> 복호화 -> "900101-1234567" 원복
    - PiiEncryptionConverter: null 입력 -> null 반환 (encrypt/decrypt 모두)
    - PiiEncryptionConverter: 같은 평문 2번 암호화 시 다른 암호문 (IV 랜덤)
    - PiiEncryptionConverter: PII_ENCRYPTION_KEY 미설정 시 IllegalStateException
    - RrnMaskingUtil.mask("900101-1234567", "ROLE_VIEWER") -> "******-*******"
    - RrnMaskingUtil.mask("900101-1234567", "ROLE_OPERATOR") -> "900101-1******"
    - RrnMaskingUtil.mask("900101-1234567", "ROLE_MANAGER") -> "900101-1234567"
    - RrnMaskingUtil.mask("900101-1234567", "ROLE_ADMIN") -> "900101-1234567"
    - RrnMaskingUtil.mask(null, "ROLE_VIEWER") -> ""
    - RrnMaskingUtil.mask("", "ROLE_VIEWER") -> ""
  </behavior>
  <action>
    1. **PiiEncryptionConverter.java** (common/crypto):
       - `@Converter` implements `AttributeConverter<String, String>`
       - 알고리즘: `AES/GCM/NoPadding`, IV 12바이트, 인증 태그 128비트
       - `getKey()`: `System.getenv("PII_ENCRYPTION_KEY")` -> Base64 디코드 -> SecretKeySpec("AES"). 미설정이면 `throw new IllegalStateException("PII_ENCRYPTION_KEY 환경변수가 설정되지 않았습니다")`
       - `convertToDatabaseColumn(String)`: null이면 null. 아니면 SecureRandom으로 IV 생성, GCM 암호화, IV + ciphertext 합쳐서 Base64 인코딩
       - `convertToEntityAttribute(String)`: null이면 null. 아니면 Base64 디코드, IV 분리, GCM 복호화
       - setter 없음 (불변)

    2. **RrnMaskingUtil.java** (common/crypto):
       - `public final class RrnMaskingUtil` (유틸 클래스, private 생성자)
       - `public static String mask(String rrn, String role)`:
         - null 또는 blank -> "" 반환
         - "ROLE_VIEWER" -> "******-*******"
         - "ROLE_OPERATOR" -> `rrn.substring(0, 8) + "******"` (예: "900101-1******")
         - default (ROLE_MANAGER, ROLE_ADMIN) -> rrn 그대로 반환
       - D-04 준수: 3단계 마스킹 정확히 구현

    3. **PiiEncryptionConverterTest.java**:
       - @BeforeEach에서 환경변수 대신 테스트용 키를 reflection으로 주입하거나, 테스트 전용 서브클래스 사용
       - 또는 환경변수 `PII_ENCRYPTION_KEY`를 테스트에서 설정 (System.setProperty 대신 테스트 전용 접근 방식)
       - 실용적 접근: 생성자 또는 protected 메서드로 키를 주입 가능하게 설계하되, 프로덕션에서는 환경변수 사용

    4. **RrnMaskingUtilTest.java**:
       - 각 역할별 마스킹 결과 검증 (위 behavior 그대로)
       - null, 빈 문자열 입력 처리 검증
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*PiiEncryptionConverterTest*" --tests "*RrnMaskingUtilTest*" -x bootJar</automated>
  </verify>
  <acceptance_criteria>
    - PiiEncryptionConverter.java에 `AES/GCM/NoPadding` 포함
    - PiiEncryptionConverter.java에 `System.getenv("PII_ENCRYPTION_KEY")` 포함
    - PiiEncryptionConverter.java에 `implements AttributeConverter<String, String>` 포함
    - RrnMaskingUtil.java에 `"******-*******"` 포함
    - RrnMaskingUtil.java에 `ROLE_VIEWER` 포함
    - RrnMaskingUtil.java에 `ROLE_OPERATOR` 포함
    - `./gradlew test --tests "*PiiEncryptionConverterTest*"` 종료코드 0
    - `./gradlew test --tests "*RrnMaskingUtilTest*"` 종료코드 0
  </acceptance_criteria>
  <done>
    PiiEncryptionConverter가 AES-256-GCM으로 암/복호화 수행.
    RrnMaskingUtil이 D-04 3단계 마스킹 정확히 구현.
    모든 단위 테스트 통과.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AOP 감사 로그 Aspect + AuditLog 엔티티 + append-only RULE</name>
  <files>
    backend/src/main/java/com/navy/casualty/audit/annotation/AuditLog.java,
    backend/src/main/java/com/navy/casualty/audit/aspect/AuditLogAspect.java,
    backend/src/main/java/com/navy/casualty/audit/entity/AuditLogEntry.java,
    backend/src/main/java/com/navy/casualty/audit/repository/AuditLogRepository.java,
    backend/src/main/java/com/navy/casualty/audit/service/AuditLogService.java,
    backend/src/main/resources/db/migration/V11__audit_log_append_only_rule.sql,
    backend/src/test/java/com/navy/casualty/audit/AuditLogAspectTest.java
  </files>
  <read_first>
    backend/src/main/resources/db/migration/V6__create_audit_log_table.sql,
    backend/src/main/resources/application.yml
  </read_first>
  <behavior>
    - @AuditLog(action="CREATE", targetTable="TB_DEAD") 메서드 호출 시 TB_AUDIT_LOG에 행 삽입
    - 감사 로그에 user_id, action, target_table, ip_address, created_at, detail 기록
    - 메서드 실패 시에도 감사 로그 기록 (detail에 "실패: {에러메시지}")
    - D-08: LIST 조회는 기록하지 않음 (어노테이션 자체가 없으면 기록 안 됨)
    - D-09: detail에 한줄 요약 텍스트 (JSON diff 아님)
    - TB_AUDIT_LOG에 UPDATE/DELETE 시도 시 차단됨
  </behavior>
  <action>
    1. **AuditLog.java** (audit/annotation):
       ```java
       @Target(ElementType.METHOD)
       @Retention(RetentionPolicy.RUNTIME)
       public @interface AuditLog {
           String action();                 // VIEW, CREATE, UPDATE, DELETE, PRINT, EXPORT
           String targetTable() default ""; // 대상 테이블명
       }
       ```

    2. **AuditLogEntry.java** (audit/entity):
       - @Entity @Table(name = "TB_AUDIT_LOG")
       - @Id @GeneratedValue(IDENTITY) Long id
       - String userId, String action, String targetTable, Long targetId, String detail, String ipAddress
       - @Column(name = "created_at", updatable = false) LocalDateTime createdAt
       - @PrePersist로 createdAt = LocalDateTime.now()
       - **setter 없음** -- Builder 패턴으로만 생성 (@Builder, 모든 필드 final 또는 생성자만)
       - 감사 컬럼(updatedAt 등) 없음 -- append-only 특성

    3. **AuditLogRepository.java** (audit/repository):
       - extends JpaRepository<AuditLogEntry, Long>
       - `Page<AuditLogEntry> findByCreatedAtBetween(LocalDateTime start, LocalDateTime end, Pageable pageable)`
       - `Page<AuditLogEntry> findByUserId(String userId, Pageable pageable)`

    4. **AuditLogService.java** (audit/service):
       - save(AuditLogEntry entry): repository.save()
       - search(startDate, endDate, userId, action, pageable): Specification 또는 QueryDSL로 동적 검색 (D-07 필터 3개 지원)

    5. **AuditLogAspect.java** (audit/aspect):
       - @Aspect @Component
       - 주입: AuditLogRepository, HttpServletRequest (RequestContextHolder 경유)
       - `@Around("@annotation(auditLog)")` 메서드:
         - userId: SecurityContextHolder -> username, 없으면 "ANONYMOUS"
         - ipAddress: request.getRemoteAddr()
         - try { proceed() } 성공 시: detail 생성 (D-09 한줄 요약), AuditLogEntry 빌더로 생성 후 save
         - catch 시: detail = "실패: " + e.getMessage(), save 후 re-throw
       - **detail 생성 로직**: action별 포맷
         - CREATE: 첫 번째 DTO 인자에서 주요 필드 추출 -> "군번 12345 사망자 등록" 형식
         - UPDATE: "계급: 상등병 -> 병장" 형식 (변경 전후 비교가 어려우면 "사망자 ID 123 수정" 형식)
         - DELETE: "삭제 사유: 오등록" 형식
         - VIEW: "군번 12345 상세 조회" 형식
         - 복잡한 diff는 하지 않음. 간단한 toString 추출으로 충분 (D-09)
       - targetId: 메서드 인자에서 Long/long 타입 첫 번째 값 추출 (없으면 null)

    6. **V11__audit_log_append_only_rule.sql**:
       ```sql
       -- AUDIT-02: TB_AUDIT_LOG append-only 보호
       -- UPDATE/DELETE 시도를 PostgreSQL RULE로 차단
       CREATE RULE no_update_audit_log AS
           ON UPDATE TO TB_AUDIT_LOG DO INSTEAD NOTHING;

       CREATE RULE no_delete_audit_log AS
           ON DELETE TO TB_AUDIT_LOG DO INSTEAD NOTHING;
       ```

    7. **AuditLogAspectTest.java**:
       - @SpringBootTest + @Transactional
       - 테스트용 @AuditLog 메서드를 가진 서비스 Bean 생성
       - 메서드 호출 후 AuditLogRepository에서 로그 존재 확인
       - 실패 시에도 로그 기록 확인
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*AuditLogAspectTest*" -x bootJar</automated>
  </verify>
  <acceptance_criteria>
    - AuditLog.java에 `@Target(ElementType.METHOD)` 포함
    - AuditLog.java에 `String action()` 포함
    - AuditLogAspect.java에 `@Around("@annotation(auditLog)")` 포함
    - AuditLogAspect.java에 `SecurityContextHolder` 포함 (userId 추출)
    - AuditLogEntry.java에 `@Table(name = "TB_AUDIT_LOG")` 포함
    - AuditLogEntry.java에 setter 없음 (@Builder 또는 생성자만)
    - V11__audit_log_append_only_rule.sql에 `CREATE RULE no_update_audit_log` 포함
    - V11__audit_log_append_only_rule.sql에 `CREATE RULE no_delete_audit_log` 포함
    - `./gradlew test --tests "*AuditLogAspectTest*"` 종료코드 0
  </acceptance_criteria>
  <done>
    @AuditLog 어노테이션 + AOP Aspect로 서비스 메서드 감사 로그 자동 기록.
    AuditLogEntry Builder 패턴 (append-only).
    TB_AUDIT_LOG UPDATE/DELETE 차단 RULE 적용.
    D-08 (상세 조회만), D-09 (한줄 요약) 준수.
    AuditLogAspectTest 통과.
  </done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew test --tests "*Pii*" --tests "*Rrn*" --tests "*Audit*"` 전체 통과
- PiiEncryptionConverter 암/복호화 왕복 테스트 통과
- RrnMaskingUtil 3단계 마스킹 테스트 통과
- AuditLogAspect 자동 기록 테스트 통과
</verification>

<success_criteria>
- AES-256-GCM 암호화 컨버터가 동작하고 환경변수 기반 키 관리
- 3단계 마스킹(VIEWER/OPERATOR/MANAGER+)이 D-04 사양대로 동작
- @AuditLog AOP가 감사 로그를 자동 기록
- TB_AUDIT_LOG가 append-only (UPDATE/DELETE 차단 RULE)
- 모든 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/02-rbac/02-02-SUMMARY.md`
</output>
