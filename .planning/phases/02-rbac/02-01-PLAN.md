---
phase: 02-rbac
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/build.gradle.kts
  - backend/src/main/java/com/navy/casualty/security/CustomUserDetails.java
  - backend/src/main/java/com/navy/casualty/security/CustomUserDetailsService.java
  - backend/src/main/java/com/navy/casualty/security/SecurityConfig.java
  - backend/src/main/java/com/navy/casualty/security/SecurityUtils.java
  - backend/src/main/java/com/navy/casualty/user/entity/User.java
  - backend/src/main/java/com/navy/casualty/user/repository/UserRepository.java
  - backend/src/main/java/com/navy/casualty/user/service/UserService.java
  - backend/src/main/java/com/navy/casualty/auth/controller/AuthController.java
  - backend/src/main/java/com/navy/casualty/auth/dto/LoginRequest.java
  - backend/src/main/java/com/navy/casualty/auth/dto/LoginResponse.java
  - backend/src/main/java/com/navy/casualty/auth/service/AuthService.java
  - backend/src/main/java/com/navy/casualty/common/config/JpaAuditingConfig.java
  - backend/src/main/java/com/navy/casualty/common/config/SecurityConfig.java
  - backend/src/main/java/com/navy/casualty/common/exception/GlobalExceptionHandler.java
  - backend/src/main/resources/db/migration/V10__insert_admin_user.sql
  - backend/src/test/java/com/navy/casualty/auth/AuthControllerTest.java
  - backend/src/test/java/com/navy/casualty/auth/AuthServiceTest.java
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-07

must_haves:
  truths:
    - "유효한 username/password로 POST /api/auth/login 시 200 + 세션 쿠키 반환"
    - "잘못된 비밀번호로 5회 로그인 시 계정 잠금되고 이후 시도도 동일 에러 메시지 반환"
    - "세션 타임아웃은 30분으로 설정됨"
    - "동시 세션 1개 제한 -- 새 로그인 시 기존 세션 만료"
    - "GET /api/auth/me 로 현재 세션 사용자 정보 조회 가능"
  artifacts:
    - path: "backend/src/main/java/com/navy/casualty/security/SecurityConfig.java"
      provides: "RBAC SecurityFilterChain, SessionRegistry, RoleHierarchy, PasswordEncoder"
      exports: ["filterChain", "sessionRegistry", "roleHierarchy", "passwordEncoder"]
    - path: "backend/src/main/java/com/navy/casualty/auth/controller/AuthController.java"
      provides: "POST /api/auth/login, POST /api/auth/logout, GET /api/auth/me"
    - path: "backend/src/main/java/com/navy/casualty/user/entity/User.java"
      provides: "TB_USER JPA 엔티티"
      contains: "@Entity @Table(name = \"TB_USER\")"
    - path: "backend/src/main/resources/db/migration/V10__insert_admin_user.sql"
      provides: "초기 admin 계정"
      contains: "INSERT INTO TB_USER"
  key_links:
    - from: "AuthController"
      to: "AuthenticationManager"
      via: "UsernamePasswordAuthenticationToken"
      pattern: "authenticationManager.authenticate"
    - from: "CustomUserDetailsService"
      to: "UserRepository"
      via: "loadUserByUsername"
      pattern: "userRepository.findByUsername"
    - from: "SecurityConfig"
      to: "SessionRegistry"
      via: "sessionManagement.maximumSessions(1)"
      pattern: "maximumSessions\\(1\\)"
---

<objective>
Spring Security 세션 기반 인증 인프라와 로그인/로그아웃 REST API를 구현한다.

Purpose: Phase 2의 모든 보안 기능이 이 인프라 위에 구축된다. SecurityConfig RBAC, UserDetailsService, 계정 잠금, 세션 관리가 핵심.
Output: 인증 API (login/logout/me), User 엔티티, SecurityConfig RBAC, 초기 admin 계정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rbac/02-CONTEXT.md
@.planning/phases/02-rbac/02-RESEARCH.md

<interfaces>
<!-- Phase 1 산출물 -- 수정/재사용할 기존 코드 -->

From backend/src/main/java/com/navy/casualty/common/dto/ApiResponse.java:
```java
public static <T> ApiResponse<T> ok(T data)
public static <T> ApiResponse<T> error(int status, String message)
```

From backend/src/main/java/com/navy/casualty/common/entity/BaseAuditEntity.java:
```java
@Getter @MappedSuperclass @EntityListeners(AuditingEntityListener.class)
public abstract class BaseAuditEntity {
    private LocalDateTime createdAt;
    private LocalDateTime updatedAt;
    private String createdBy;
    private String lastModifiedBy;
    private LocalDateTime deletedAt;
    private String deletedBy;
    private String deleteReason;
    public void softDelete(String deletedBy, String reason)
}
```

From V8__create_users_roles.sql (TB_USER 스키마):
```sql
CREATE TABLE TB_USER (
    id BIGSERIAL PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    name VARCHAR(50) NOT NULL,
    role VARCHAR(20) NOT NULL DEFAULT 'VIEWER',
    enabled BOOLEAN NOT NULL DEFAULT TRUE,
    account_locked BOOLEAN NOT NULL DEFAULT FALSE,
    failed_login_count INT NOT NULL DEFAULT 0,
    last_login_at TIMESTAMP,
    -- BaseAuditEntity 필드들 ...
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Security 인프라 + User 엔티티 + 인증 서비스</name>
  <files>
    backend/build.gradle.kts,
    backend/src/main/java/com/navy/casualty/security/CustomUserDetails.java,
    backend/src/main/java/com/navy/casualty/security/CustomUserDetailsService.java,
    backend/src/main/java/com/navy/casualty/security/SecurityConfig.java,
    backend/src/main/java/com/navy/casualty/security/SecurityUtils.java,
    backend/src/main/java/com/navy/casualty/user/entity/User.java,
    backend/src/main/java/com/navy/casualty/user/repository/UserRepository.java,
    backend/src/main/java/com/navy/casualty/user/service/UserService.java,
    backend/src/main/java/com/navy/casualty/auth/service/AuthService.java,
    backend/src/main/java/com/navy/casualty/common/config/JpaAuditingConfig.java,
    backend/src/main/java/com/navy/casualty/common/config/SecurityConfig.java,
    backend/src/test/java/com/navy/casualty/auth/AuthServiceTest.java
  </files>
  <read_first>
    backend/build.gradle.kts,
    backend/src/main/java/com/navy/casualty/common/config/SecurityConfig.java,
    backend/src/main/java/com/navy/casualty/common/config/JpaAuditingConfig.java,
    backend/src/main/java/com/navy/casualty/common/entity/BaseAuditEntity.java,
    backend/src/main/java/com/navy/casualty/common/dto/ApiResponse.java,
    backend/src/main/resources/db/migration/V8__create_users_roles.sql,
    backend/src/main/resources/application.yml
  </read_first>
  <behavior>
    - AuthService.incrementFailCount("user1") 호출 5번 후 accountLocked = true
    - AuthService.resetFailCount("user1") 호출 시 failedLoginCount = 0, accountLocked = false
    - AuthService.unlockAccount(userId) 호출 시 accountLocked = false, failedLoginCount = 0
    - CustomUserDetailsService.loadUserByUsername("admin") 시 UserDetails 반환
    - CustomUserDetailsService.loadUserByUsername("nonexist") 시 UsernameNotFoundException
    - SecurityUtils.getCurrentUsername() -- SecurityContext에 인증 있으면 username 반환
  </behavior>
  <action>
    1. **build.gradle.kts 의존성 추가:**
       ```kotlin
       implementation("org.springframework.boot:spring-boot-starter-aop")
       testImplementation("org.springframework.security:spring-security-test")
       ```

    2. **기존 SecurityConfig 삭제 및 새 패키지로 이동:**
       - `backend/src/main/java/com/navy/casualty/common/config/SecurityConfig.java` 삭제 (기존 permitAll)
       - `backend/src/main/java/com/navy/casualty/security/SecurityConfig.java` 생성

    3. **SecurityConfig.java** (security 패키지):
       - `@Configuration @EnableWebSecurity @EnableMethodSecurity`
       - filterChain: csrf.disable(), authorizeHttpRequests에서:
         - `/api/auth/login` -> permitAll
         - `/api/auth/logout` -> permitAll
         - `/api/admin/**` -> hasRole("ADMIN")
         - `/api/**` -> authenticated
         - anyRequest -> permitAll (정적 리소스)
       - sessionManagement: IF_REQUIRED, maximumSessions(1), maxSessionsPreventsLogin(false), sessionRegistry(sessionRegistry())
       - exceptionHandling: 401 JSON `{"success":false,"code":401,"message":"인증이 필요합니다"}`, 403 JSON `{"success":false,"code":403,"message":"접근 권한이 없습니다"}`
       - formLogin(disable), httpBasic(disable)
       - @Bean SessionRegistry -> SessionRegistryImpl
       - @Bean RoleHierarchy -> `RoleHierarchyImpl.fromHierarchy("ROLE_ADMIN > ROLE_MANAGER \n ROLE_MANAGER > ROLE_OPERATOR \n ROLE_OPERATOR > ROLE_VIEWER")`
       - @Bean MethodSecurityExpressionHandler -> setRoleHierarchy
       - @Bean PasswordEncoder -> BCryptPasswordEncoder(12)
       - @Bean AuthenticationManager -> config.getAuthenticationManager()

    4. **User.java** (user/entity):
       - @Entity @Table(name = "TB_USER") @SQLRestriction("deleted_at IS NULL")
       - extends BaseAuditEntity
       - 필드: id(Long, @GeneratedValue IDENTITY), username, password, name, role(String), enabled(boolean), accountLocked(boolean), failedLoginCount(int), lastLoginAt(LocalDateTime)
       - role 값: "ADMIN", "MANAGER", "OPERATOR", "VIEWER"

    5. **UserRepository.java**: `Optional<User> findByUsername(String username)`

    6. **CustomUserDetails.java** (security):
       - implements UserDetails
       - 필드: id(Long), username, password, name, role(String), enabled, accountLocked
       - getAuthorities() -> `List.of(new SimpleGrantedAuthority("ROLE_" + role))`
       - isAccountNonLocked() -> !accountLocked
       - isEnabled() -> enabled
       - static from(User user) 팩토리 메서드

    7. **CustomUserDetailsService.java** (security):
       - implements UserDetailsService
       - loadUserByUsername: userRepository.findByUsername() -> CustomUserDetails.from(user)
       - 없으면 UsernameNotFoundException("사용자를 찾을 수 없습니다") -- 이 메시지는 내부용, 외부에는 D-02 통일 메시지

    8. **AuthService.java** (auth/service):
       - incrementFailCount(username): failed_login_count +1, 5 이상이면 account_locked=true
       - resetFailCount(username): failed_login_count=0, account_locked=false, last_login_at=now
       - unlockAccount(userId): account_locked=false, failed_login_count=0

    9. **UserService.java** (user/service):
       - findById(Long id): User 반환, 없으면 IllegalArgumentException
       - findAll(): List<User> 반환
       - createUser(username, password, name, role): BCrypt 해싱 후 저장
       - updateRole(userId, role): 역할 변경
       - toggleEnabled(userId): 활성/비활성 토글

    10. **SecurityUtils.java** (security):
        - static getCurrentUsername(): Optional<String> -- SecurityContextHolder에서 추출
        - static getCurrentUser(): CustomUserDetails -- SecurityContextHolder에서 추출, 없으면 throw

    11. **JpaAuditingConfig.java 수정** (common/config):
        - AuditorAware를 SecurityContextHolder 기반으로 교체
        - `SecurityContextHolder.getContext().getAuthentication()` -> username 반환
        - 인증 없으면 "SYSTEM" 반환 (마이그레이션 등)

    12. **AuthServiceTest.java** (테스트):
        - @SpringBootTest + @Transactional + H2
        - 5회 실패 후 잠금 테스트
        - resetFailCount 테스트
        - unlockAccount 테스트
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*AuthServiceTest*" -x bootJar</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/main/java/com/navy/casualty/security/SecurityConfig.java 파일에 `maximumSessions(1)` 포함
    - backend/src/main/java/com/navy/casualty/security/SecurityConfig.java 파일에 `RoleHierarchyImpl.fromHierarchy` 포함
    - backend/src/main/java/com/navy/casualty/security/SecurityConfig.java 파일에 `BCryptPasswordEncoder(12)` 포함
    - backend/src/main/java/com/navy/casualty/user/entity/User.java 파일에 `@Table(name = "TB_USER")` 포함
    - backend/src/main/java/com/navy/casualty/security/CustomUserDetailsService.java 파일에 `implements UserDetailsService` 포함
    - backend/src/main/java/com/navy/casualty/auth/service/AuthService.java 파일에 `incrementFailCount` 메서드 포함
    - backend/src/main/java/com/navy/casualty/common/config/JpaAuditingConfig.java 파일에 `SecurityContextHolder` 포함
    - backend/src/main/java/com/navy/casualty/common/config/SecurityConfig.java 삭제됨 (security 패키지로 이동)
    - build.gradle.kts에 `spring-boot-starter-aop` 포함
    - build.gradle.kts에 `spring-security-test` 포함
    - `./gradlew test --tests "*AuthServiceTest*"` 종료코드 0
  </acceptance_criteria>
  <done>
    SecurityConfig RBAC 규칙, User 엔티티, CustomUserDetailsService, AuthService(잠금 로직), SecurityUtils, JpaAuditingConfig 인증 연동 완료. 
    AuthServiceTest 통과 (5회 잠금, 리셋, 해제).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 인증 REST API (login/logout/me) + 강제 로그아웃 + 초기 admin 계정</name>
  <files>
    backend/src/main/java/com/navy/casualty/auth/controller/AuthController.java,
    backend/src/main/java/com/navy/casualty/auth/dto/LoginRequest.java,
    backend/src/main/java/com/navy/casualty/auth/dto/LoginResponse.java,
    backend/src/main/java/com/navy/casualty/common/exception/GlobalExceptionHandler.java,
    backend/src/main/resources/db/migration/V10__insert_admin_user.sql,
    backend/src/test/java/com/navy/casualty/auth/AuthControllerTest.java
  </files>
  <read_first>
    backend/src/main/java/com/navy/casualty/common/dto/ApiResponse.java,
    backend/src/main/java/com/navy/casualty/auth/service/AuthService.java,
    backend/src/main/java/com/navy/casualty/security/SecurityConfig.java,
    backend/src/main/java/com/navy/casualty/security/CustomUserDetails.java,
    backend/src/main/java/com/navy/casualty/security/SecurityUtils.java,
    backend/src/main/java/com/navy/casualty/common/exception/GlobalExceptionHandler.java
  </read_first>
  <behavior>
    - POST /api/auth/login {username:"admin", password:"admin1234"} -> 200 + {username, name, role}
    - POST /api/auth/login {username:"admin", password:"wrong"} -> 401 + "사용자 ID 또는 비밀번호가 올바르지 않습니다"
    - POST /api/auth/login 잠금된 계정 -> 401 + 동일 메시지 (D-02)
    - POST /api/auth/logout -> 200 + 세션 무효화
    - GET /api/auth/me (인증됨) -> 200 + 사용자 정보
    - GET /api/auth/me (미인증) -> 401
    - POST /api/admin/users/{id}/force-logout (ADMIN) -> 200
    - POST /api/admin/users/{id}/force-logout (NON-ADMIN) -> 403
  </behavior>
  <action>
    1. **LoginRequest.java** (auth/dto):
       ```java
       public record LoginRequest(
           @NotBlank(message = "사용자 ID를 입력하세요") String username,
           @NotBlank(message = "비밀번호를 입력하세요") String password
       ) {}
       ```

    2. **LoginResponse.java** (auth/dto):
       ```java
       public record LoginResponse(String username, String name, String role) {
           public static LoginResponse from(CustomUserDetails user) {
               return new LoginResponse(user.getUsername(), user.getName(),
                   user.getRole());
           }
       }
       ```

    3. **AuthController.java** (auth/controller):
       - @RestController @RequestMapping("/api/auth")
       - 주입: AuthenticationManager, AuthService, SessionRegistry
       - **POST /login**: AuthenticationManager.authenticate(UsernamePasswordAuthenticationToken) 후 SecurityContext를 세션에 저장. 성공 시 resetFailCount, 실패 시 incrementFailCount. catch(AuthenticationException) -> ApiResponse.error(401, "사용자 ID 또는 비밀번호가 올바르지 않습니다") -- D-02 준수. LockedException도 동일 메시지.
       - **POST /logout**: session.invalidate() + SecurityContextHolder.clearContext() -> ApiResponse.ok(null)
       - **GET /me**: SecurityUtils.getCurrentUser() -> LoginResponse.from() -> ApiResponse.ok()

    4. **강제 로그아웃 (AUTH-07)**: AuthController 또는 별도 AdminController에:
       - POST /api/admin/users/{userId}/force-logout
       - @PreAuthorize("hasRole('ADMIN')")
       - SessionRegistry.getAllSessions(username, false).forEach(SessionInformation::expireNow)

    5. **GlobalExceptionHandler.java 수정**:
       - `@ExceptionHandler(AccessDeniedException.class)` 추가 -> 403 + "접근 권한이 없습니다"
       - `@ExceptionHandler(AuthenticationException.class)` 추가 -> 401 + "인증이 필요합니다"

    6. **V10__insert_admin_user.sql**:
       ```sql
       -- 초기 관리자 계정 (비밀번호: admin1234, BCrypt(12) 사전 해싱)
       INSERT INTO TB_USER (username, password, name, role, enabled, account_locked, failed_login_count, created_by)
       VALUES (
           'admin',
           '$2a$12$LJ3m4ys3uz2YHIxPM0Y5S.3TS8VGA6IjDg78B1sGFJpBksmB/VxDy',
           '시스템관리자',
           'ADMIN',
           TRUE,
           FALSE,
           0,
           'SYSTEM'
       );
       ```
       (주: BCrypt 해시는 빌드 시 `echo -n "admin1234" | htpasswd -nbBC 12 "" -` 또는 Java 코드로 생성 필요. 실행자가 직접 BCrypt(12) 해시를 생성하여 삽입할 것.)

    7. **AuthControllerTest.java**:
       - @SpringBootTest @AutoConfigureMockMvc @Transactional
       - 로그인 성공 테스트 (admin/admin1234 -> 200)
       - 로그인 실패 테스트 (wrong password -> 401 + D-02 메시지)
       - 미인증 /api/auth/me -> 401
       - 5회 실패 후 잠금 -> 동일 401 메시지 (D-02)
       - @WithMockUser(roles="ADMIN") 강제 로그아웃 테스트
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*AuthControllerTest*" -x bootJar</automated>
  </verify>
  <acceptance_criteria>
    - AuthController.java에 `@PostMapping("/login")` 포함
    - AuthController.java에 `@PostMapping("/logout")` 포함
    - AuthController.java에 `@GetMapping("/me")` 포함
    - AuthController.java 또는 별도 컨트롤러에 `/force-logout` 엔드포인트 포함
    - LoginRequest.java에 `@NotBlank` 포함
    - AuthController.java에 `"사용자 ID 또는 비밀번호가 올바르지 않습니다"` 문자열 포함 (D-02)
    - V10__insert_admin_user.sql에 `INSERT INTO TB_USER` 포함
    - V10__insert_admin_user.sql에 `'admin'` 포함
    - GlobalExceptionHandler.java에 `AccessDeniedException` 핸들러 포함
    - `./gradlew test --tests "*AuthControllerTest*"` 종료코드 0
  </acceptance_criteria>
  <done>
    POST /api/auth/login, POST /api/auth/logout, GET /api/auth/me API 동작.
    D-02 에러 메시지 통일 (잠금 포함 모두 동일 메시지).
    AUTH-07 강제 로그아웃 API 동작.
    초기 admin 계정 Flyway 마이그레이션 존재.
    AuthControllerTest 전체 통과.
  </done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew test --tests "*Auth*"` 전체 통과
- POST /api/auth/login + POST /api/auth/logout + GET /api/auth/me 정상 동작
- 5회 실패 잠금 동작 확인
- 동시 세션 1개 제한 설정 존재
</verification>

<success_criteria>
- SecurityConfig가 RBAC 규칙으로 완전 교체됨
- 인증 API 3개 엔드포인트 동작
- 계정 잠금 (5회) 동작
- 초기 admin 계정 존재
- 모든 Auth 관련 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/02-rbac/02-01-SUMMARY.md`
</output>
