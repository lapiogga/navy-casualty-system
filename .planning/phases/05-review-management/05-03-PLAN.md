---
phase: 05-review-management
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - backend/src/main/java/com/navy/casualty/review/service/ReviewExcelService.java
  - backend/src/main/java/com/navy/casualty/review/controller/ReviewController.java
  - frontend/src/api/review.ts
  - frontend/src/pages/review/ReviewListPage.tsx
autonomous: true
requirements: [REVW-05]

must_haves:
  truths:
    - "GET /api/reviews/excel 이 검색 조건 적용된 Excel 파일을 다운로드한다"
    - "Excel 파일에서 주민번호가 역할별로 마스킹되어 있다"
    - "Excel 파일에 심사차수, 심사일자, 병명, 소속부대 심사결과, 분류, 보훈청 통보일 컬럼이 포함되어 있다"
    - "프론트엔드 Excel 다운로드 버튼이 Blob으로 파일을 받아 다운로드 트리거한다"
  artifacts:
    - path: "backend/src/main/java/com/navy/casualty/review/service/ReviewExcelService.java"
      provides: "SXSSFWorkbook Excel 생성 (심사 고유 컬럼 포함)"
      contains: "SXSSFWorkbook"
    - path: "frontend/src/api/review.ts"
      provides: "useExportReviewExcel 훅"
      contains: "useExportReviewExcel"
  key_links:
    - from: "ReviewController"
      to: "ReviewExcelService"
      via: "DI injection"
      pattern: "reviewExcelService"
    - from: "ReviewExcelService"
      to: "RrnMaskingUtil"
      via: "주민번호 마스킹"
      pattern: "RrnMaskingUtil\\.mask"
    - from: "ReviewListPage.tsx"
      to: "api/review.ts useExportReviewExcel"
      via: "Excel 다운로드 버튼"
      pattern: "useExportReviewExcel"
---

<objective>
전공사상심사 Excel 내보내기 기능을 구현한다: SXSSFWorkbook 기반 ReviewExcelService, Controller 엔드포인트, 프론트엔드 다운로드 버튼. Phase 3/4 DeadExcelService/WoundedExcelService 패턴을 복제하되 심사 고유 컬럼을 반영한다.

Purpose: REVW-05 요구사항. 검색 조건이 적용된 전공사상심사 현황을 Excel로 내보내되, 주민번호는 역할별 마스킹 적용.
Output: GET /api/reviews/excel 엔드포인트 + 프론트엔드 다운로드 버튼
</objective>

<execution_context>
@C:\Users\User\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\User\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-review-management/05-01-SUMMARY.md

<interfaces>
<!-- 05-01에서 생성된 인터페이스 -->

From ReviewRepositoryCustom.java (05-01):
```java
public interface ReviewRepositoryCustom {
    Page<Review> search(ReviewSearchRequest request, Pageable pageable);
    List<Review> searchAll(ReviewSearchRequest request);
}
```

From ReviewController.java (05-01):
```java
@RestController @RequestMapping("/api/reviews")
// GET /excel 엔드포인트 추가 필요
```

From Review.java (05-01):
```java
// 필드: reviewRound, reviewDate, name, serviceNumber, ssnEncrypted, ssnHash,
//       birthDate, rankId, branchId, unitId, enlistmentDate, diseaseName,
//       unitReviewResult, classification(ReviewClassification), notificationDate, status
```

From DeadExcelService.java / WoundedExcelService.java (참조 패턴):
```java
@Service @RequiredArgsConstructor @Transactional(readOnly = true)
// SXSSFWorkbook(100) + 코드 테이블 Map 캐시 + RrnMaskingUtil.mask + @AuditLog(EXPORT)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReviewExcelService + Controller Excel 엔드포인트</name>
  <files>
    backend/src/main/java/com/navy/casualty/review/service/ReviewExcelService.java
    backend/src/main/java/com/navy/casualty/review/controller/ReviewController.java
  </files>
  <read_first>
    backend/src/main/java/com/navy/casualty/dead/service/DeadExcelService.java
    backend/src/main/java/com/navy/casualty/wounded/service/WoundedExcelService.java
    backend/src/main/java/com/navy/casualty/review/entity/Review.java
    backend/src/main/java/com/navy/casualty/review/entity/ReviewClassification.java
    backend/src/main/java/com/navy/casualty/review/dto/ReviewSearchRequest.java
    backend/src/main/java/com/navy/casualty/review/repository/ReviewRepository.java
    backend/src/main/java/com/navy/casualty/review/controller/ReviewController.java
    backend/src/main/java/com/navy/casualty/common/crypto/RrnMaskingUtil.java
    backend/src/main/java/com/navy/casualty/audit/annotation/AuditLog.java
  </read_first>
  <action>
    1. ReviewExcelService.java 생성 (DeadExcelService/WoundedExcelService 복제 + 심사 고유 컬럼):
       ```java
       package com.navy.casualty.review.service;

       @Service @RequiredArgsConstructor @Transactional(readOnly = true)
       public class ReviewExcelService {
           private final ReviewRepository reviewRepository;
           private final RankCodeRepository rankCodeRepository;
           private final BranchCodeRepository branchCodeRepository;
           private final UnitCodeRepository unitCodeRepository;

           private static final String[] HEADERS = {
               "번호", "심사차수", "심사일자", "군구분", "군번", "성명",
               "주민등록번호", "생년월일", "계급", "소속", "입대일자",
               "병명", "소속부대 심사결과", "분류", "상태", "보훈청 통보일"
           };

           @AuditLog(action = "EXPORT", targetTable = "TB_REVIEW")
           public void exportExcel(ReviewSearchRequest request, HttpServletResponse response) throws IOException {
               String userRole = getCurrentUserRole();
               List<Review> list = reviewRepository.searchAll(request);

               // 코드 테이블 Map 캐시 (N+1 방지)
               Map<Long, String> rankMap = rankCodeRepository.findAll().stream()
                   .collect(Collectors.toMap(RankCode::getId, RankCode::getRankName));
               Map<Long, String> branchMap = branchCodeRepository.findAll().stream()
                   .collect(Collectors.toMap(BranchCode::getId, BranchCode::getBranchName));
               Map<Long, String> unitMap = unitCodeRepository.findAll().stream()
                   .collect(Collectors.toMap(UnitCode::getId, UnitCode::getUnitName));

               try (SXSSFWorkbook workbook = new SXSSFWorkbook(100)) {
                   Sheet sheet = workbook.createSheet("전공사상심사 현황");
                   Row headerRow = sheet.createRow(0);
                   CellStyle headerStyle = createHeaderStyle(workbook);
                   for (int i = 0; i < HEADERS.length; i++) {
                       Cell cell = headerRow.createCell(i);
                       cell.setCellValue(HEADERS[i]);
                       cell.setCellStyle(headerStyle);
                   }

                   for (int i = 0; i < list.size(); i++) {
                       Review r = list.get(i);
                       Row row = sheet.createRow(i + 1);
                       int col = 0;
                       row.createCell(col++).setCellValue(i + 1);                                          // 번호
                       row.createCell(col++).setCellValue(r.getReviewRound());                             // 심사차수
                       row.createCell(col++).setCellValue(r.getReviewDate() != null ? r.getReviewDate().toString() : "");  // 심사일자
                       row.createCell(col++).setCellValue(r.getBranchId() != null ? branchMap.getOrDefault(r.getBranchId(), "") : "");  // 군구분
                       row.createCell(col++).setCellValue(r.getServiceNumber());                           // 군번
                       row.createCell(col++).setCellValue(r.getName());                                    // 성명
                       row.createCell(col++).setCellValue(RrnMaskingUtil.mask(r.getSsnEncrypted(), userRole));  // 주민번호(마스킹)
                       row.createCell(col++).setCellValue(r.getBirthDate() != null ? r.getBirthDate().toString() : "");  // 생년월일
                       row.createCell(col++).setCellValue(r.getRankId() != null ? rankMap.getOrDefault(r.getRankId(), "") : "");  // 계급
                       row.createCell(col++).setCellValue(r.getUnitId() != null ? unitMap.getOrDefault(r.getUnitId(), "") : "");  // 소속
                       row.createCell(col++).setCellValue(r.getEnlistmentDate() != null ? r.getEnlistmentDate().toString() : "");  // 입대일자
                       row.createCell(col++).setCellValue(r.getDiseaseName() != null ? r.getDiseaseName() : "");  // 병명
                       row.createCell(col++).setCellValue(r.getUnitReviewResult() != null ? r.getUnitReviewResult() : "");  // 소속부대 심사결과
                       row.createCell(col++).setCellValue(r.getClassification() != null ? r.getClassification().getLabel() : "");  // 분류(한글)
                       row.createCell(col++).setCellValue(r.getStatus().name());                           // 상태
                       row.createCell(col++).setCellValue(r.getNotificationDate() != null ? r.getNotificationDate().toString() : "");  // 보훈청 통보일
                   }

                   response.setContentType("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet");
                   response.setHeader("Content-Disposition", "attachment; filename=review_list_" + LocalDate.now() + ".xlsx");
                   workbook.write(response.getOutputStream());
               }
           }

           private String getCurrentUserRole() { /* SecurityContextHolder 패턴 - DeadExcelService와 동일 */ }
           private CellStyle createHeaderStyle(SXSSFWorkbook workbook) { /* DeadExcelService와 동일 */ }
       }
       ```

    2. ReviewController.java 수정 (GET /excel 엔드포인트 추가):
       - ReviewExcelService 의존성을 생성자 주입에 추가
       ```java
       @GetMapping("/excel")
       @PreAuthorize("hasRole('VIEWER')")
       public void exportExcel(ReviewSearchRequest request, HttpServletResponse response) throws IOException {
           reviewExcelService.exportExcel(request, response);
       }
       ```
  </action>
  <verify>
    <automated>cd backend && ./gradlew compileJava -x processAot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/main/java/com/navy/casualty/review/service/ReviewExcelService.java 존재
    - grep "SXSSFWorkbook" ReviewExcelService.java 결과 있음
    - grep "RrnMaskingUtil.mask" ReviewExcelService.java 결과 있음
    - grep "@AuditLog.*EXPORT.*TB_REVIEW" ReviewExcelService.java 결과 있음
    - grep "전공사상심사 현황" ReviewExcelService.java 결과 있음
    - grep "심사차수" ReviewExcelService.java 결과 있음
    - grep "소속부대 심사결과" ReviewExcelService.java 결과 있음
    - grep "분류" ReviewExcelService.java 결과 있음
    - grep "보훈청 통보일" ReviewExcelService.java 결과 있음
    - grep "getLabel" ReviewExcelService.java 결과 있음 (ReviewClassification.getLabel())
    - grep "/excel" ReviewController.java 결과 있음
    - grep "reviewExcelService" ReviewController.java 결과 있음
    - ./gradlew compileJava 성공
  </acceptance_criteria>
  <done>SXSSFWorkbook 기반 전공사상심사 Excel 내보내기 서비스 완성. 심사차수/심사일자/병명/소속부대심사결과/분류/보훈청통보일 컬럼 포함, 주민번호 역할별 마스킹, 감사로그 EXPORT 기록.</done>
</task>

<task type="auto">
  <name>Task 2: 프론트엔드 Excel 다운로드 훅 + ReviewListPage 다운로드 버튼 연결</name>
  <files>
    frontend/src/api/review.ts
    frontend/src/pages/review/ReviewListPage.tsx
  </files>
  <read_first>
    frontend/src/api/review.ts
    frontend/src/pages/review/ReviewListPage.tsx
    frontend/src/api/dead.ts
    frontend/src/api/wounded.ts
    frontend/src/pages/dead/DeadListPage.tsx
    frontend/src/api/client.ts
    frontend/src/types/review.ts
  </read_first>
  <action>
    1. frontend/src/api/review.ts에 useExportReviewExcel 훅 추가 (dead.ts useExportDeadExcel 복제):
       ```typescript
       export function useExportReviewExcel() {
         return useMutation({
           mutationFn: async (params: ReviewSearchParams) => {
             const res = await apiClient.get('/reviews/excel', {
               params,
               responseType: 'blob',
             });
             const url = window.URL.createObjectURL(new Blob([res.data]));
             const link = document.createElement('a');
             link.href = url;
             link.download = `review_list_${new Date().toISOString().slice(0, 10)}.xlsx`;
             document.body.appendChild(link);
             link.click();
             document.body.removeChild(link);
             window.URL.revokeObjectURL(url);
           },
           onSuccess: () => message.success('Excel 다운로드가 완료되었습니다'),
           onError: () => message.error('Excel 다운로드에 실패했습니다'),
         });
       }
       ```

    2. frontend/src/pages/review/ReviewListPage.tsx 수정:
       - import { useExportReviewExcel } from '../../api/review' 추가
       - const exportExcel = useExportReviewExcel(); 선언
       - 기존 disabled placeholder 버튼을 실제 버튼으로 교체:
         ```tsx
         <Button
           icon={<DownloadOutlined />}
           onClick={() => exportExcel.mutate(searchParams)}
           loading={exportExcel.isPending}
         >
           Excel 다운로드
         </Button>
         ```
       - DownloadOutlined 아이콘이 이미 import되어 있는지 확인, 없으면 추가
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "useExportReviewExcel" frontend/src/api/review.ts 결과 있음
    - grep "responseType.*blob" frontend/src/api/review.ts 결과 있음
    - grep "review_list_" frontend/src/api/review.ts 결과 있음
    - grep "useExportReviewExcel" frontend/src/pages/review/ReviewListPage.tsx 결과 있음
    - grep "DownloadOutlined" frontend/src/pages/review/ReviewListPage.tsx 결과 있음
    - npx tsc --noEmit 에러 없음
  </acceptance_criteria>
  <done>프론트엔드 전공사상심사 Excel 다운로드 기능 완성. Blob 응답 처리 + 파일 다운로드 트리거 + 로딩 상태 표시.</done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew compileJava -x processAot` 컴파일 성공
- `cd frontend && npx tsc --noEmit` 에러 없음
- ReviewExcelService에 SXSSFWorkbook + RrnMaskingUtil.mask + @AuditLog(EXPORT)
- ReviewListPage에 Excel 다운로드 버튼이 활성 상태
</verification>

<success_criteria>
- GET /api/reviews/excel 이 검색 조건 적용된 .xlsx 파일을 반환
- Excel에 심사차수, 심사일자, 병명, 소속부대 심사결과, 분류, 보훈청 통보일 컬럼 포함
- Excel에 주민번호 역할별 마스킹 적용
- 프론트엔드에서 Excel 다운로드 버튼 클릭 시 파일 다운로드
- 감사 로그에 EXPORT 액션 기록
</success_criteria>

<output>
After completion, create `.planning/phases/05-review-management/05-03-SUMMARY.md`
</output>
