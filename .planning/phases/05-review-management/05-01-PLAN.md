---
phase: 05-review-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/db/migration/V16__add_review_columns.sql
  - backend/src/main/java/com/navy/casualty/review/entity/Review.java
  - backend/src/main/java/com/navy/casualty/review/entity/ReviewClassification.java
  - backend/src/main/java/com/navy/casualty/review/entity/ReviewStatus.java
  - backend/src/main/java/com/navy/casualty/review/entity/ReviewHistory.java
  - backend/src/main/java/com/navy/casualty/review/dto/ReviewSnapshot.java
  - backend/src/main/java/com/navy/casualty/review/dto/ReviewCreateRequest.java
  - backend/src/main/java/com/navy/casualty/review/dto/ReviewUpdateRequest.java
  - backend/src/main/java/com/navy/casualty/review/dto/ReviewSearchRequest.java
  - backend/src/main/java/com/navy/casualty/review/dto/ReviewResponse.java
  - backend/src/main/java/com/navy/casualty/review/dto/ReviewDeleteRequest.java
  - backend/src/main/java/com/navy/casualty/review/dto/ReviewHistoryResponse.java
  - backend/src/main/java/com/navy/casualty/review/repository/ReviewRepository.java
  - backend/src/main/java/com/navy/casualty/review/repository/ReviewRepositoryCustom.java
  - backend/src/main/java/com/navy/casualty/review/repository/ReviewRepositoryImpl.java
  - backend/src/main/java/com/navy/casualty/review/repository/ReviewHistoryRepository.java
  - backend/src/main/java/com/navy/casualty/review/service/ReviewService.java
  - backend/src/main/java/com/navy/casualty/review/controller/ReviewController.java
  - backend/src/test/java/com/navy/casualty/review/ReviewClassificationTest.java
  - backend/src/test/java/com/navy/casualty/review/ReviewStatusTest.java
autonomous: true
requirements: [REVW-01, REVW-02, REVW-03, REVW-04, REVW-06, REVW-07, REVW-08, AUDIT-07]

must_haves:
  truths:
    - "GET /api/reviews?name=홍길동&page=0&size=10 이 검색 조건에 맞는 심사 목록을 페이징으로 반환한다"
    - "POST /api/reviews 로 심사를 등록하면 201과 ReviewResponse를 반환한다"
    - "PUT /api/reviews/{id} 로 심사 정보를 수정하면 변경 전 스냅샷이 TB_REVIEW_HISTORY에 JSONB로 자동 저장된다"
    - "DELETE /api/reviews/{id} 로 논리 삭제 시 삭제 사유가 필수이다"
    - "동일 군번+심사차수 조합으로 중복 등록 시 409 에러를 반환한다"
    - "GET /api/reviews/{id}/histories 로 심사차수별 이력을 시간순으로 조회할 수 있다"
    - "PUT /api/reviews/{id}/notify 로 보훈청 통보 일시를 기록할 수 있다"
    - "심사 결과(classification) 변경 시 해당 군번의 Dead/Wounded 레코드가 자동 갱신된다"
    - "상태 전이가 REGISTERED->UNDER_REVIEW->CONFIRMED->NOTIFIED 순서로만 가능하다"
  artifacts:
    - path: "backend/src/main/java/com/navy/casualty/review/entity/Review.java"
      provides: "전공사상심사 JPA 엔티티"
      contains: "@Entity"
    - path: "backend/src/main/java/com/navy/casualty/review/entity/ReviewClassification.java"
      provides: "전공상/공상/기각/보류 4-value enum"
      contains: "COMBAT_WOUND"
    - path: "backend/src/main/java/com/navy/casualty/review/entity/ReviewHistory.java"
      provides: "심사 이력 JSONB 스냅샷 엔티티"
      contains: "snapshot"
    - path: "backend/src/main/java/com/navy/casualty/review/service/ReviewService.java"
      provides: "비즈니스 로직 (CRUD + 이력 저장 + 자동반영 + 보훈청 통보)"
      contains: "@AuditLog"
    - path: "backend/src/main/java/com/navy/casualty/review/controller/ReviewController.java"
      provides: "REST API 엔드포인트"
      contains: "@RestController"
  key_links:
    - from: "ReviewService"
      to: "ReviewHistoryRepository"
      via: "수정 시 변경 전 스냅샷 저장"
      pattern: "reviewHistoryRepository\\.save"
    - from: "ReviewService"
      to: "DeadRepository"
      via: "COMBAT_WOUND시 Dead.updateDeathType() + 상태 CONFIRMED 전이, findByServiceNumber로 매칭"
      pattern: "deadRepository\.findByServiceNumber"
    - from: "ReviewService"
      to: "WoundedRepository"
      via: "COMBAT_WOUND/DUTY_WOUND시 Wounded.updateWoundType() + 상태 CONFIRMED 전이, findByServiceNumber로 매칭"
      pattern: "woundedRepository\.findByServiceNumber"
    - from: "ReviewController"
      to: "ReviewService"
      via: "DI injection"
      pattern: "private final ReviewService"
    - from: "Review.java"
      to: "BaseAuditEntity"
      via: "extends"
      pattern: "extends BaseAuditEntity"
---

<objective>
전공사상심사 백엔드 전체를 구현한다: Review/ReviewHistory 엔티티, ReviewClassification(4-value) + ReviewStatus(4단계) enum, QueryDSL 동적 검색, 이력 스냅샷 자동 저장, 심사 결과->Dead/Wounded 자동 반영, 보훈청 통보 일시 기록, Flyway V16 마이그레이션.

Purpose: Phase 5 핵심 백엔드. Phase 3/4 Dead/Wounded 패턴을 복제하면서 (1) TB_REVIEW_HISTORY JSONB 스냅샷 이력 저장 (2) 크로스-도메인 자동 반영 로직 두 가지 신규 요소를 추가한다.
Output: /api/reviews CRUD + /api/reviews/{id}/histories 이력조회 + /api/reviews/{id}/notify 보훈청통보 엔드포인트
</objective>

<execution_context>
@C:\Users\User\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\User\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-review-management/05-RESEARCH.md

<interfaces>
<!-- 공통 인프라 (Phase 1/2에서 생성) -->

From backend/src/main/java/com/navy/casualty/common/entity/BaseAuditEntity.java:
```java
@MappedSuperclass
public abstract class BaseAuditEntity {
    private LocalDateTime createdAt;
    private LocalDateTime updatedAt;
    private String createdBy;
    private String lastModifiedBy;
    private LocalDateTime deletedAt;
    private String deletedBy;
    private String deleteReason;
    public void softDelete(String deletedBy, String reason) { ... }
}
```

From backend/src/main/java/com/navy/casualty/common/dto/ApiResponse.java:
```java
public class ApiResponse<T> {
    public static <T> ApiResponse<T> ok(T data) { ... }
    public static <T> ApiResponse<T> error(int status, String message) { ... }
}
```

From backend/src/main/java/com/navy/casualty/audit/annotation/AuditLog.java:
```java
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface AuditLog { String action(); String targetTable(); }
```

From backend/src/main/java/com/navy/casualty/common/crypto/PiiEncryptionConverter.java:
- @Convert(converter = PiiEncryptionConverter.class) 로 엔티티 필드에 적용

From DB V5 (TB_REVIEW, TB_REVIEW_HISTORY 이미 존재):
```sql
CREATE TABLE TB_REVIEW (
    id BIGSERIAL PRIMARY KEY,
    review_round INT NOT NULL, review_date DATE,
    name VARCHAR(50) NOT NULL, service_number VARCHAR(20) NOT NULL,
    ssn_encrypted TEXT, birth_date DATE, enlistment_date DATE,
    unit_id BIGINT REFERENCES TB_UNIT_CODE(id),
    disease_name VARCHAR(200), unit_review_result VARCHAR(20),
    classification VARCHAR(20), notification_date TIMESTAMP,
    -- audit 컬럼들...
);
CREATE TABLE TB_REVIEW_HISTORY (
    id BIGSERIAL PRIMARY KEY,
    review_id BIGINT NOT NULL REFERENCES TB_REVIEW(id),
    review_round INT NOT NULL, snapshot JSONB NOT NULL,
    changed_at TIMESTAMP NOT NULL DEFAULT NOW(), changed_by VARCHAR(50)
);
```

Note: V5에 rank_id, branch_id, ssn_hash 컬럼이 누락 -> V16에서 추가

From Dead/Wounded 엔티티 패턴:
- Dead: serviceNumber, deathTypeId 등
- Wounded: serviceNumber, woundType(COMBAT_WOUND/DUTY_WOUND/GENERAL_WOUND) 등
- DeadRepository.findByServiceNumber / WoundedRepository.findByServiceNumber 패턴
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flyway V16 + Review/ReviewHistory 엔티티 + ReviewClassification/ReviewStatus enum + DTO + Repository</name>
  <files>
    backend/src/main/resources/db/migration/V16__add_review_columns.sql
    backend/src/main/java/com/navy/casualty/review/entity/Review.java
    backend/src/main/java/com/navy/casualty/review/entity/ReviewClassification.java
    backend/src/main/java/com/navy/casualty/review/entity/ReviewStatus.java
    backend/src/main/java/com/navy/casualty/review/entity/ReviewHistory.java
    backend/src/main/java/com/navy/casualty/review/dto/ReviewSnapshot.java
    backend/src/main/java/com/navy/casualty/review/dto/ReviewCreateRequest.java
    backend/src/main/java/com/navy/casualty/review/dto/ReviewUpdateRequest.java
    backend/src/main/java/com/navy/casualty/review/dto/ReviewSearchRequest.java
    backend/src/main/java/com/navy/casualty/review/dto/ReviewResponse.java
    backend/src/main/java/com/navy/casualty/review/dto/ReviewDeleteRequest.java
    backend/src/main/java/com/navy/casualty/review/dto/ReviewHistoryResponse.java
    backend/src/main/java/com/navy/casualty/review/repository/ReviewRepository.java
    backend/src/main/java/com/navy/casualty/review/repository/ReviewRepositoryCustom.java
    backend/src/main/java/com/navy/casualty/review/repository/ReviewRepositoryImpl.java
    backend/src/main/java/com/navy/casualty/review/repository/ReviewHistoryRepository.java
    backend/src/test/java/com/navy/casualty/review/ReviewClassificationTest.java
    backend/src/test/java/com/navy/casualty/review/ReviewStatusTest.java
  </files>
  <read_first>
    backend/src/main/java/com/navy/casualty/dead/entity/Dead.java
    backend/src/main/java/com/navy/casualty/dead/entity/DeadStatus.java
    backend/src/main/java/com/navy/casualty/dead/dto/DeadCreateRequest.java
    backend/src/main/java/com/navy/casualty/dead/dto/DeadUpdateRequest.java
    backend/src/main/java/com/navy/casualty/dead/dto/DeadSearchRequest.java
    backend/src/main/java/com/navy/casualty/dead/dto/DeadResponse.java
    backend/src/main/java/com/navy/casualty/dead/dto/DeadDeleteRequest.java
    backend/src/main/java/com/navy/casualty/dead/repository/DeadRepository.java
    backend/src/main/java/com/navy/casualty/dead/repository/DeadRepositoryCustom.java
    backend/src/main/java/com/navy/casualty/dead/repository/DeadRepositoryImpl.java
    backend/src/main/java/com/navy/casualty/wounded/entity/Wounded.java
    backend/src/main/java/com/navy/casualty/wounded/entity/WoundedStatus.java
    backend/src/main/java/com/navy/casualty/common/entity/BaseAuditEntity.java
    backend/src/main/java/com/navy/casualty/common/crypto/PiiEncryptionConverter.java
    backend/src/main/resources/db/migration/V5__create_review_table.sql
    backend/src/main/resources/db/migration/V13__add_ssn_hash_column.sql
    backend/src/test/java/com/navy/casualty/dead/DeadStatusTest.java
  </read_first>
  <behavior>
    - ReviewClassification.COMBAT_WOUND.getLabel() == "전공상"
    - ReviewClassification.DUTY_WOUND.getLabel() == "공상"
    - ReviewClassification.REJECTED.getLabel() == "기각"
    - ReviewClassification.DEFERRED.getLabel() == "보류"
    - ReviewStatus.REGISTERED.canTransitionTo(UNDER_REVIEW) == true
    - ReviewStatus.UNDER_REVIEW.canTransitionTo(CONFIRMED) == true
    - ReviewStatus.CONFIRMED.canTransitionTo(NOTIFIED) == true
    - ReviewStatus.REGISTERED.canTransitionTo(CONFIRMED) == false (건너뛰기 불가)
    - ReviewStatus.NOTIFIED.canTransitionTo(어떤 값이든) == false
    - ReviewSnapshot.from(review)에 ssnEncrypted 필드가 포함되지 않음
  </behavior>
  <action>
    1. V16__add_review_columns.sql 생성:
       ```sql
       -- V16: TB_REVIEW ssn_hash + rank_id + branch_id 컬럼 추가
       -- ssn_hash: 주민번호 SHA-256 해시 (중복 검증용, Dead/Wounded와 동일 패턴)
       -- rank_id/branch_id: REVW-01 복합 검색 요구사항 충족용 (V5에서 누락)
       ALTER TABLE TB_REVIEW ADD COLUMN ssn_hash VARCHAR(64);
       ALTER TABLE TB_REVIEW ADD COLUMN rank_id BIGINT REFERENCES TB_RANK_CODE(id);
       ALTER TABLE TB_REVIEW ADD COLUMN branch_id BIGINT REFERENCES TB_BRANCH_CODE(id);
       ALTER TABLE TB_REVIEW ADD COLUMN status VARCHAR(20) NOT NULL DEFAULT 'REGISTERED';
       CREATE UNIQUE INDEX idx_review_ssn_hash ON TB_REVIEW (ssn_hash) WHERE deleted_at IS NULL;
       ```

    2. ReviewClassification.java 생성 (4-value enum):
       ```java
       package com.navy.casualty.review.entity;
       public enum ReviewClassification {
           COMBAT_WOUND("전공상"), DUTY_WOUND("공상"), REJECTED("기각"), DEFERRED("보류");
           private final String label;
           ReviewClassification(String label) { this.label = label; }
           public String getLabel() { return label; }
       }
       ```

    3. ReviewStatus.java 생성 (DeadStatus/WoundedStatus 동일 4단계 패턴):
       ```java
       package com.navy.casualty.review.entity;
       public enum ReviewStatus {
           REGISTERED, UNDER_REVIEW, CONFIRMED, NOTIFIED;
           public boolean canTransitionTo(ReviewStatus next) {
               return switch (this) {
                   case REGISTERED -> next == UNDER_REVIEW;
                   case UNDER_REVIEW -> next == CONFIRMED;
                   case CONFIRMED -> next == NOTIFIED;
                   case NOTIFIED -> false;
               };
           }
       }
       ```

    4. Review.java 생성 (Dead.java 패턴 기반, 심사 고유 필드 반영):
       - @Entity @Table(name = "TB_REVIEW") @SQLRestriction("deleted_at IS NULL")
       - extends BaseAuditEntity
       - 필드: reviewRound(int, not null), reviewDate(LocalDate), name, serviceNumber, ssnEncrypted(@Convert PiiEncryptionConverter), ssnHash, birthDate, rankId, branchId, unitId, enlistmentDate, diseaseName, unitReviewResult(String), classification(ReviewClassification @Enumerated STRING), notificationDate(LocalDateTime), status(ReviewStatus @Enumerated STRING, default REGISTERED)
       - update(ReviewUpdateRequest) 메서드: reviewRound, reviewDate, name, birthDate, rankId, branchId, unitId, enlistmentDate, diseaseName, unitReviewResult, classification 갱신
       - updateStatus(ReviewStatus) 메서드: canTransitionTo 검증 후 전이
       - updateSsn(String ssnPlain, String ssnHash) 메서드
       - recordNotification() 메서드: notificationDate = LocalDateTime.now(), status를 NOTIFIED로 전이

    5. ReviewHistory.java 생성:
       ```java
       @Entity @Table(name = "TB_REVIEW_HISTORY")
       @Getter @NoArgsConstructor(access = AccessLevel.PROTECTED) @AllArgsConstructor @Builder
       public class ReviewHistory {
           @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
           private Long id;
           @Column(name = "review_id", nullable = false)
           private Long reviewId;
           @Column(name = "review_round", nullable = false)
           private Integer reviewRound;
           @JdbcTypeCode(SqlTypes.JSON)
           @Column(name = "snapshot", nullable = false, columnDefinition = "jsonb")
           private String snapshot;
           @Column(name = "changed_at", nullable = false)
           @Builder.Default
           private LocalDateTime changedAt = LocalDateTime.now();
           @Column(name = "changed_by", length = 50)
           private String changedBy;
       }
       ```

    6. ReviewSnapshot.java 생성 (record DTO, ssn 제외):
       ```java
       package com.navy.casualty.review.dto;
       public record ReviewSnapshot(
           Integer reviewRound, LocalDate reviewDate, String name, String serviceNumber,
           LocalDate birthDate, LocalDate enlistmentDate, Long unitId, Long rankId, Long branchId,
           String diseaseName, String unitReviewResult, String classification,
           String status, LocalDateTime notificationDate
       ) {
           public static ReviewSnapshot from(Review review) {
               return new ReviewSnapshot(
                   review.getReviewRound(), review.getReviewDate(), review.getName(),
                   review.getServiceNumber(), review.getBirthDate(), review.getEnlistmentDate(),
                   review.getUnitId(), review.getRankId(), review.getBranchId(),
                   review.getDiseaseName(), review.getUnitReviewResult(),
                   review.getClassification() != null ? review.getClassification().name() : null,
                   review.getStatus() != null ? review.getStatus().name() : null,
                   review.getNotificationDate()
               );
           }
       }
       ```
       주의: ssnEncrypted/ssnHash는 절대 포함하지 않는다 (민감정보 보호).

    7. ReviewCreateRequest.java (record DTO):
       - @NotNull Integer reviewRound, LocalDate reviewDate, @NotBlank String name, @NotBlank String serviceNumber, String ssn, LocalDate birthDate, Long rankId, Long branchId, Long unitId, LocalDate enlistmentDate, String diseaseName, String unitReviewResult, ReviewClassification classification

    8. ReviewUpdateRequest.java (record DTO):
       - Integer reviewRound, LocalDate reviewDate, @NotBlank String name, String ssn, LocalDate birthDate, Long rankId, Long branchId, Long unitId, LocalDate enlistmentDate, String diseaseName, String unitReviewResult, ReviewClassification classification

    9. ReviewSearchRequest.java (record DTO, 모든 필드 nullable):
       - Long branchId, String serviceNumber, String name, LocalDate birthDate, Long rankId, Long unitId, ReviewClassification classification, ReviewStatus status

    10. ReviewResponse.java (record DTO + from 팩토리):
        ```java
        public record ReviewResponse(
            Long id, Integer reviewRound, LocalDate reviewDate,
            String name, String serviceNumber, String ssnMasked,
            LocalDate birthDate, String rankName, String branchName, String unitName,
            LocalDate enlistmentDate, String diseaseName, String unitReviewResult,
            String classificationName, String status, LocalDateTime notificationDate,
            LocalDateTime createdAt
        ) {
            public static ReviewResponse from(Review r, String maskedSsn,
                String rankName, String branchName, String unitName) {
                return new ReviewResponse(
                    r.getId(), r.getReviewRound(), r.getReviewDate(),
                    r.getName(), r.getServiceNumber(), maskedSsn,
                    r.getBirthDate(), rankName, branchName, unitName,
                    r.getEnlistmentDate(), r.getDiseaseName(), r.getUnitReviewResult(),
                    r.getClassification() != null ? r.getClassification().getLabel() : "",
                    r.getStatus().name(), r.getNotificationDate(), r.getCreatedAt()
                );
            }
        }
        ```

    11. ReviewDeleteRequest.java: `public record ReviewDeleteRequest(@NotBlank String reason) {}`

    12. ReviewHistoryResponse.java:
        ```java
        public record ReviewHistoryResponse(
            Long id, Long reviewId, Integer reviewRound,
            ReviewSnapshot snapshot, LocalDateTime changedAt, String changedBy
        ) {
            public static ReviewHistoryResponse from(ReviewHistory h, ObjectMapper mapper) {
                ReviewSnapshot snap = null;
                try { snap = mapper.readValue(h.getSnapshot(), ReviewSnapshot.class); }
                catch (Exception e) { /* log.warn */ }
                return new ReviewHistoryResponse(
                    h.getId(), h.getReviewId(), h.getReviewRound(),
                    snap, h.getChangedAt(), h.getChangedBy()
                );
            }
        }
        ```

    13. ReviewRepository.java:
        ```java
        public interface ReviewRepository extends JpaRepository<Review, Long>, ReviewRepositoryCustom {
            boolean existsByServiceNumberAndReviewRound(String serviceNumber, int reviewRound);
            boolean existsBySsnHash(String ssnHash);
            Optional<Review> findByServiceNumber(String serviceNumber);
        }
        ```

    14. ReviewRepositoryCustom.java + ReviewRepositoryImpl.java (Dead/Wounded QueryDSL 패턴 복제):
        - search(ReviewSearchRequest, Pageable) -> Page<Review>
        - searchAll(ReviewSearchRequest) -> List<Review>
        - 검색 조건: branchId, serviceNumber(contains), name(contains), birthDate, rankId, unitId, classification, status

    15. ReviewHistoryRepository.java:
        ```java
        public interface ReviewHistoryRepository extends JpaRepository<ReviewHistory, Long> {
            List<ReviewHistory> findByReviewIdOrderByChangedAtDesc(Long reviewId);
        }
        ```

    16. ReviewClassificationTest.java 생성 (TDD - behavior 검증):
        

    17. ReviewStatusTest.java 생성 (TDD - 상태 전이 검증, DeadStatusTest 패턴 복제):
        
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*ReviewClassificationTest" --tests "*ReviewStatusTest" -x processTestAot 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/main/resources/db/migration/V16__add_review_columns.sql 존재
    - grep "ssn_hash" V16 파일에 결과 있음
    - grep "rank_id" V16 파일에 결과 있음
    - grep "branch_id" V16 파일에 결과 있음
    - grep "status" V16 파일에 결과 있음
    - grep "COMBAT_WOUND" ReviewClassification.java 결과 있음
    - grep "REJECTED" ReviewClassification.java 결과 있음
    - grep "DEFERRED" ReviewClassification.java 결과 있음
    - grep "canTransitionTo" ReviewStatus.java 결과 있음
    - grep "UNDER_REVIEW" ReviewStatus.java 결과 있음
    - grep "@Entity" Review.java 결과 있음
    - grep "TB_REVIEW" Review.java 결과 있음
    - grep "PiiEncryptionConverter" Review.java 결과 있음
    - grep "classification" Review.java 결과 있음
    - grep "notificationDate" Review.java 결과 있음
    - grep "reviewRound" Review.java 결과 있음
    - grep "JSONB\|jsonb\|SqlTypes.JSON" ReviewHistory.java 결과 있음
    - grep "snapshot" ReviewHistory.java 결과 있음
    - grep "ssnEncrypted" ReviewSnapshot.java 결과 없음 (민감정보 제외)
    - grep "BooleanBuilder" ReviewRepositoryImpl.java 결과 있음
    - grep "findByReviewIdOrderByChangedAtDesc" ReviewHistoryRepository.java 결과 있음
    - ./gradlew compileJava 성공
    - ReviewClassificationTest.java 존재 (4개 라벨 테스트)
    - ReviewStatusTest.java 존재 (5개 상태 전이 테스트)
    - ./gradlew test --tests *ReviewClassificationTest --tests *ReviewStatusTest 전체 통과
  </acceptance_criteria>
  <done>Review 엔티티(심사차수+분류+보훈청통보+상태), ReviewHistory 엔티티(JSONB 스냅샷), ReviewClassification 4-value enum, ReviewStatus 4단계 전이, ReviewSnapshot(ssn 제외), 7개 DTO, QueryDSL Repository, Flyway V16 완성. 컴파일 성공.</done>
</task>

<task type="auto">
  <name>Task 2: ReviewService (이력 스냅샷 + 자동반영 + 보훈청통보) + ReviewController + 테스트</name>
  <files>
    backend/src/main/java/com/navy/casualty/review/service/ReviewService.java
    backend/src/main/java/com/navy/casualty/review/controller/ReviewController.java
  </files>
  <read_first>
    backend/src/main/java/com/navy/casualty/dead/service/DeadService.java
    backend/src/main/java/com/navy/casualty/dead/controller/DeadController.java
    backend/src/main/java/com/navy/casualty/wounded/service/WoundedService.java
    backend/src/main/java/com/navy/casualty/dead/repository/DeadRepository.java
    backend/src/main/java/com/navy/casualty/wounded/repository/WoundedRepository.java
    backend/src/main/java/com/navy/casualty/review/entity/Review.java
    backend/src/main/java/com/navy/casualty/review/entity/ReviewClassification.java
    backend/src/main/java/com/navy/casualty/review/entity/ReviewStatus.java
    backend/src/main/java/com/navy/casualty/review/entity/ReviewHistory.java
    backend/src/main/java/com/navy/casualty/review/dto/ReviewSnapshot.java
    backend/src/main/java/com/navy/casualty/review/dto/ReviewCreateRequest.java
    backend/src/main/java/com/navy/casualty/review/dto/ReviewUpdateRequest.java
    backend/src/main/java/com/navy/casualty/review/dto/ReviewResponse.java
    backend/src/main/java/com/navy/casualty/review/dto/ReviewHistoryResponse.java
    backend/src/main/java/com/navy/casualty/review/repository/ReviewRepository.java
    backend/src/main/java/com/navy/casualty/review/repository/ReviewHistoryRepository.java
    backend/src/main/java/com/navy/casualty/common/crypto/RrnMaskingUtil.java
    backend/src/main/java/com/navy/casualty/audit/annotation/AuditLog.java
    backend/src/test/java/com/navy/casualty/dead/DeadServiceTest.java
  </read_first>
  <action>
    1. ReviewService.java 생성 (DeadService 복제 + 3가지 신규 로직):
       - @Service @RequiredArgsConstructor @Transactional(readOnly = true)
       - 의존성: ReviewRepository, ReviewHistoryRepository, RankCodeRepository, BranchCodeRepository, UnitCodeRepository, DeadRepository, WoundedRepository, ObjectMapper
       
       **기본 CRUD (Dead/Wounded 패턴 동일):**
       - search(ReviewSearchRequest, Pageable) -> Page<ReviewResponse>
       - @AuditLog(action = "CREATE", targetTable = "TB_REVIEW") @Transactional create(ReviewCreateRequest):
         - 군번+심사차수 중복 검증: existsByServiceNumberAndReviewRound
         - 주민번호 해시 중복은 심사 특성상 동일인 복수 심사 가능하므로 검증하지 않음
         - Builder: reviewRound, reviewDate, name, serviceNumber, ssnEncrypted, ssnHash, birthDate, rankId, branchId, unitId, enlistmentDate, diseaseName, unitReviewResult, classification
         - 등록 시 classification이 있으면 자동반영 로직 호출
       
       **신규 1: 이력 스냅샷 자동 저장 (REVW-06, AUDIT-07):**
       - @AuditLog(action = "UPDATE", targetTable = "TB_REVIEW") @Transactional update(Long id, ReviewUpdateRequest):
         - (1) 변경 전 스냅샷 저장:
           ```java
           String snapshot = objectMapper.writeValueAsString(ReviewSnapshot.from(review));
           ReviewHistory history = ReviewHistory.builder()
               .reviewId(review.getId())
               .reviewRound(review.getReviewRound())
               .snapshot(snapshot)
               .changedBy(getCurrentUsername())
               .build();
           reviewHistoryRepository.save(history);
           ```
         - (2) 이전 classification 임시 저장 (자동반영 비교용)
         - (3) review.update(request) 호출
         - (4) 주민번호 변경 시 해시 재계산
         - (5) classification이 변경된 경우 자동반영 로직 호출
       
       **신규 2: 크로스-도메인 자동 반영 (REVW-07) — RESEARCH Pattern 3 기반:**
       - private void applyClassificationToRecord(Review review):
         - review.getClassification()이 null이면 즉시 return
         - String sn = review.getServiceNumber()
         - classification별 4분기 처리 (switch 사용):

           **(a) COMBAT_WOUND(전공상):**
           1. Dead 우선 탐색: deadRepository.findByServiceNumber(sn)
              - Dead 존재시: dead.updateDeathType(전공상에 해당하는 deathTypeId) + dead.updateStatus(DeadStatus.CONFIRMED) (canTransitionTo 검증 포함)
              - Dead 미존재시: Wounded 탐색으로 fallback
           2. Wounded fallback: woundedRepository.findByServiceNumber(sn)
              - Wounded 존재시: wounded.updateWoundType(WoundType.COMBAT_WOUND) + wounded.updateStatus(WoundedStatus.CONFIRMED)
              - 둘 다 미존재시: log.info("COMBAT_WOUND 자동반영 대상 없음: serviceNumber={}", sn) -> skip (에러 아님)

           **(b) DUTY_WOUND(공상):**
           1. woundedRepository.findByServiceNumber(sn)
              - Wounded 존재시: wounded.updateWoundType(WoundType.DUTY_WOUND) + wounded.updateStatus(WoundedStatus.CONFIRMED)
              - Wounded 미존재시: log.info("DUTY_WOUND 자동반영 대상 없음: serviceNumber={}", sn) -> skip

           **(c) REJECTED(기각):**
           - Dead/Wounded 필드 변경 없음. log.info("심사 기각 처리: reviewId={}", review.getId()) 로그만 기록

           **(d) DEFERRED(보류):**
           - Dead/Wounded 필드 변경 없음. 로그만 기록

         - 중요: 단일 @Transactional 내에서 Review + History + Dead/Wounded 모두 처리. REQUIRES_NEW 사용 금지.
         - 중요: Dead/Wounded의 updateStatus()는 각 엔티티의 canTransitionTo() 검증을 내부에서 수행. 전이 불가 시 IllegalStateException -> 로그 경고 후 skip (트랜잭션 롤백 안 함).
       
       **신규 3: 보훈청 통보 일시 기록 (REVW-08):**
       - @AuditLog(action = "UPDATE", targetTable = "TB_REVIEW") @Transactional recordNotification(Long id):
         - review.recordNotification() 호출 -> notificationDate = LocalDateTime.now() + status -> NOTIFIED 전이
       
       **이력 조회 (REVW-06):**
       - getHistories(Long reviewId) -> List<ReviewHistoryResponse>:
         - reviewHistoryRepository.findByReviewIdOrderByChangedAtDesc(reviewId)
         - ReviewHistoryResponse.from(history, objectMapper) 변환
       
       **기타:**
       - @AuditLog(action = "DELETE", targetTable = "TB_REVIEW") @Transactional softDelete(Long id, String reason)
       - @Transactional updateStatus(Long id, ReviewStatus newStatus)
       - private toResponse(Review) -> ReviewResponse (역할별 주민번호 마스킹 + 코드명 조회)
       - private hashSsn(String ssn) -> SHA-256 hex (Dead/WoundedService와 동일)
       - private getCurrentUserRole(), getCurrentUsername()

    2. ReviewController.java 생성 (DeadController 복제 + 이력/통보 엔드포인트 추가):
       - @RestController @RequestMapping("/api/reviews") @RequiredArgsConstructor
       - GET / : search (REVW-01)
       - POST / : create -> 201 응답 (REVW-02)
       - PUT /{id} : update (REVW-03)
       - DELETE /{id} : softDelete (REVW-04)
       - PUT /{id}/status : updateStatus
       - GET /{id}/histories : getHistories (REVW-06) -> List<ReviewHistoryResponse>
       - PUT /{id}/notify : recordNotification (REVW-08)
       - 보안: GET은 hasRole('VIEWER'), POST/PUT은 hasRole('OPERATOR'), DELETE/상태변경/통보는 hasRole('MANAGER')
  </action>
  <verify>
    <automated>cd backend && ./gradlew compileJava -x processAot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/main/java/com/navy/casualty/review/service/ReviewService.java 존재
    - grep "@AuditLog.*CREATE.*TB_REVIEW" ReviewService.java 결과 있음
    - grep "@AuditLog.*UPDATE.*TB_REVIEW" ReviewService.java 결과 있음
    - grep "@AuditLog.*DELETE.*TB_REVIEW" ReviewService.java 결과 있음
    - grep "reviewHistoryRepository.save" ReviewService.java 결과 있음
    - grep "ReviewSnapshot.from" ReviewService.java 결과 있음
    - grep "objectMapper.writeValueAsString" ReviewService.java 결과 있음
    - grep "deadRepository" ReviewService.java 결과 있음
    - grep "woundedRepository" ReviewService.java 결과 있음
    - grep "applyClassificationToRecord" ReviewService.java 결과 있음
    - grep "recordNotification" ReviewService.java 결과 있음
    - grep "existsByServiceNumberAndReviewRound" ReviewService.java 결과 있음
    - grep "RrnMaskingUtil.mask" ReviewService.java 결과 있음
    - backend/src/main/java/com/navy/casualty/review/controller/ReviewController.java 존재
    - grep "@RequestMapping.*api/reviews" ReviewController.java 결과 있음
    - grep "histories" ReviewController.java 결과 있음
    - grep "notify" ReviewController.java 결과 있음
    - grep "@PostMapping" ReviewController.java 결과 있음
    - grep "@DeleteMapping" ReviewController.java 결과 있음
    - ./gradlew compileJava 성공
    - ./gradlew test --tests *ReviewClassificationTest --tests *ReviewStatusTest 전체 통과 (상태 전이 단위 테스트 검증)
  </acceptance_criteria>
  <done>ReviewService(CRUD + 이력 스냅샷 자동저장 + Dead/Wounded 자동반영 + 보훈청 통보 + 감사로그), ReviewController(REST API 7개 엔드포인트) 완성. 단일 트랜잭션 내 크로스-도메인 업데이트 보장.</done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew compileJava -x processAot` 컴파일 성공
- V16 마이그레이션에 ssn_hash + rank_id + branch_id + status 컬럼
- ReviewClassification 4-value enum (COMBAT_WOUND/DUTY_WOUND/REJECTED/DEFERRED)
- ReviewStatus 4단계 전이 (REGISTERED->UNDER_REVIEW->CONFIRMED->NOTIFIED)
- ReviewHistory에 JSONB 스냅샷 컬럼 매핑
- ReviewSnapshot에 ssnEncrypted 미포함
- ReviewService.update()에서 변경 전 스냅샷 자동 저장
- ReviewService에서 Dead/Wounded 크로스-도메인 자동 반영
- ReviewController에 /histories, /notify 엔드포인트 존재
</verification>

<success_criteria>
- GET /api/reviews 검색 조건 + 페이징 동작
- POST /api/reviews 등록 (군번+심사차수 중복 검증)
- PUT /api/reviews/{id} 수정 + TB_REVIEW_HISTORY 스냅샷 자동 저장
- DELETE /api/reviews/{id} 논리 삭제 (사유 필수)
- GET /api/reviews/{id}/histories 이력 목록 반환
- PUT /api/reviews/{id}/notify 보훈청 통보 일시 기록
- classification 변경 시 Dead/Wounded 자동 반영
- 컴파일 성공
</success_criteria>

<output>
After completion, create `.planning/phases/05-review-management/05-01-SUMMARY.md`
</output>
