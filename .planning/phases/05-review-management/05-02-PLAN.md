---
phase: 05-review-management
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - frontend/src/types/review.ts
  - frontend/src/api/review.ts
  - frontend/src/pages/review/ReviewListPage.tsx
  - frontend/src/pages/review/ReviewFormModal.tsx
  - frontend/src/pages/review/ReviewDeleteModal.tsx
  - frontend/src/pages/review/ReviewHistoryDrawer.tsx
  - frontend/src/routes/index.tsx
autonomous: true
requirements: [REVW-01, REVW-02, REVW-03, REVW-04, REVW-06, REVW-08]

must_haves:
  truths:
    - "전공사상심사 메뉴 클릭 시 /review 경로에서 ReviewListPage가 렌더링된다"
    - "검색 폼에서 군구분/군번/성명/생년월일/계급/소속/분류/상태 복합 검색이 가능하다"
    - "검색 폼 접기/펼치기 토글이 동작한다"
    - "조회 버튼 클릭 시 1페이지로 리셋되며 서버사이드 Table이 갱신된다"
    - "등록 Modal에서 심사차수, 심사일자, 병명, 소속부대 심사결과, 전공상 분류 필드가 존재한다"
    - "목록에서 이력 버튼 클릭 시 Drawer가 열리고 Timeline으로 심사차수별 이력이 표시된다"
    - "보훈청 통보 버튼 클릭 시 통보 일시가 기록된다"
    - "등록/수정/삭제 후 목록이 자동 갱신된다 (invalidateQueries)"
  artifacts:
    - path: "frontend/src/types/review.ts"
      provides: "ReviewRecord, ReviewSearchParams, ReviewHistoryRecord, ReviewSnapshot 타입"
      contains: "ReviewRecord"
    - path: "frontend/src/api/review.ts"
      provides: "react-query 훅 (useReviewList, useCreateReview, useUpdateReview, useDeleteReview, useReviewHistories, useRecordNotification)"
      contains: "useReviewList"
    - path: "frontend/src/pages/review/ReviewListPage.tsx"
      provides: "전공사상심사 목록 + 검색 폼 화면"
      contains: "useReviewList"
    - path: "frontend/src/pages/review/ReviewFormModal.tsx"
      provides: "등록/수정 Modal (심사차수 + 분류 Select)"
      contains: "reviewRound"
    - path: "frontend/src/pages/review/ReviewHistoryDrawer.tsx"
      provides: "Ant Design Timeline + Drawer 이력 타임라인"
      contains: "Timeline"
  key_links:
    - from: "ReviewListPage.tsx"
      to: "api/review.ts"
      via: "useReviewList hook"
      pattern: "useReviewList"
    - from: "ReviewHistoryDrawer.tsx"
      to: "api/review.ts"
      via: "useReviewHistories hook"
      pattern: "useReviewHistories"
    - from: "ReviewFormModal.tsx"
      to: "api/review.ts"
      via: "useCreateReview / useUpdateReview hooks"
      pattern: "useCreateReview|useUpdateReview"
    - from: "routes/index.tsx"
      to: "ReviewListPage.tsx"
      via: "route definition"
      pattern: "path.*review.*ReviewListPage"
---

<objective>
전공사상심사 프론트엔드 전체를 구현한다: TypeScript 타입, react-query API 훅, 목록 페이지(검색 폼 + Table), 등록/수정/삭제 Modal, 심사 이력 타임라인 Drawer, 보훈청 통보 버튼, 라우팅 연결.

Purpose: Phase 5 프론트엔드. Dead/Wounded UI 패턴을 복제하면서 (1) Ant Design Timeline + Drawer 이력 타임라인 (2) 보훈청 통보 버튼 두 가지 신규 UI를 추가한다.
Output: /review 경로에 전공사상심사 관리 화면 + 이력 Drawer 완성
</objective>

<execution_context>
@C:\Users\User\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\User\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-review-management/05-01-SUMMARY.md
@.planning/phases/05-review-management/05-RESEARCH.md

<interfaces>
<!-- 기존 프론트엔드 인프라 -->

From frontend/src/api/client.ts:
```typescript
import axios from 'axios';
const apiClient = axios.create({ baseURL: '/api', timeout: 30000 });
export default apiClient;
```

From frontend/src/hooks/useAuth.tsx:
```typescript
interface AuthContextValue {
  user: AuthUser | null; login, logout, isAdmin, isManagerOrAbove, hasRole(role: Role): boolean;
}
```

From frontend/src/types/dead.ts (공통 타입 재사용):
```typescript
export interface PageData<T> { content: T[]; totalElements: number; totalPages: number; size: number; number: number; }
export interface RankCode { id: number; rankName: string; }
export interface BranchCode { id: number; branchName: string; }
export interface UnitCode { id: number; unitName: string; }
```

From frontend/src/api/dead.ts (코드 훅 재사용):
```typescript
export function useRanks() { ... }
export function useBranches() { ... }
export function useUnits() { ... }
```

From frontend/src/utils/rrnValidation.ts:
```typescript
export function validateRrn(rrn: string | null | undefined): boolean;
```

From frontend/src/routes/index.tsx:
```typescript
{ path: 'review', element: <PlaceholderPage title="전공사상심사" /> },
// -> ReviewListPage로 교체 필요
```

From backend API 계약 (05-01에서 생성):
- GET /api/reviews?name=홍&page=0&size=10 -> ApiResponse<Page<ReviewResponse>>
- POST /api/reviews -> 201 ApiResponse<ReviewResponse>
- PUT /api/reviews/{id} -> ApiResponse<ReviewResponse>
- DELETE /api/reviews/{id} body: {reason: "..."} -> ApiResponse<Void>
- PUT /api/reviews/{id}/status?status=UNDER_REVIEW -> ApiResponse<Void>
- GET /api/reviews/{id}/histories -> ApiResponse<List<ReviewHistoryResponse>>
- PUT /api/reviews/{id}/notify -> ApiResponse<Void>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript 타입 + react-query API 훅 (이력 + 보훈청 통보 포함)</name>
  <files>
    frontend/src/types/review.ts
    frontend/src/api/review.ts
  </files>
  <read_first>
    frontend/src/types/dead.ts
    frontend/src/types/wounded.ts
    frontend/src/api/dead.ts
    frontend/src/api/wounded.ts
    frontend/src/api/client.ts
    frontend/src/hooks/useAuth.tsx
    frontend/src/utils/rrnValidation.ts
  </read_first>
  <action>
    1. frontend/src/types/review.ts 생성:
       ```typescript
       // 공통 타입 재사용
       export type { PageData, RankCode, BranchCode, UnitCode } from './dead';

       export type ReviewClassification = 'COMBAT_WOUND' | 'DUTY_WOUND' | 'REJECTED' | 'DEFERRED';

       export const CLASSIFICATION_LABELS: Record<ReviewClassification, string> = {
         COMBAT_WOUND: '전공상',
         DUTY_WOUND: '공상',
         REJECTED: '기각',
         DEFERRED: '보류',
       };

       export interface ReviewRecord {
         id: number;
         reviewRound: number;
         reviewDate: string | null;
         name: string;
         serviceNumber: string;
         ssnMasked: string;
         birthDate: string | null;
         rankName: string | null;
         branchName: string | null;
         unitName: string | null;
         enlistmentDate: string | null;
         diseaseName: string | null;
         unitReviewResult: string | null;
         classificationName: string | null;
         status: 'REGISTERED' | 'UNDER_REVIEW' | 'CONFIRMED' | 'NOTIFIED';
         notificationDate: string | null;
         createdAt: string;
       }

       export interface ReviewSearchParams {
         branchId?: number;
         serviceNumber?: string;
         name?: string;
         birthDate?: string;
         rankId?: number;
         unitId?: number;
         classification?: ReviewClassification;
         status?: string;
         page?: number;
         size?: number;
       }

       export interface ReviewCreateForm {
         reviewRound: number;
         reviewDate?: string;
         name: string;
         serviceNumber: string;
         ssn?: string;
         birthDate?: string;
         rankId?: number;
         branchId?: number;
         unitId?: number;
         enlistmentDate?: string;
         diseaseName?: string;
         unitReviewResult?: string;
         classification?: ReviewClassification;
       }

       export interface ReviewUpdateForm {
         reviewRound?: number;
         reviewDate?: string;
         name: string;
         ssn?: string;
         birthDate?: string;
         rankId?: number;
         branchId?: number;
         unitId?: number;
         enlistmentDate?: string;
         diseaseName?: string;
         unitReviewResult?: string;
         classification?: ReviewClassification;
       }

       // 이력 관련 타입 (REVW-06)
       export interface ReviewSnapshot {
         reviewRound: number;
         reviewDate: string | null;
         name: string;
         serviceNumber: string;
         birthDate: string | null;
         enlistmentDate: string | null;
         unitId: number | null;
         rankId: number | null;
         branchId: number | null;
         diseaseName: string | null;
         unitReviewResult: string | null;
         classification: string | null;
         status: string | null;
         notificationDate: string | null;
       }

       export interface ReviewHistoryRecord {
         id: number;
         reviewId: number;
         reviewRound: number;
         snapshot: ReviewSnapshot;
         changedAt: string;
         changedBy: string;
       }
       ```

    2. frontend/src/api/review.ts 생성:
       ** 중요: onError 콜백에서 `any` 타입 사용 금지. axios.isAxiosError() 타입 가드 사용. **
       ** 코드 테이블 훅은 dead.ts에서 re-export **
       ```typescript
       import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
       import axios from 'axios';
       import { message } from 'antd';
       import apiClient from './client';
       import type { ReviewRecord, ReviewSearchParams, ReviewCreateForm, ReviewUpdateForm, ReviewHistoryRecord } from '../types/review';
       import type { PageData } from '../types/dead';

       export { useRanks, useBranches, useUnits } from './dead';

       interface ApiResponse<T> { success: boolean; status: number; message: string; data: T; }

       function getErrorMessage(error: unknown, fallback: string): string {
         if (axios.isAxiosError<{ message: string }>(error)) {
           return error.response?.data?.message || fallback;
         }
         return fallback;
       }

       export const reviewKeys = {
         all: ['reviews'] as const,
         lists: () => [...reviewKeys.all, 'list'] as const,
         list: (params: ReviewSearchParams) => [...reviewKeys.lists(), params] as const,
         histories: (id: number) => [...reviewKeys.all, 'histories', id] as const,
       };

       export function useReviewList(params: ReviewSearchParams) {
         return useQuery({
           queryKey: reviewKeys.list(params),
           queryFn: async () => {
             const res = await apiClient.get<ApiResponse<PageData<ReviewRecord>>>('/reviews', { params });
             return res.data.data;
           },
         });
       }

       export function useCreateReview() {
         const qc = useQueryClient();
         return useMutation({
           mutationFn: (data: ReviewCreateForm) => apiClient.post('/reviews', data).then(r => r.data),
           onSuccess: () => { qc.invalidateQueries({ queryKey: reviewKeys.lists() }); message.success('등록되었습니다'); },
           onError: (error: unknown) => { message.error(getErrorMessage(error, '등록에 실패했습니다')); },
         });
       }

       export function useUpdateReview() {
         const qc = useQueryClient();
         return useMutation({
           mutationFn: ({ id, data }: { id: number; data: ReviewUpdateForm }) => apiClient.put(`/reviews/${id}`, data).then(r => r.data),
           onSuccess: () => { qc.invalidateQueries({ queryKey: reviewKeys.lists() }); message.success('수정되었습니다'); },
           onError: (error: unknown) => { message.error(getErrorMessage(error, '수정에 실패했습니다')); },
         });
       }

       export function useDeleteReview() {
         const qc = useQueryClient();
         return useMutation({
           mutationFn: ({ id, reason }: { id: number; reason: string }) => apiClient.delete(`/reviews/${id}`, { data: { reason } }).then(r => r.data),
           onSuccess: () => { qc.invalidateQueries({ queryKey: reviewKeys.lists() }); message.success('삭제되었습니다'); },
           onError: (error: unknown) => { message.error(getErrorMessage(error, '삭제에 실패했습니다')); },
         });
       }

       export function useUpdateReviewStatus() {
         const qc = useQueryClient();
         return useMutation({
           mutationFn: ({ id, status }: { id: number; status: string }) => apiClient.put(`/reviews/${id}/status`, null, { params: { status } }).then(r => r.data),
           onSuccess: () => { qc.invalidateQueries({ queryKey: reviewKeys.lists() }); message.success('상태가 변경되었습니다'); },
         });
       }

       // 이력 조회 (REVW-06)
       export function useReviewHistories(reviewId: number | null) {
         return useQuery({
           queryKey: reviewKeys.histories(reviewId!),
           queryFn: async () => {
             const res = await apiClient.get<ApiResponse<ReviewHistoryRecord[]>>(`/reviews/${reviewId}/histories`);
             return res.data.data;
           },
           enabled: reviewId !== null,
         });
       }

       // 보훈청 통보 일시 기록 (REVW-08)
       export function useRecordNotification() {
         const qc = useQueryClient();
         return useMutation({
           mutationFn: (id: number) => apiClient.put(`/reviews/${id}/notify`).then(r => r.data),
           onSuccess: () => { qc.invalidateQueries({ queryKey: reviewKeys.lists() }); message.success('보훈청 통보가 기록되었습니다'); },
           onError: (error: unknown) => { message.error(getErrorMessage(error, '보훈청 통보 기록에 실패했습니다')); },
         });
       }
       ```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/types/review.ts 파일 존재
    - grep "ReviewRecord" frontend/src/types/review.ts 결과 있음
    - grep "ReviewClassification" frontend/src/types/review.ts 결과 있음
    - grep "CLASSIFICATION_LABELS" frontend/src/types/review.ts 결과 있음
    - grep "ReviewHistoryRecord" frontend/src/types/review.ts 결과 있음
    - grep "ReviewSnapshot" frontend/src/types/review.ts 결과 있음
    - grep "notificationDate" frontend/src/types/review.ts 결과 있음
    - grep "reviewRound" frontend/src/types/review.ts 결과 있음
    - frontend/src/api/review.ts 파일 존재
    - grep "useReviewList" frontend/src/api/review.ts 결과 있음
    - grep "useCreateReview" frontend/src/api/review.ts 결과 있음
    - grep "useReviewHistories" frontend/src/api/review.ts 결과 있음
    - grep "useRecordNotification" frontend/src/api/review.ts 결과 있음
    - grep "isAxiosError" frontend/src/api/review.ts 결과 있음
    - grep -c "any" frontend/src/api/review.ts 결과 0 (any 타입 없음)
    - npx tsc --noEmit 에러 없음
  </acceptance_criteria>
  <done>전공사상심사 TypeScript 타입(ReviewRecord, ReviewHistoryRecord, ReviewSnapshot, ReviewClassification 포함), react-query API 훅 8개(CRUD 4 + 상태변경 1 + 이력조회 1 + 보훈청통보 1 + 코드 re-export) 완성.</done>
</task>

<task type="auto">
  <name>Task 2: 목록 페이지 + 등록/수정 Modal + 삭제 Modal + 이력 Drawer + 라우팅 연결</name>
  <files>
    frontend/src/pages/review/ReviewListPage.tsx
    frontend/src/pages/review/ReviewFormModal.tsx
    frontend/src/pages/review/ReviewDeleteModal.tsx
    frontend/src/pages/review/ReviewHistoryDrawer.tsx
    frontend/src/routes/index.tsx
  </files>
  <read_first>
    frontend/src/api/review.ts
    frontend/src/types/review.ts
    frontend/src/utils/rrnValidation.ts
    frontend/src/hooks/useAuth.tsx
    frontend/src/pages/dead/DeadListPage.tsx
    frontend/src/pages/dead/DeadFormModal.tsx
    frontend/src/pages/dead/DeadDeleteModal.tsx
    frontend/src/pages/wounded/WoundedListPage.tsx
    frontend/src/pages/wounded/WoundedFormModal.tsx
    frontend/src/routes/index.tsx
  </read_first>
  <action>
    1. ReviewListPage.tsx 생성 (DeadListPage/WoundedListPage 패턴 복제 + 이력/통보 버튼 추가):
       - 검색 폼 Card 접기/펼치기 (searchExpanded state)
       - 검색 필드: 군구분 Select(useBranches), 군번 Input, 성명 Input, 생년월일 DatePicker, 계급 Select(useRanks), 소속 Select(useUnits), 분류 Select(4개 옵션: 전공상/공상/기각/보류), 상태 Select(4개 옵션: REGISTERED/UNDER_REVIEW/CONFIRMED/NOTIFIED)
       - 분류 Select 옵션:
         ```tsx
         <Select placeholder="분류 선택" allowClear>
           <Select.Option value="COMBAT_WOUND">전공상</Select.Option>
           <Select.Option value="DUTY_WOUND">공상</Select.Option>
           <Select.Option value="REJECTED">기각</Select.Option>
           <Select.Option value="DEFERRED">보류</Select.Option>
         </Select>
         ```
       - 조회 버튼: setPage(1) + 검색 파라미터 설정
       - Ant Design Table columns:
         - 번호, 심사차수, 군구분, 군번, 성명, 주민번호(마스킹), 계급, 소속, 심사일자, 병명, 소속부대 심사결과, 분류(classificationName), 상태, 보훈청 통보일, 등록일, 관리
         - 상태 Tag 색상: REGISTERED=blue, UNDER_REVIEW=orange, CONFIRMED=green, NOTIFIED=gold
         - 분류 Tag: COMBAT_WOUND=red, DUTY_WOUND=orange, REJECTED=default, DEFERRED=blue
       - 관리 컬럼 버튼:
         - 수정 (OPERATOR 이상)
         - 삭제 (MANAGER 이상)
         - 이력 버튼 (모든 역할): 클릭 시 ReviewHistoryDrawer 열기
         - 보훈청 통보 버튼 (MANAGER 이상, status가 CONFIRMED일 때만 활성): 클릭 시 확인 팝업 후 useRecordNotification 호출
       - 상단: 등록 버튼 + Excel 다운로드 버튼(disabled placeholder, Plan 03에서 활성화)
       - useReviewList(searchParams) 훅 사용
       - State: historyDrawerReviewId(number|null) -> ReviewHistoryDrawer 연동

    2. ReviewFormModal.tsx 생성 (DeadFormModal/WoundedFormModal 패턴 복제 + 심사 고유 필드):
       - props: open, onClose, editRecord?(ReviewRecord | null)
       - Ant Design Modal + Form:
         - 심사차수 InputNumber (필수, 최소 1)
         - 심사일자 DatePicker
         - 군번 Input (수정시 disabled), 성명 Input, 주민번호 Input (validateRrn 검증)
         - 생년월일 DatePicker (주민번호 자동 추출)
         - 계급 Select(useRanks), 군구분 Select(useBranches), 소속 Select(useUnits)
         - 입대일자 DatePicker
         - ** 심사 고유 필드 **:
           - 병명 Input (diseaseName, 최대 200자)
           - 소속부대 심사결과 Input (unitReviewResult)
           - 전공상 분류 Select (classification): 전공상/공상/기각/보류
         - Dead 대비 제거 필드: 사망구분, 사망코드, 사망일자, 전화번호, 주소
         - 필수 필드: 심사차수, 성명, 군번
       - onOk: useCreateReview.mutate() 또는 useUpdateReview.mutate()
       - 날짜: dayjs 변환, "YYYY-MM-DD" 형식 전송

    3. ReviewDeleteModal.tsx 생성 (DeadDeleteModal 복제):
       - props: open, onClose, recordId(number | null)
       - 삭제 사유 TextArea (필수)
       - useDeleteReview.mutate({ id, reason })

    4. ReviewHistoryDrawer.tsx 생성 (Ant Design Timeline + Drawer):
       ```tsx
       import { Drawer, Timeline, Descriptions, Tag, Spin, Empty } from 'antd';
       import { useReviewHistories } from '../../api/review';
       import { CLASSIFICATION_LABELS } from '../../types/review';
       import type { ReviewClassification } from '../../types/review';

       interface Props {
         reviewId: number | null;
         open: boolean;
         onClose: () => void;
       }

       export default function ReviewHistoryDrawer({ reviewId, open, onClose }: Props) {
         const { data: histories, isLoading } = useReviewHistories(reviewId);

         const getColor = (classification: string | null) => {
           if (classification === 'REJECTED') return 'red';
           if (classification === 'DEFERRED') return 'gray';
           return 'blue';
         };

         return (
           <Drawer title="심사 이력" open={open} onClose={onClose} width={600}>
             {isLoading && <Spin />}
             {!isLoading && (!histories || histories.length === 0) && (
               <Empty description="변경 이력이 없습니다" />
             )}
             {!isLoading && histories && histories.length > 0 && (
               <Timeline
                 items={histories.map(h => ({
                   color: getColor(h.snapshot?.classification),
                   children: (
                     <div key={h.id}>
                       <p><strong>{h.reviewRound}차 심사</strong> — {new Date(h.changedAt).toLocaleString('ko-KR')}</p>
                       <Descriptions size="small" column={1} bordered>
                         <Descriptions.Item label="분류">
                           {h.snapshot?.classification ? (
                             <Tag>{CLASSIFICATION_LABELS[h.snapshot.classification as ReviewClassification] || h.snapshot.classification}</Tag>
                           ) : '-'}
                         </Descriptions.Item>
                         <Descriptions.Item label="소속부대 심사결과">
                           {h.snapshot?.unitReviewResult || '-'}
                         </Descriptions.Item>
                         <Descriptions.Item label="병명">
                           {h.snapshot?.diseaseName || '-'}
                         </Descriptions.Item>
                         <Descriptions.Item label="상태">
                           {h.snapshot?.status || '-'}
                         </Descriptions.Item>
                         <Descriptions.Item label="변경자">
                           {h.changedBy || '-'}
                         </Descriptions.Item>
                       </Descriptions>
                     </div>
                   ),
                 }))}
               />
             )}
           </Drawer>
         );
       }
       ```

    5. frontend/src/routes/index.tsx 수정:
       - `import ReviewListPage from '../pages/review/ReviewListPage';` 추가
       - `{ path: 'review', element: <PlaceholderPage title="전공사상심사" /> }` 를
         `{ path: 'review', element: <ReviewListPage /> }` 로 교체
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/review/ReviewListPage.tsx 존재
    - grep "useReviewList" ReviewListPage.tsx 결과 있음
    - grep "searchExpanded" ReviewListPage.tsx 결과 있음
    - grep "COMBAT_WOUND\|DUTY_WOUND\|REJECTED\|DEFERRED" ReviewListPage.tsx 결과 있음
    - grep "UNDER_REVIEW" ReviewListPage.tsx 결과 있음
    - grep "reviewRound\|심사차수" ReviewListPage.tsx 결과 있음
    - grep "notificationDate\|통보" ReviewListPage.tsx 결과 있음
    - grep "useRecordNotification" ReviewListPage.tsx 결과 있음
    - grep "ReviewHistoryDrawer" ReviewListPage.tsx 결과 있음
    - frontend/src/pages/review/ReviewFormModal.tsx 존재
    - grep "reviewRound" ReviewFormModal.tsx 결과 있음
    - grep "classification" ReviewFormModal.tsx 결과 있음
    - grep "unitReviewResult" ReviewFormModal.tsx 결과 있음
    - grep "validateRrn" ReviewFormModal.tsx 결과 있음
    - grep "useCreateReview\|useUpdateReview" ReviewFormModal.tsx 결과 있음
    - frontend/src/pages/review/ReviewDeleteModal.tsx 존재
    - grep "useDeleteReview" ReviewDeleteModal.tsx 결과 있음
    - frontend/src/pages/review/ReviewHistoryDrawer.tsx 존재
    - grep "Timeline" ReviewHistoryDrawer.tsx 결과 있음
    - grep "Drawer" ReviewHistoryDrawer.tsx 결과 있음
    - grep "useReviewHistories" ReviewHistoryDrawer.tsx 결과 있음
    - grep "CLASSIFICATION_LABELS" ReviewHistoryDrawer.tsx 결과 있음
    - grep "Descriptions" ReviewHistoryDrawer.tsx 결과 있음
    - grep "ReviewListPage" frontend/src/routes/index.tsx 결과 있음
    - grep "PlaceholderPage.*전공사상" frontend/src/routes/index.tsx 결과 없음 (교체 완료)
    - npx tsc --noEmit 에러 없음
  </acceptance_criteria>
  <done>전공사상심사 목록 페이지(검색 폼 접기/펼치기 + 서버사이드 Table + 분류/상태 Tag + 이력/통보 버튼), 등록/수정 Modal(심사차수+분류+병명+소속부대심사결과), 삭제 Modal(사유 필수), 이력 타임라인 Drawer(Ant Design Timeline + Descriptions), 라우팅 연결 완성.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 에러 없음
- /review 경로에 ReviewListPage 연결됨
- 검색 폼 접기/펼치기, 조회 시 1페이지 리셋, 서버사이드 페이징
- 등록 Modal에 심사차수 + 분류 Select + 병명 + 소속부대 심사결과
- 이력 버튼 클릭 시 Drawer 열리고 Ant Design Timeline으로 이력 표시
- 보훈청 통보 버튼 클릭 시 통보 일시 기록
- API 훅 onError에 any 타입 없음
</verification>

<success_criteria>
- 전공사상심사 목록 화면이 /review 경로에서 렌더링됨
- 검색 폼 8개 필드 + 접기/펼치기
- Table에 심사차수, 분류, 소속부대 심사결과, 보훈청 통보일 컬럼 포함
- 등록 Modal에서 심사차수(필수), 분류 Select, 병명, 소속부대 심사결과
- Drawer에서 Timeline 컴포넌트로 심사 이력 시각화
- 보훈청 통보 버튼으로 통보 일시 기록
- TypeScript 컴파일 성공
</success_criteria>

<output>
After completion, create `.planning/phases/05-review-management/05-02-SUMMARY.md`
</output>
