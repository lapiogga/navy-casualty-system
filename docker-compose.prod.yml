# 운영(에어갭) 배포 설정
# 사용법: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
services:
  app:
    restart: always
    environment:
      SPRING_PROFILES_ACTIVE: prod
    volumes:
      - ./certs:/app/certs:ro
    # INFRA-05: TLS 활성화는 Phase 8에서 아래 환경변수 설정
    # SERVER_SSL_KEY_STORE: /app/certs/keystore.p12
    # SERVER_SSL_KEY_STORE_PASSWORD: ${SSL_KEYSTORE_PASSWORD}
    # SERVER_SSL_KEY_STORE_TYPE: PKCS12

  db:
    # 운영 환경에서는 외부 포트 노출하지 않음
    ports: !reset []
